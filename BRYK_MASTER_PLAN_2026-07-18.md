# BRYK — Master Plan

**Дата:** 2026-07-18 · **Канонический док трека** (правки — дописывать датированные секции сюда, sibling-доков не плодить)
**Роли:** Fable (Cowork) = архитектура/ревью · терминал = билд и прогон · Rob = глаз и перформанс
**Кодовое имя:** BRYK. Файлы трека нейм-спейсить `BRYK_*` / `Projects/bryk/`.

---

## 0. Видение

Визуализатор для диджей-сетов на вечеринке. **Полностью отдельная аппка** (решение Роба 2026-07-18): не дека MUSICVIZ, не слой его стека — самостоятельный single-file инструмент, который берёт из наработок только проверенные модули.

Три кита:

1. **Геометрия как язык.** Прямоугольник, круг, треугольник — и всё, что из них собирается: сетки, кольца, колонны, туннели. Строгие формы, которые музыка заставляет жить.
2. **Цвет.** В отличие от ч/б канона флота — BRYK полноцветный: палитры-банк + цветовые оси, управляемые фичами звука. Это его собственная визуальная идентичность.
3. **Микрофон + камера.** Звук зала — через mic/line-in (audio-core). Камера — двумя ролями (решение Роба): **слой в миксе** (видео со своим blend/opacity под/над геометрией) и **текстура внутри фигур** (фигуры режут живое видео как маски).

Ключевой инсайт остаётся тем же, что в MUSICVIZ: ничего не строим с нуля — но здесь мы **копируем модули внутрь bryk/** (snapshot, правило одного владельца файла), а не шарим живые зависимости.

---

## 1. Что берём из наработок (инвентарь переиспользования)

| Источник | Что берём | Как |
|---|---|---|
| `music-viz/audio-core.js` | весь DSP: 6 AGC-полос, rms/centroid/flux, onset (median+MAD), fast/slow огибающие, автокорр-BPM + PLL-фаза, tap-tempo, fixture-режим. API: `makeAudioCore(opts)` / `makeFixture(bpm)` (проверено 2026-07-18) | копия `bryk/audio-core.js` + его node-тест |
| MUSICVIZ mvj-inject | грамматика мод-строки (feature ▸ param ▸ mode ▸ depth ▸ curve ▸ atk/rel ▸ gate), скраб-контрол «число-в-треке · drag · type · exceed→extend», input-пикер устройств, ARM AUDIO, сцен-банк v4, disabled-матрица транспорта | портируем паттерны, не код целиком |
| particle-dance | детерминированные seeded-паттерны хореографии, reveal/replay/loop транспорт, panel grammar | модель «паттерн = чистая функция от (t, seed, params)» |
| image-shuffler | cover-fit врезка картинки в кадр произвольной пропорции; уроки paint-order (входящий элемент ПОД, центр — сверху); экспорт `captureStream` | cover-fit — прямо в камера-рельс; paint-order — в приёмы-каскады |
| Guilloché / мастер-план §1 | драматургия chaos→order как drop-эффект | приём «Собраться» (§4) |
| panel-canon | каждый range с number-инпутом, `extendParamRange`, ритм 4/8/12, декларативная сборка панели | обязательный канон панели |
| fleet-finalize | scene-only invert, hotkey-грамматика (H/F/B/T/I) | перформанс-режим |

Чего НЕ берём: iframe-деки, vj-shim, BroadcastChannel-пульт, blend-стек iframe'ов — это машинерия MUSICVIZ-hub'а, standalone-аппке не нужна.

---

## 2. Архитектура

```
 bryk/
 ├─ index.html      ★ вся аппка (single-file: панель + движок + приёмы)
 ├─ audio-core.js   snapshot DSP (script src, same-origin)
 ├─ audio-core.test.js
 └─ serve.sh        localhost-сервер (getUserMedia не работает с file://)
```

### 2.1 Слои = стопка канвасов (не iframe)

Всё нативно, в одном DOM. Композит — CSS `mix-blend-mode` + `opacity` на стакнутых канвасах (GPU-композит, приём проверен в MUSICVIZ, но там были iframes — здесь дешевле):

```
 #stage
 ├─ #bg    фон: цвет/градиент из палитры (CSS или canvas)
 ├─ #cam   камера-слой: <canvas>, blend ▾ + opacity  ← роль «слой в миксе»
 ├─ #geo   геометрия: главный canvas, фигуры + camera-fill  ← роль «текстура в фигурах»
 └─ #fx    оверлей: grain/strobe/vignette/posterize (интенсивность ← фичи)
```

Порядок слоёв переключаем (камера над/под геометрией) — это z-index swap, бесплатно.

### 2.2 Камера-рельс (video-core, новый модуль)

- Пикер устройств: `enumerateDevices('videoinput')` + `getUserMedia({video:{deviceId}})` — та же грамматика, что audio-пикер MUSICVIZ (label-unlock после первого grant).
- Скрытый `<video autoplay muted playsinline>` → каждый кадр **один** `drawImage` в offscreen-снапшот → из снапшота уже и #cam-слой (cover-fit по shuffler-формуле), и заливки фигур. Один decode на кадр, сколько бы фигур ни резало видео.
- **Текстура в фигурах:** `ctx.save() → path(фигуры) → ctx.clip() → drawImage(snapshot, coverFit) → ctx.restore()`. До ~30–40 клипов на кадр — норм; массовые сетки заливаются видео через один общий clip составного пути.
- **Стилизация видео — обязательна:** клубная камера тёмная и шумная. Постеризация/duotone/threshold в цвета активной палитры превращает шум в фактуру и вписывает видео в цветовую систему (не «сырая вебка на экране»). Это дефолт, «чистое видео» — опция.
- Нет камеры / нет grant → слой честно выключен, фигуры фолбэк на палитровую заливку (никаких чёрных дыр).

### 2.3 Геометрия-движок

Сцена = **раскладка × материал × хореография** (три ортогональных оси — отсюда вариатива):

- **Примитивы:** rect, circle, triangle. Инстанс: `{shape, x, y, scale, rot, fill, stroke, order}`.
- **Раскладки (composition):** solo · grid N×M · ring (кольцо из N) · stack (концентрика) · columns · scatter (seeded) · tunnel (перспективная труба из уменьшающихся контуров) · mirror (зеркальные пары). Раскладка — чистая функция `(params, seed) → instances[]` (модель particle-dance).
- **Материалы (fill):** палитра-цвет · камера (clip) · контур (stroke only) · градиент палитры. Микс внутри раскладки: например каждая 3-я фигура — камера, остальные — цвет.
- **Хореография:** см. §4 «Приёмы». Позиция/масштаб/поворот/цвет каждого инстанса модулируются фичами.

### 2.4 Звук → движение

- `audio-core` как есть: `Audio.features = {rms, centroid, flux, onset, bands{6}, env{fast/slow}, beat{bpm, phase, beatN, barN, phraseN}}`.
- **Уровень 1 — зашитые связки в приёмах** (главный): каждый приём декларирует свои мишени (`pulse: scale ← env.bassFast` и т.п.). Диджею не нужна матрица — приёмы уже музыкальны.
- **Уровень 2 — мод-рэк для тонкой настройки** (фаза P3): портированная грамматика мод-строки MUSICVIZ поверх любого параметра активной сцены. Не строим раньше, чем заработает уровень 1.
- Квантование: смена сцен/приёмов — по `onBar`, переброс раскладки — по `onPhrase` (переключение не рвёт музыку).

### 2.5 Цветовая система

- **Палитра-банк:** 6–8 кураторских палитр по 4–5 цветов (bg + 3–4 фигурных). Палитра — юнит переключения (клавиши/onBar), не отдельные пипетки.
- **Цветовые оси от звука:** hue-drift ← centroid (яркость спектра = теплота), lightness-пульс ← env.bassFast, color-flip (перестановка ролей внутри палитры) ← onset/bar.
- Интерполяция в OKLCH (перелив без грязи), хранение — токены палитры.
- Duotone камеры — из активной палитры (§2.2), чтобы видео и геометрия жили в одном цвете.

### 2.6 Перформанс-режим

- `ARM AUDIO` (жест обязателен для AudioContext) → пикеры mic/cam → метры.
- Клавиатура: `1–8` сцены · `T` tap ×4 · `[`/`]` nudge · `F` fullscreen · `H` скрыть весь хром · `B` blackout · `C` камера on/off · `P` следующая палитра · `Space` следующий приём (quantized).
- Сцен-банк 8 слотов, localStorage + export/import json (модель MUSICVIZ v4: input, слои, раскладка, палитра, приём, rows).
- Вывод: fullscreen на проектор/ТВ, foreground-вкладка (фоновая паузит rAF).

---

## 3. Почему не WebGL (решение по стеку)

Canvas 2D: примитивы-контуры, clip-заливки видео, blend через CSS — всё в бюджете 60fps при десятках фигур (particle-dance гоняет сотни точек в 2D без проблем; drawImage видео 1×/кадр — копейки). WebGL дал бы тысячи инстансов и шейдерные FX, но стоит недели отладки (см. Particles-3D blank-render сагу). Правило: **2D до упора, WebGL только если конкретный приём в него не влез** — тогда это отдельное решение с отдельной строкой в этом доке.

---

## 4. Приёмы (каталог вариативы, v1)

Хореография, сгруппированная по музыкальной драматургии. Каждый приём = декларация связок + кривая.

**На удар (fast):**
- **Pulse** — scale фигур ← env.bassFast (exp-кривая, разный depth по раскладке).
- **Strobe-flip** — onset: инверсия fill↔stroke или color-flip палитры, decay 120ms.
- **Kick-scatter** — onset: разлёт инстансов от центра с пружинным возвратом.

**На бит/такт (clock):**
- **Spin** — rot ← beat.phase (кольца и туннели — гипноз на ровной фазе).
- **Shuffle** — onBar: seeded-перестановка фигур по слотам раскладки (paint-order по урокам shuffler: входящая ПОД, центральная сверху).
- **Cascade** — волна scale/цвета по инстансам с лагом i·60ms от удара.

**На дыхание (slow):**
- **Breathe** — scale/интерлиньяж сетки ← env.rmsSlow.
- **Tunnel-zoom** — скорость движения по трубе ← env.bassSlow.
- **Hue-drift** — медленный увод палитры ← centroid.

**Драматургия (build-up/drop):**
- **Собраться** — flux растёт (build-up) → раскладка расшатывается в хаос; drop (onset после пика) → мгновенный снап в строгую сетку. Перенос guilloché chaos→order на геометрию — главный wow-приём.
- **Врубить толпу** — на drop камера-слой вспыхивает в полную opacity/screen на 2 такта → уходит.

Вариатива = раскладки (8) × материалы (4) × приёмы (11) — сотни состояний из малого словаря. Новые приёмы дописывать сюда датированно.

---

## 5. Фазы

| Фаза | Что | Критерий приёмки |
|---|---|---|
| **P0 Каркас** | index.html + audio-core snapshot + serve.sh · ARM AUDIO + mic-пикер + метры · 3 примитива соло · раскладки grid/ring · панель по panel-canon · Pulse + Spin | под фикстуру и живой mic фигуры дышат в бит; панель = канон (скраб, number, extendParamRange) |
| **P1 Цвет + приёмы** | палитра-банк + цветовые оси · приёмы «на удар» и «clock» полным составом · сцен-банк + клавиатура | 8 сцен переключаются по bar без рывка; color-flip живёт на onset |
| **P2 Камера** | video-core: пикер, снапшот-рельс, #cam-слой с blend/opacity, camera-fill в фигурах, duotone-стилизация | видео режется фигурами при 60fps; нет камеры → честный фолбэк |
| **P3 Перформанс** | fullscreen/H/B, quantized-переключения, «Собраться» и drop-приёмы, мод-рэк (уровень 2), export/import сцен | прогон 20 минут живого сета без деградации FPS |
| **P4 Полиш** | FX-оверлей, хвосты по глазу Роба | Роб доволен на реальной тусовке |

Исполнение — терминал по делегату (`BRYK_DELEGATE_P0_*.md` когда Роб скажет «делай»); Fable — архитектура и ревью. Этот док — план, код по нему ещё не писался.

---

## 6. Риски и гигиена

- **Permissions на площадке:** mic+cam = два prompt'а; прогнать grant заранее на боевом ноуте, https/localhost only. `serve.sh` обязателен.
- **AudioContext** стартует только после жеста — ARM AUDIO, без исключений.
- **Фоновая вкладка** Chrome паузит rAF — вывод только foreground (урок MUSICVIZ).
- **Тёмный зал = шумная камера** — duotone/threshold-стилизация это лечит и это дефолт; плюс ползунок gain/контраста камеры.
- **FPS-бюджет:** один video-decode на кадр (offscreen снапшот); много clip'ов → составной путь; счётчик FPS в панели с P0.
- **Зеркалить камеру** по флагу (селфи-режим для DJ vs прямой для зала).
- **BlackHole** как аудио-вход работает из коробки (просто девайс в пикере) — системный звук без микрофона, приём из MUSICVIZ §3.1.

---

*Датированные правки — секциями ниже.*

---

## 7. 2026-07-18 · Объёмная подача (2.5D) — карта переиспользования по коду

**Решение Роба:** честное 3D не нужно; нужна объёмная подача как в image-shuffler — присвоение PNG/SVG к осям, полёт с вариацией лицов (лицом на нас, лицом вперёд, ребром). Проведён скан кода наработок и скиллов. Итог: **рендер-ядро BRYK = движок image-shuffler**, взятый пофункционально. §2.3 и §3 читаются с поправками ниже.

### 7.1 Ядро из `image-shuffler/index.html` (894 строки, номера проверены 2026-07-18)

| Модуль | Строки | Что делает | В BRYK |
|---|---|---|---|
| `rot3Y/rot3X` + `project(p,cam,W,H)` | 168–177 | world→screen; `cam={yaw,tilt,persp,zoom,autoYaw}`; `persp=0` орто → `>0` перспектива (плавная ручка «плоско⟷объёмно») | как есть |
| **Item-контракт** layout-функции | 193–326 | `(i,N,t,C) → {x,y,z, scale, alpha, skip, order, snap, faceCam, rot2d \| right/up \| oy/op}` | центральный контракт BRYK — всё живёт через него |
| **Вариация лицов** — 3 режима | 221–232, 577–603 | ① `faceCam` — билборд «лицом на нас» (fast path `drawBillboard` 539); ② явные `oy/op` — поворот вокруг Y/X: ребро, флип, разворот; ③ `faceHeading` 0..0.8 — нормаль заваливается от камеры к вектору движения («лицом вперёд», banks into motion) | как есть; `faceHeading` и `oy` — audio-мишени |
| `drawQuadWorld` + `texTriInto` | 488–537 | текстурный квад: сетка 5×5 → аффинные треугольники в scratch-canvas → один composite (нет двойного блендинга на fade, инфляция клипа против щелей) | как есть — это «3D-ощущение» без WebGL |
| **Tent paint-order** | 587–590 | `order=-\|dist от центра\|`, sort ascending: входящий элемент ПОД всеми, центральный сверху (уроки v5–v8) | как есть |
| `flowItem` ось-движок | 193–234 | оси-lanes под любым углом: `axes/angle/bend/bendFreq/gap/scaleGrad/tilt3d/faceHeading/photoAngle` + бесшовный off-screen recycle («присвоение к осям» = буквально это) | как есть |
| **Банк 14 раскладок** `LAYOUTS` | 253–327 | flow · waterfall · sine-wave · zigzag · pulsing-circle · radial (кольцо в глубину) · vortex · helix · spring · grid · float · carousel (true cover-flow) · card-deck · flip-book | как есть = готовые раскладки BRYK; §2.3-список (solo/tunnel/mirror) — досборка поверх того же контракта |
| Motion Pad `motionResolve` | 359–375 | X=speed × Y=ease, билинейный бленд 4 архетипов | как есть; speed — audio-мишень |
| `buildTexture` cover-fit | 426–447 | картинка → texture-canvas произвольной пропорции | база текстур-пайплайна (7.2) |
| `buildSliders`/`setPath`/`deepMerge` | 420–423, 612 | панель по декларации, конфиг-мёрж | по канону panel-canon |
| `captureStream` экспорт | 731–768 | видеозапись cover-crop | P4, бесплатно |

Скелет и харнесс — по скиллу **builder-engine** (unified API, panel grammar, jsdom verify), unpacked-копия шаффлер-скилла лежит `Projects/image-motion-builder/`.

### 7.2 Ключевой ход: ВСЁ — текстура (один пайплайн на три источника)

Движок рисует текстурные квады — значит, geometry/PNG/SVG/камера унифицируются на входе:

1. **PNG** — drop, как в shuffler (`addFiles` 679).
2. **SVG** — drop → `Image` → растр в texture-canvas (тот же `buildTexture`); перерастеризация при смене размера — дёшево.
3. **Примитивы rect/circle/triangle** — *процедурные текстуры*: фигура рисуется в offscreen с цветами активной палитры (fill/stroke/градиент). Смена палитры = перерисовка текстур, не сцены.
4. **Камера** — video-снапшот (§2.2) → texture-canvas, обновляется каждый кадр. **Упрощение против §2.2:** маска формы (круг/треугольник режет видео) делается clip'ом ОДИН раз внутри texture-canvas, а не на каждый инстанс сцены — сколько угодно летающих «видео-кругов» за один clip. Duotone-стилизация — там же, на снапшоте.

Итого фигура BRYK = текстура × item-контракт. «Летающий треугольник с толпой внутри, поворачивающийся ребром» — это texture(камера+маска) + `oy` анимация. Ничего нового в рендере.

### 7.3 Добавка из particle-dance (`particle-dance-builder/index.html`)

`patternPoint(p, pattern, t, opts)` — 15 seeded-паттернов (cloud · lattice · vortex · ribbon · burst · wave · spring · zigzag · spiral/DNA · axes · orbitals · fan · steps · glyphs · form) с `spread/dance/amplitude/frequency/twist/lanes`, orientMode и depthFade. Модель совместимая: чистая функция → позиции. **Адаптер в item-контракт (билборды малого размера) = второй банк раскладок** — рой мелких фигур против крупных квадов shuffler'а. Это и есть два регистра подачи: «карточки летят» (shuffler) и «масса танцует» (particle-dance) в одном движке.

### 7.4 Audio → мишени движка (конкретизация §4 под это ядро)

| Фича | Мишень | Эффект |
|---|---|---|
| `env.bassFast` | `scale` инстансов / `scene.size` | Pulse |
| `beat.phase` | `t` раскладки (radial/carousel/vortex) | Spin — вращение защёлкнуто на бит |
| `onset` | `photoAngle` kick · color-flip текстур | Strobe-flip |
| `env.rmsSlow` | `cam.persp` 0→0.85 · `cam.tilt` | сцена «раскрывается в объём» на дыхании — главный трюк 2.5D |
| build-up (`flux` рост) | `faceHeading` 0→0.8 · `flow.bend` | фигуры разворачиваются рёбрами вперёд, хаос |
| drop | снап в `grid`/`carousel`, `persp` в пик | «Собраться» |
| `onBar` | смена раскладки (quantized swap) | Shuffle |
| `env.midSlow` | `flow.gap` / `scaleGrad` | плотность дышит |
| `centroid` | hue-drift палитры → перерисовка процедурных текстур | цвет от тембра |

### 7.5 Поправки к ранним секциям

- **§2.1:** #geo-canvas = shuffler-ядро (item-контракт + project). Стопка #bg/#cam/#geo/#fx остаётся; #cam-слой («слой в миксе») — по-прежнему отдельный canvas, а camera-fill фигур теперь через текстуры (7.2.4), не через clip сцены.
- **§2.3:** «раскладка × материал × хореография» остаётся, но раскладки = LAYOUTS-банк + particle-адаптер; материал = источник текстуры; хореография = приёмы §4 через мишени 7.4.
- **§3:** формулировка «плоский 2D» снята — рендер 2.5D (проекция + квады), по-прежнему Canvas 2D, WebGL по-прежнему нет.
- **§5 P0:** скелет строится сразу на shuffler-ядре (не «примитивы соло на плоскости»): P0 = ядро + процедурные текстуры примитивов + flow/grid/radial + Pulse/Spin от audio-core.

---

## 8. 2026-07-18 · Физический слой (Motion Primer) + план прокачки

**Задание Роба:** просканить Motion Primer и particle-dance-варианты с гравитациями/вихрями — и прокачать. Просканировано по коду. Итог: **Motion Primer — готовая 2D-физическая песочница**, всё названное там уже есть; берём её силы третьим регистром хореографии.

### 8.1 Скан `Motion Primer — Skeleton.html` (464 строки, номера проверены 2026-07-18)

| Модуль | Строки | Что делает | В BRYK |
|---|---|---|---|
| `springAccel` | 116 | пружина к цели (stiffness/damping) | транспорт-к-цели вместо lerp — «прилетело и отпружинило» |
| `attract` | 118 | магнит: тяга усиливается у центра (`boost/(dist+soft)`) — всасывание | приём «Магнит» |
| `flowPotential` + `curlAt` | 119–120 | curl-noise векторное поле (вихревой дрейф без дивергенции) | приём «Вихрь», дрейф сцены |
| `boidForce` | 121–128 | стая: separation / alignment / cohesion в радиусе | приём «Стая» |
| `resolveBodies` | 129–142 | пары: collide-push + импульс · avoid-обтекание · spin-подкрут · crowd-глушение в тесноте | живые столкновения, kick-цепочки |
| `BEHAVIORS` | 146–153 | Swarm · Pack (укладка) · Magnet · Orbit · **Fall (гравитация, пилятся у пола)** · Scatter (разлёт) | режимы = заготовки приёмов |
| `MANNER_PRESET` | 155–158 | Free / Cluster / Disperse / Flock / Flow / **Vortex** (cohesion×separation×flow) | манера роя = audio-мишень |
| `state.adv` | 175 | `gravity / swirl / reach` — уже параметры | прямые audio-мишени |
| **Formation** `buildFormation`/`insideMask` | 195–228 | текст или картинка → offscreen-маска → точки: Fill (внутри букв) / Frame (рамка вокруг, буквы = дырки); адаптивная плотность ≤500 точек | приём «Слово» — главная прокачка (8.3) |
| orient-режимы | 163, 176 | velocity / lean / spin — глиф ориентируется по движению | треугольник = стрелка в полёте |

Плюс в particle-dance уже есть 3D-collide/avoid роя (`index.html:683`) — расталкивание в глубине.

### 8.2 Архитектура хореографии — три ортогональных регистра (финальная модель)

```
 цель (target-провайдер)      ×   транспорт к цели         ×   локальные взаимодействия
 ─ LAYOUTS shuffler (§7.1)        ─ lerp-follow (как есть)     ─ collide / push / avoid / spin
 ─ patternPoint рой (§7.3)        ─ springAccel (пружина)      ─ boids (стая)
 ─ Formation-маска (8.1)          ─ силы: attract/curl/gravity ─ crowd-глушение
```

Физика работает в world-space ПОВЕРХ item-контракта §7.1: силы двигают `it.x/y` (velocity-интеграция) к целям раскладки, z остаётся от раскладки, рендер не меняется. Никакой 3D-физики не строим — плоскость сил + 2.5D подача, этого достаточно и это честно по FPS.

### 8.3 Прокачка: новые приёмы на физике (дополнение каталога §4)

- **«Магнит-дроп»** — build-up: `attract` всасывает всё к центру, urge ← flux; на drop — `Scatter`-импульс наружу + `persp` в пик. Драматургия сжатие→взрыв.
- **«Вихрь»** — manner Vortex / curl-поле: swirl ← env.midSlow, фигуры банкуются в поток через `faceHeading`. Гипноз на ровных участках.
- **«Гравитация»** — брейкдаун: Fall — всё падает и пилится у пола (resolveBodies); возврат бита — гравитация инвертируется/выключается, всплытие к раскладке.
- **«Слово»** ★ — Formation-маска: фигуры собираются в текст (BRYK · имя трека · DROP) или в SVG-лого (маска из ассета — тот же drop-пайплайн §7.2) на onPhrase; Fill/Frame режимы. Текст задаётся с клавиатуры прямо в сете.
- **«Стая»** — боиды треугольников (orient=velocity: треугольник = стрелка), manner-свитч по фазам трека: build-up=Disperse, drop=Cluster.
- **«Расталкивание»** — плотный grid + collide: onset → импульс случайному инстансу → цепная реакция по соседям.
- **Пружинные переходы** — смена раскладок через springAccel вместо снапа: стиль прилёта задаёт Motion Pad (Y-ось = характер пружины).

### 8.4 Audio → мишени физики

| Фича | Мишень | Эффект |
|---|---|---|
| `flux` (build-up) | `attract.urge` · manner→Disperse | сжатие/рассып перед дропом |
| drop | Scatter-импульс · gravity flip | взрыв |
| `env.bassFast` | `collide.push` · kick-импульс | удар телом |
| `env.midSlow` | `swirl` / curl scale | вихрь дышит |
| `beat.phase` | `orbit.speed` (защёлка на бит) | орбиты в темпе |
| `onPhrase` | Formation on/off (слово собирается/рассыпается) | структурные точки трека |
| `rmsSlow` | `boid.cohesion` | стая плотнеет на громкости |

### 8.5 Поправки к фазам (§5)

- **P1** += регистр «транспорт к цели» (lerp | spring) + collide/avoid из resolveBodies.
- **P3** += приёмы 8.3 полным составом; «Слово» с вводом текста с клавиатуры (перформанс-фича).
- Порог сложности: физика O(N²) в resolveBodies — бюджет ~120–150 тел при 60fps (проверено масштабом Motion Primer count=90 и particle-dance count=340 без пар); плотные рои — без collide, collide — для крупных составов ≤150.

---

## 9. 2026-07-18 · Единая сцена: сведение режимов, мастеринг, типографика

**Задание Роба:** просканить всю историю (EQJ-визуализатор → particle-dance → Motion Primer → image-shuffler), понять как уместить в ОДИН отображаемый скрин; учесть опыт наслоения режимов EQJ (там был перегруз); переходы между режимами как микшер A→B или общий мастеринг; режимы должны взаимодействовать («замечать друг друга»); Formation — с выбором установленных шрифтов, предпроверкой и переводом в кривые.

### 9.0 Скан истории — уроки EQJ

Стек EQJ/MUSICVIZ — это уже была попытка совмещения именно этих четырёх движков: `mvj-inject.html:322` — **LINES** (Synthex) · **BODIES** (verlet) · **DOTS** (particle-dance) · **FORMS** (Motion Primer), слои-iframe + нативный, комб-микшер v3 (строка: n·имя·opacity·M·S·blend), карточки маппинга. Уроки:

1. **Перегруз:** режимов и параметров слишком много — вживую этим рулить нельзя. → BRYK: лимит 4 одновременных режима, один главный регулятор на карточку.
2. **Слои не видят друг друга:** композит только blend/opacity картинок — режимы не взаимодействуют. → BRYK: единый пул (9.1).
3. **Противоречие фактур** (2D-линии Synthex против 2.5D-билдеров) в BRYK отсутствует: все четыре источника — частицы/тела/квады в одном пространстве. Это и позволяет пул.
4. **Жемчужина из BODIES** (`mvj-inject:363`): *«Sound hits FORCES, not positions»* + fixed-step verlet (музыка не ломает физику на лагах). Берём как закон BRYK.

### 9.1 Один скрин = один пул (главное решение)

НЕ стопка независимых слоёв-картинок. Один canvas #geo, **один пул тел** `{x,y,z,vx,vy,texture,face}`, а режимы — «программы» над пулом:

```
режим (mode) = target-провайдер (§8.2) + набор сил + материал(текстуры)
         каждый активный режим имеет ВЕС ПРИСУТСТВИЯ w ∈ 0..1
```

- **Сведение = веса, не прозрачность.** Цель тела = взвешенный бленд целей активных режимов (веса нормируются); силы аддитивны ×w; материал кроссфейдится. `w→0` — режим *тает из процесса* (перестаёт влиять на тела), а не «выцветает картинкой». Это формализация Робова «убирали присутствие значения эффекта — и легче сводили».
- **Морф A→B = частный случай:** два режима, один фейдер (`w_B = x, w_A = 1−x`). Переход квантуется onBar/onPhrase, кривая шага — Motion Pad. Степ-переходы между режимами — это он.
- **Взаимодействие режимов:** collide/boids/crowd (§8.1) работают на ВСЁМ пуле независимо от того, чья цель у тела — тела разных режимов физически замечают друг друга. Два способа делить пул (оба нужны, по приёму): **blend** — все тела слушают все режимы через веса; **split** — доли пула закреплены за режимами (рой DOTS расталкивается с квадами карусели).

### 9.2 Мастеринг (вместо покруток каждого слоя)

- **Карточка режима:** имя · большой фейдер **PRESENCE** (w) · M/S · disclosure с параметрами внутри. Снаружи — один регулятор.
- **Мастер-шина: 3 макро** — `ENERGY` · `DENSITY` · `CHAOS`. Каждый макрос разложен на параметры активных режимов декларацией в пресете (макро-маппинг). DJ вживую рулит: presence-фейдеры + 3 макро + сцены. Всё остальное — подготовка до сета.
- Банк сцен сохраняет: состав режимов + веса + макро-раскладку + палитру (эволюция сцен v4 MUSICVIZ).

### 9.3 UI одного скрина

Левая **нав-панель** (разделы: MIX · MODES · AUDIO · LOOK · SCENES) + колонка **карточек-слоёв** (стек карточек активных режимов, грамматика строки из комб-микшера v3: n·имя·PRESENCE·M/S) — сцена занимает остальной экран. Скрабы по panel-canon. `H` прячет весь хром. Один экран, ноль всплывающих окон.

### 9.4 Формация + типографика (уточнение к 8.3 «Слово»)

- **Пикер установленных шрифтов:** Local Font Access API — `queryLocalFonts()` (Chrome 103+, permission + user gesture, secure context — наш localhost ок; проверить при билде). Фолбэк: курируемый список + `document.fonts.check`.
- **Предпроверка заранее** (при выборе шрифта, не в сете): рендер тест-строки кириллица+латиница+цифры в offscreen → детект фолбэка сравнением пиксельной маски с системным дефолтом → бейдж ✓/✗ и превью алфавита прямо в пикере.
- **«Перевод в кривые» — zero-dep:** растр текста → alpha-сетка → **marchingSquares** (готовый общий модуль: `Contours:179`, `Metaballs:227`) → цепочки сегментов → полилинии контуров букв → **arc-length ресемпл** → равномерные упорядоченные точки вдоль кривой каждой буквы. Частицам «легче строиться»: сборка идёт по порядку вдоль контура (буква за буквой, каскад), а не хаотичным заполнением. Fill-решётка Motion Primer остаётся вторым режимом формации.
- Кэш кривых на (шрифт × текст × размер). Тот же пайплайн бесплатно векторизует SVG/PNG-лого (растеризация → маска → кривые).

### 9.5 «Лицом вперёд по вектору» — унификация на все регистры

`faceHeading` из shuffler (§7.1, строки 221–232) становится общим свойством тела: heading = нормированная скорость (физика) · производная пути (раскладки — уже есть в `flowItem` p1/p2) · касательная контура буквы (формация). Один параметр `face` 0..1 (билборд ⟷ по движению) на карточке режима, audio-мишень (build-up разворачивает).

### 9.6 Поправки к ранним секциям

- **§2.1:** «слои геометрии» отменены в пользу режимов с весами над единым пулом; стопка канвасов сокращается до #bg · #cam (видео-подложка/слой — остаётся отдельным canvas с blend) · #geo (пул) · #fx.
- **§5:** P1 = пул + веса + 2 режима + морф-фейдер A/B; P2 без изменений; P3 += мастеринг-макро (3 шт) + формация со шрифтами (9.4) + face-унификация.
- Карта карточки-режима и грамматика панели — по panel-canon, финальный вид согласуется по глазу Роба на P1.

---

## R10. 2026-07-18 · Ресёрч стилей/эффектов: RD · Fluid · Space Type Generator

**Задание Роба:** ресёрч по гитхабам и готовым решениям — стили и эффекты уровня Karl Sims RD-tool, WebGL Fluid Simulation, Space Type Generator (изучить все режимы селекта). Проверено по живым страницам и репам 2026-07-18.

### 10.1 Три референса — статус и что берём

| Референс | Техника | Код | Что берём |
|---|---|---|---|
| **Karl Sims — RD Tool** (karlsims.com/rdtool.html) | Gray-Scott reaction-diffusion на WebGL; фиче-сет снят со страницы: pattern-space (k/f), color map + emboss/invert, **Flow-варианты** (Radial/Rotate/Swirl/Bubble/Ring/Vortex/Vertical), **Orientation-поля** (Radial/Circles/Swirl/Bubble/Linear), Scale Radial/Random/Ring, URL-шаринг пресета `?s=` | закрытый (личный тул) | фиче-сет как спека нашего RD-режима; сам Gray-Scott шейдер ~30 строк GLSL — пишем свой или берём открытый (10.2) |
| **PavelDoGreat/WebGL-Fluid-Simulation** | GPU Navier-Stokes (GPU Gems 38): advection, pressure, vorticity, dye, bloom, sunrays | **MIT, 16.3k★**, весь движок = один `script.js` (+dat.gui, выкидываем) | берём движок целиком; сплэты программно: `onset → splat(x,y,dx,dy,color)` в позициях фигур, цвет dye из палитры, сила ← bass |
| **Space Type Generator** (Kiel Mutschelknaus) | кинетическая типографика, Processing; 23 режима сняты с живого селекта: CYLINDER · FIELD · STRIPES · COIL · FLAG · MORISAWA · CASCADE · RIBBON · LAYERS · DANGER · STRING · BADGE · CLUTTER · CONSTRUCT · SNAP · FLASH · POW · CRASH · CRASH CLOCK · VESSEL · SHINE · BOOST · BoxSquad | ⚠️ был open source (статья It's Nice That 2019, орг github.com/spacetypegenerator), **сейчас публичных реп НОЛЬ** — код спрятан | код не копируем; берём таксономию механик (10.3) — почти всё ложится на наши регистры |

### 10.2 Открытые реализации RD (кандидаты, лицензию проверить при билде)

`amandaghassaei/ReactionDiffusionShader` (Gray-Scott + **векторное поле ориентации диффузии** — ровно фишка Sims'а) · `piellardj/reaction-diffusion-webgl` · `pmneila/jsexp` (grayscott классика) · `PedroRavaglia/Reaction-Diffusion` (WebGL2) · `lejeunerenard/reaction-diffusion` (ping-pong буферы). Шейдер тривиален — дефолт: пишем свой по спеке 10.1, открытые репы = шпаргалки.

### 10.3 Разбор 23 режимов STG → наши механики

- **Поверхности вращения/ленты** — CYLINDER, COIL, VESSEL, RIBBON, STRIPES: текст-текстуры на ring/tunnel/helix раскладках (§7.1 LAYOUTS) — есть.
- **Ткань/волна** — FLAG, MORISAWA (сетка-волна): ★ единственный недостающий механизм — **деформация сетки квада**: `drawQuadWorld` уже растягивает текстуру по сетке 5×5 (§7.1) — добавляем опциональный displacement-колбэк на узлы сетки → волна/флаг/морисава-грид на ЛЮБОЙ текстуре (буквы, PNG, камера!). Малая правка, огромный выхлоп.
- **Каскады/стеки** — CASCADE, LAYERS, STRING: наш Cascade-приём + stack-раскладка — есть.
- **Импакт** — SNAP, FLASH, POW, CRASH, CRASH CLOCK, BOOST: Scatter/kick-импульсы + strobe (§4, §8.3) — есть, буквы как тела.
- **Структуры** — BADGE, CONSTRUCT, CLUTTER, DANGER, SHINE, BoxSquad: формация-маска + grid/scatter + рамки-примитивы — есть.

Вывод: STG-вариатива ≈ наши три регистра × текст-как-текстура × один новый механизм (деформация сетки). Шрифт Lofi Mono их — не трогаем, у нас системные (§9.4).

### 10.4 Интеграция в архитектуру — два канала

1. **Источники текстур №5 и №6** (расширение «всё — текстура» §7.2): fluid и RD рендерятся в свои offscreen/WebGL канвасы → их выход = текстуры → могут заливать фигуры (треугольник с жидкостью внутри, круг с RD-орнаментом) и лететь по любой раскладке.
2. **Фоновые слои стопки**: #fluid и #rd как отдельные канвасы под/над #geo со своим PRESENCE-весом (карточка режима, §9.2) — фон дышит жидкостью, геометрия поверх.

**Поправка к §3 (WebGL-политика):** пул тел и рендер квадов остаются Canvas 2D; fluid/RD — изолированные WebGL-канвасы в стопке, каждый = самостоятельный модуль со своей карточкой. Это и есть предусмотренное §3 «отдельное решение отдельной строкой»: WebGL допущен ТОЛЬКО для этих двух эффект-модулей.

### 10.5 Audio-мишени эффектов

| Фича | Fluid | RD |
|---|---|---|
| `onset` | splat в позициях фигур/случайно | инъекция seed-пятна |
| `env.bassFast` | сила/радиус сплэта | speed симуляции |
| `env.midSlow` | vorticity (curl) | k/f дрейф по pattern-space (морф орнамента!) |
| `centroid` | цвет dye из палитры | color map сдвиг |
| build-up / drop | глушим ↔ заливаем весь экран | flow-режим Swirl→Radial |

### 10.6 Фазы

Fluid — в **P4** (готовый MIT-движок, интеграция дешёвая). RD — в **P4+** (после прогона fluid; свой шейдер по спеке 10.1). Деформация сетки квада (10.3) — раньше, в **P1** (это правка нашего ядра, не WebGL).

---

## R11. 2026-07-18 · Ресёрч VJ-инструментов по отзывам → приёмы

**Задание Роба:** ресёрч хороших по отзывам визуализаторов для вечеринок, снять приёмы. Проверено по обзорам и репам 2026-07-18.

### 11.1 Что любят в топовых инструментах (и что это значит для BRYK)

| Инструмент | За что любят по отзывам | Для BRYK |
|---|---|---|
| **NestDrop** (MilkDrop-движок, стандарт psy-вечеринок) | мгновенное переключение пресетов, квантованное к биту; deck-микс пресетов; бесплатный | ✅ подтверждает наши: сцен-банк + onBar-квантование + морф A/B (§9.1) |
| **Resolume** (индустриальный стандарт) | слои и ГРУППЫ: стек визуалов управляется как один юнит; библиотека blend/FX | ✅ подтверждает мастеринг §9.2 (группа = наш пул с весами); FX-набор = чек-лист для #fx |
| **Synesthesia** | шейдер-сцены мгновенно реагируют на звук; глобальные Meta-контролы поверх любой сцены | ✅ подтверждает 3 макро ENERGY/DENSITY/CHAOS §9.2 — ровно их Meta-модель |
| **VDMX** | «connect anything to anything» — глубокий audio-анализ роутится на любой параметр | ✅ подтверждает мод-рэк уровня 2 (§2.4) |
| **MilkDrop / butterchurn** | психоделическая глубина из простых средств; тысячи пресетов сообщества | ⭐ главная добыча — 11.2 и 11.3 |

Вывод: архитектура §9 (пул+веса+макро+квантование) совпадает с тем, за что хвалят лидеров. Ничего не перестраиваем — добираем приёмы.

### 11.2 ⭐ butterchurn — готовый MilkDrop для web

`jberg/butterchurn` — **MIT**, WebGL-реализация MilkDrop; + пакет `butterchurn-presets` (100+ кураторских, поддержка тысяч community `.milk`). Подключается к Web Audio **AnalyserNode — у нас он уже есть в audio-core** (§3.2 мастер-плана MUSICVIZ), т.е. интеграция = отдать наш анализатор. Встраиваем как третий эффект-модуль по правилу §10.4: карточка режима «MILK» (фоновый WebGL-слой со своим PRESENCE + источник текстур). Мгновенная психоделическая глубина вариатива почти бесплатно. Лицензии community-пресетов пёстрые — в поставку берём только пакет butterchurn-presets.

### 11.3 Новые приёмы из VJ-мира (дополнение каталога §4/§8.3)

- **«Эхо-туннель» (warp-feedback)** ★ — сердце MilkDrop: предыдущий кадр #geo рисуется под текущим со сдвигом zoom/rotate/warp и затуханием. Canvas 2D: `drawImage(#geo→сам в себя, scale·rotate·alpha)` — копейки по цене, огромный эффект. Параметры: zoom ± (туннель внутрь/наружу), rotate, decay; мишени: zoom ← bassSlow, rotate ← beat.phase. **P1.**
- **«Калейдоскоп»** — N зеркальных секторов сцены (clip-сектор + повороты-отражения копий). N ∈ {2,4,6,8}, переключение onBar. P3.
- **«Волна»** — осциллограф как режим: time-domain буфер (в AnalyserNode уже есть) рисуется кольцом/лентой/контуром фигуры — классика MilkDrop, у нас бесплатна. Волна может быть и контуром примитива (круг из волны). P1–P2.
- **FX-чеклист** для #fx по Resolume-стандарту: strobe · mirror · chromatic aberration · glitch/VHS · bloom-имитация — grain/scanlines/vignette уже были в EQJ FX MVP (Наряд 6), переносим список целиком.

### 11.4 Поправки фаз

**P1** += Эхо-туннель + Волна (canvas-механики, дёшево). **P3** += Калейдоскоп + FX-чеклист. **P4:** порядок эффект-модулей — fluid → butterchurn → RD (по соотношению выхлоп/усилия).

---

## 10. 2026-07-18 · Полный код-аудит источников + решения Роба по цвету, тексту, панелям, пост-эффектам

**Что произошло:** четыре параллельных скана по коду (image-shuffler · панели Synthex · модуль маппинга EQJ · шейдеры). Все номера строк ниже **проверены по файлам 2026-07-18**, не по памяти. Плюс решения Роба, снятые в этой же сессии. Секции 2, 3, 5, 7, 8, 9 читаются с поправками §10.7.

### 10.0 Решения Роба (зафиксировано)

1. **Тёмная база.** Мероприятие, зал, проектор — белый фон светит и неуместен. `fleet-finalize` с его канон-фоном `#f6f5f2` на BRYK **не распространяется**; из него берём только hotkey-грамматику и идею scene-only invert.
2. **Палитра присваивается диапазоном**, не пипетками по одному. Доминанты — **жёлтый, красный, синий**, теплее олимпийских. Частиц много, палитра держит **8 цветов и больше**.
3. **Правка цветов вживую** — полноценный пикер, не три ручки. Берём готовый механизм с добавлением цветов в набор.
4. **Формация** — поле ввода, дефолт `BRYK`, вписать можно что угодно. Выбор шрифта рядом, сразу. Не «ROB».
5. **Панели не переделываем** — переносим грамматику Synthex (левая + правая, скраб-инпуты) как есть.
6. **Пост-эффекты** (аберрация, преломление, стекло) — последним проходом, отдельным слотом. **Сфера/шар не нужны.** Не применять к экспериментам на ходу.
7. **Аудио-панели EQJ** (вход звука, распределение частот) — пересобрать по канону панелей Synthex, ревью по скиллу `apple-design`.

### 10.1 Цветовая система BRYK (черновик, правится одним набором)

```
--bg      oklch(0.13 0.012 265)   почти чёрный с синим подтоном (не #000: бандинг на LED)
--yellow  oklch(0.84 0.165 88)    золотистый, не лимон
--red     oklch(0.60 0.205 32)    вермильон, теплее олимпийского #EE334E
--blue    oklch(0.55 0.170 250)   нэви, светимость поднята под тёмный фон
```

Почему не буквально олимпийские: их синий `#0081C8` уходит в циан и холодит сцену на тёмном, красный `#EE334E` розовит. Сдвиг hue жёлтого к 88 и красного к 32 даёт тёплый сплав.

Источник математики — **`Projects/color-tuner.html`** (116 строк, 14.07), готовое и чистое: `hexToOklch` (:57–72) полная конверсия sRGB→OKLab→OKLCH · `inG(L,C,H)` проверка гамута + `fitC()` бинарный поиск максимальной хромы, 26 итераций (:51–52) · `hexOf(L,C,H)` обратно в hex с гаммой (:54) · шкала светлот + `ramp(H,C)` (:55–56). Оси UI там же: **H 0–360 шаг 1, C 0–0.37 шаг 0.005**, L фиксированной шестиступенчатой шкалой.

### 10.2 Шейдеры / пост-проход — `synthex-engine/engine/Synthex Engine — v1.html` (правлен 18.07 01:09, волны W-ROB-9/10/11)

**Обвязка переносится целиком, это ровно нужный паттерн:**
- `#glcv` отдельным `position:fixed` оверлеем над 2D-канвасом, `pointer-events:none` (CSS :81, DOM :398)
- ленивый init WebGL1 `{premultipliedAlpha:false,antialias:false}` (:2988–2989)
- **`gl.texImage2D(…, canvas)`** — 2D-канвас напрямую в текстуру, `UNPACK_FLIP_Y_WEBGL=true` (:3106)
- один фуллскрин-quad, `TRIANGLE_STRIP` на 4 вершины, **без FBO** — весь стек за один проход
- вызов в конце `draw()`: `if(shaderAnyOn()){ if(runShader())glcv.style.display='block'; }` (:3189)
- `glFail` — тихая деградация, если GL не поднялся (:2990, :3086)
- гизмо вынесен на третий канвас `#gizcv` НАД `#glcv`, чтобы аберрация не красила оси (:3181)
- экспорт: `expComposite()` бейкает `#cv + #glcv` в offscreen, чтобы `captureStream` нёс шейдер (:2984–2987, :5727, :5775)

| Эффект | Строки | Состояние для BRYK |
|---|---|---|
| **Chromatic aberration** | :3043–3056 | **Готов как есть.** 16-tap, смещение ∝ `distance(uv,.5)*2` — радиальная оптика, полностью плоскостная, сферы нет. Hue фринджей вращением Родригеса ТОЛЬКО добавки (база не перекрашивается) |
| **God rays** | :3075–3083 | Готов. 32-tap march, band-pass по яркости |
| **Fbm domain-warp** | :3010–3017 | Правка: снять радиальное затухание `mDist` → фуллскрин |
| **Liquify** | :3018–3019 | Правка: снять `max(0,1-distance*4)` → фуллскрин |
| **Lens / glass** | :3020–3022 (`sphereTx`), :3033–3042, :3060–3074 | **Привязан к шару.** Маска `gIn` круглая, нормаль `gN` из круга. Требует переделки нормали |

Юниформы: дефолты :1346–1352, UI-декларация `SHADER_STACK` :4520–4534. `uChrAmt` 0–1 (деф 0.55) · `uChrAng` 0–360° · `uChrHue` 0–360° · `uLenAmt/Bul/Ch/Light/Shiny` 0–1 · `uFbmAmt/Sc/Spd` · `uLiqAmt/Fq` · `uRayAmt/Dec/Th` · общие `uTex,uRes,uTime,uPaper`.

**Донор плоскостного стекла** — `Projects/orbox/assets/sphere/unicorn-scene.json` (9.07, 55КБ): скомпилированный GLSL ES 3.0 Unicorn Studio с настоящей дисперсией на `refract()`. `sampleDispersionTap(rd, normal, iorBase, dispCoefficients, step, blurOffset)` — три IOR на R/G/B, `frostOrDispersion` — 4 тапа с блюр-осями. **Функция универсальна: работает от ЛЮБОЙ нормали.** Подставить нормаль плоского высотного поля (fbm / SDF скруглённого прямоугольника) вместо raymarch по SDF-сфере — и это плоскостной liquid-glass пост-пасс. Это прямой путь к «преломлению без шара».

**Фолбэк без WebGL:** аберрация на чистом Canvas-2D через CMY-плашки с `globalCompositeOperation='multiply'` и квантованным сдвигом по X — `Synthex Engine — v1.html:3156–3165`, параметр `post.chroma` 0–3 шаг 0.01 (UI :4287). Плюс SVG-вариант (:3460).

**Грабли:** в headless-скриншотах видна полоса сверху — артефакт `glReadPixels`-флипа композита; в 2D-`drawImage`-пути буфер правильный (:3106). `hexToRgb` возвращает **массив**, не `{r,g,b}` (:3120).

### 10.3 Рендер-ядро — `image-shuffler/index.html` (894 строки, 05.07, git нет, версий нет)

Номера строк §7.1 **проверены и подтверждены**. Переиспользуемая зона помечена в файле: `@core-begin` (:162) — `@core-end` (:396), 234 строки чистой математики без DOM.

| Модуль | Строки | Статус |
|---|---|---|
| `rot3Y/rot3X` | 168–169 | как есть |
| `project(p,cam,W,H)` | 172–177 | как есть. **Параметры `W,H` не используются**, функция читает глобал `FR`; все 3 вызова передают их зря. Порядок: yaw (Y) → tilt (X), камера на `d=3.2`, `persp=0` → чистая ортография |
| `flowItem` (осевой движок) | 193–234 | как есть |
| **Item-контракт** | де-факто в `frame` 567–588 | `{x,y,z, scale, alpha, skip, order, snap, faceCam, rot2d, right/up, oy/op}`. `faceCam` по умолчанию **true** (`!==false`). Два взаимоисключающих способа ориентации: явный `right/up` ИЛИ `oy/op` |
| `LAYOUTS` — 14 раскладок | 253–327 | как есть |
| `texTriInto` / `drawQuadWorld` / `drawBillboard` | 488–506 / 507–537 / 539–543 | как есть. Сетка 5×5 → 50 треугольников, билинейка **в world-space**, всё непрозрачно в `_scratch` и один блит с `globalAlpha` (иначе двойной бленд даёт видимую сетку на фейде). Inflate-клип `INF=0.6`px |
| `buildTexture` + `roundRectPath` | 426–453 | как есть |
| Motion Pad `motionResolve` | 359–375 | как есть, но см. баг ниже |
| `buildSliders` | 612–638 | **не берём** (см. 10.5) |
| `captureStream` запись | 730–767 | как есть, P4 |

**Скругление углов — подтверждено, механизм правильный.** `state.frame.radius` (:411, деф **8**), слайдер 0–50 шаг 1 (:790), живой пересбор `rebuildTextures()` (:630). Радиус = **процент от короткой стороны**: `clamp(radius/100*min(TW,TH), 0, min(TW,TH)/2)` (:431), потолок = полная капсула. Применяется **в texture-canvas** через `roundRectPath` + `clip()` (:432–434, :449–453; нативный `ctx.roundRect` не используется). Следствия: (а) радиус искажается вместе с текстурой в перспективе — физически корректно; (б) смена радиуса пересобирает ВЕСЬ банк текстур; (в) сглаживание углов зависит от `TH=360` (:428) — на крупных кадрах углы мягко-ступенчатые.

**Шаг распределения по оси** (:201): `step = max(0.09, gap*0.5*(size/100))` — **не зависит от N**. Количество кадров наоборот выводится из шага: `flowFillCount` (:470–476) `clamp(ceil((2*reach+3*step)/step)*axes + axes, 8, 160)`. Recycle (:202–204): длина петли ≥ `2*reach+2*step`, точка склейки всегда за кадром; плюс `edge`-гашение альфы (:214) и `skip` (:215); плюс снап позиции при уходе дальше 0.6 (:571). Двойная страховка, телепорт не виден.

**Расхождения с планом — важное:**

1. 🔴 **SVG не поддержан.** Ноль вхождений строки «svg» во всём файле. Формально `image/svg+xml` проходит MIME-фильтр (:680) и растеризуется общим путём, но: SVG без `width`/`height` → `naturalWidth=0` → `aspect=NaN` → `TW=NaN` → **битая текстура**; растеризация в фиксированные 360px, вектор теряется; `accept="image/*"` (:93) в Safari может не показывать SVG. **§7.2.2 плана — это работа, а не перенос.** Нужен парс `viewBox` + растеризация в целевой размер.
2. 🔴 **Привязки ассета к оси нет.** Единственное правило — `textures[i % textures.length]` (:583). Так как `lane = i % axes` (:196), при кратности (6 картинок / 3 полосы) **каждая полоса замыкается на фиксированное подмножество из 2 картинок** — перемешивания не будет. Неучтённый алиасинг. Лечится модулем маппинга (10.4).
3. **`faceHeading` зажат на 0.8** — и слайдером (:799), и кодом (:216). «Полностью ребром» через осевой движок **недостижимо**. Ребро сейчас только через `carousel`/`card-deck` (`oy` до ±1.0 рад ≈ 57°, :308) и `flip-book` (`op`, :295). Для разворота рёбрами на build-up (§7.4) потолок надо снимать.
4. **Нет переходов между раскладками** — смена стиля (:786) это hard cut: сброс `t=0`, сброс всех `p=0`, `reinit`. Морфинга нет. Пружинные переходы (§8.3) строятся с нуля.
5. **Нет stagger в reveal** (:575): `it.p += dt*1.8`, все элементы стартуют синхронно за ~0.56с. Каскад (§4 «Cascade») строится с нуля.
6. `STYLE_PRESETS` (:329–334) **не содержит `speed`**, хотя `STYLE_AXIS` его показывает (:337–340) — при смене стиля `flow.speed` унесёт значение из предыдущего.
7. `assertCore` (:376–395) итерирует по `LAYOUT_LIST`, не по `LAYOUTS` (:382) — раскладка, добавленная только в `LAYOUTS`, молча останется непротестированной. Порядок в двух списках разный, править надо оба.

**Мёртвое — не тащить:** слайдер Tempo (`motion.speed`, :811) **нерабочий** — :550 перезаписывает `state.motion.speed` результатом `motionResolve` каждый кадр, а `dt` считается из `mr.speed`; слайдер только индикатор · кнопка Loop (:159, :853) ни на что не влияет, `state.anim.loop` в рендере не читается · `state.anim.hold` (:415) не читается нигде · `wrap01` (:166) не используется · toggle-ветка `buildSliders` (:617–623) недостижима · `_scratch` (:487) только растёт, не уменьшается (:524).

**Ложь в UI:** подпись обещает пунктирную рамку кадра (:140) — **в файле ноль `setLineDash`/`strokeRect`**, `computeFR` (:875) даёт `FR={0,0,W,H}`, канвас всегда во весь вьюпорт. **Canvas ratio не отображается в превью вообще**, влияет только на кроп при экспорте — кадрируешь вслепую. Для VJ-инструмента на проекторе это чиним в P0.

### 10.4 Модуль маппинга — `music-viz/mvj-inject.html` (144КБ, 13.07)

**Терминология:** «карточка маппинга» = **мод-строка**, DOM-ряд. Не картинка, не ассет. Сущности «card» в коде нет.

**Модель — три уровня:**
```
layers[]  (layers[0] = ВЕРХНИЙ)
  └─ L = {id, engine, label, blend, opacity, muted, solo, matrix:[], ifParams:[], ifMap:{}, …}   (:670)
       └─ matrix[i] = row   ← одна мод-строка
```
Глобальный `matrix` — **ссылка**, не копия, на `activeLayer().matrix` (:549, переприсваивается :654, :678–683, :919).

**Строка** (`defRowFor` :839–841): `{feat, path, mode, base, depth, curve, atk, rel, gate, nu, lo, hi, mute, solo}` + рантайм `_v, _c, _ns, _lrn, _lo, _hi, _last, _els, _scrubs`. `ROW_KEYS` (:881) — 14 persist-полей. **`nu` (noise underlay) — восьмой элемент грамматики, в §2.4 плана его не было.**

**Ядро — `processMat` (:964–983).** Конвейер: `raw → нормализация lo/hi → curve → gate (S&H) → noise-mix → mode/depth вокруг base → clamp → attack/release → apply`.

Умные детали: `env.*` **пропускают** свой atk/rel (`if(r.feat.indexOf('env.')===0) r._v=target`) — audio-core их уже сгладил, второй фильтр даёт только лаг · при смене `path` строка перезабирает base и, **если параметр тяжёлый (`p.rebuild`), автоматически ставит `gate='beat'`** (:857) · gate это **sample-and-hold**, а не ворота в ноль (:971) — значение кривой замораживается в `_c` и обновляется только на импульс, поэтому можно модулировать дорогие rebuild-параметры.

🔴 **Конфликт строк: last-wins, не сумма.** `applyOne` (:564) — простой сеттер без аккумуляции, цикл идёт по массиву, каждая строка пишет поверх предыдущей. Две строки на один параметр → работает **только последняя**, первая жжёт CPU. **Для BRYK с весами присутствия это блокер.** Патч — аккумулятор отклонений от базы:
```js
var acc=Object.create(null);
// в теле цикла вместо applyOne:
var slot=acc[r.path]||(acc[r.path]={p:p,sum:0,n:0,base:r.base});
if(r.mode==='abs'){ slot.sum=r._v; slot.abs=true; }      // abs — override
else { slot.sum+=(r._v-r.base); slot.n++; }              // остальные — сумма ОТКЛОНЕНИЙ
// после цикла:
for(var k in acc){ var s=acc[k]; applyOne(kind,s.p, s.abs?s.sum:clamp(s.base+s.sum,s.p.min,s.p.max)); }
```

**Контракт `Param` — самое ценное для переноса.** Три провайдера, один контракт:
```js
{ key, label, min, max, step, rebuild,        // rebuild:true → троттлинг + авто-gate на бит
  scope:'src'|'root' | get()/set(v) | el }    // ОДИН из трёх способов доступа
```
- **(A) курируемый каталог** `SP` (:301–316) + dot-path walker `sBase/sGet/sSet`. **Не авто-обход state** — ручной список. Умышленно: :309 объясняет, что `post.*` выпилен не по лени, а потому что дорог и плохо выглядит. **Каталог = художественное решение о том, что стоит модулировать.**
- **(B) манифест через замыкания** — `FX_BANK` + `fxParamsFor` (:340–357), `verletParamsFor` (:432–434). Params как `[key,label,min,max,step,default]` + `apply(el,vals)`. **Самый чистый образец для BRYK.**
- **(C) 🔑 авто-скан по `data-key`** — `scanInto` (:566–577): любой билдер, у которого ползунки помечены `data-key="path.to.param"` и лежат в `.row` с `<label>`, **автоматически становится модулируемым. Ноль кода со стороны билдера.** Это ключ ко всей масштабируемости. Переносим ИДЕЮ (движок помечает контролы, хост находит), не реализацию через `contentDocument`.

**Фичи** (`FEATURES` :699, 19 шт; `featVal` :703): `rms · flux (×8, мал по природе) · onset · band.{sub,bass,lowmid,mid,highmid,treble} · env.{bassFast,bassSlow,midFast,midSlow,highFast,highSlow,rmsSlow} · beat.phase · beat.sine · beat.pulse`. **Три `beat.*` — производные, считаются в хосте, не в audio-core:** `sine = 0.5-0.5*cos(2π·phase)`, `pulse = pow(max(0,1-phase),3)`.

**Кривые** (:704): `lin · exp (v²) · sqrt · sine (0.5-0.5cos(πv), S-кривая — НЕ 2π) · gate (v>0.5) · inv`. ⚠ ключ `'sine'`, лейбл `ease` — **разошлись, переименовать при переносе**.

**`follow`** (:705): `tau = target>prev ? atk : rel; k = 1-exp(-dt/max(tau,1e-4))` — **frame-rate independent**, иначе на 120Гц всё летит вдвое быстрее.

**Auto-learn** (:968, кнопка :859) — 4-секундное окно ловит реальный min/max фичи на конкретном треке → `r.lo/r.hi`, которые растягивают вход в [0,1]. Защита от вырожденного диапазона (минимум 0.02). **Для живого сета обязательно.**

**Noise underlay** (:974, `NU_SPEED=0.25` :254): кроссфейд аудио-огибающей с медленным перлином (`pnoise` портирован из `generative-art/noise.js`, :257), `_ns` инициализируется случайно — строки не синхронны. Философия в коде: **«audio = punch, noise = life»**, параметр дышит в тишине.

🔴 **Инвариант base-sync (классическая ловушка):** *любая* правка параметра **вне** рэка обязана записать `r.base=v; r._v=v; r._last=null` во все строки с этим `path` и позвать `.set(v)` на скрабах-близнецах. Иначе drive затрёт руку пользователя в следующем кадре («отпустил ползунок — значение прыгнуло назад»). В EQJ синхронизировано из четырёх точек: :451, :518, :954, :1101–1102.

**Сцен-банк v4** (:901–933): `captureScene` → `{v:4, input, active, layers[]}`, ровно один из `syn`/`vopts`/`cfg` по типу движка. `rebuildRows` (:883) — **правильный паттерн миграции**: свежий дефолт → `Object.assign` старых данных → сброс рантайм-полей (старая сцена без `nu` получит дефолт, не `undefined`). `applyScene` (:910–921) восстанавливает **снизу вверх** (`.reverse()`, т.к. `addLayer` делает `unshift`), `pending` для async iframe, `dmerge` (:900) чтобы старая сцена не занулила новые поля. 8 слотов localStorage, мягкая обработка переполнения. ⚠ **Сцена помечена `v:4`, ключ хранилища `musicviz_scenes_v2`, `sc.v` при загрузке никогда не читается, миграции нет** — завести настоящую или выбросить поле.

🔴 **Присвоения картинок в EQJ НЕТ.** Ни `dragover`, ни `dataTransfer`, ни `readAsDataURL`, ни `new Image()`, ни `<input accept="image/*">`. Единственный `FileReader` (:932) читает JSON сцены. Единственный drop во всём проекте — `synthex-music.html:2608–2621`, и он берёт **первый `<path d>` из SVG** как контур формы (`src.form.customD`), это геометрия, не ассет. Регулярка `/<path[^>]*\sd="([^"]+)"/` игнорит transform, viewBox, группы — при переносе заменить на `DOMParser`.
**Хорошая новость:** контракт `Param` (B) покрывает случай идеально. Новый движок `image-layer` с `params:[['scale',…],['opacity',…],['hueShift',…]]` и get/set-замыканиями встаёт в рэк **без единой правки в `processMat`**. Это надстройка, не перенос.

**Не переносится:** машинерия iframe-хаба (`scanInto` через `contentDocument`, `setNoUI` :578–584, `onBuilderLoad`, `renderProxy`, захват cfg) · facet-режим (:464–476, :871–879 — `_els`/`_scrubs` как массивы, источник половины сложности; переносить как одиночные ссылки) · `composite()` и стек слоёв · `SP`-каталог содержательно (форму — да) · `MOIRE`-банк (кроме рефлекса :954: после смены пресета все `r.base` перезабираются) · транспорт/BPM-UI. **`BroadcastChannel` в файле отсутствует** — переносить нечего.

**Ядро переноса: 8 блоков, ~120 строк, ноль зависимостей** — `defRowFor`+`ROW_KEYS`+`rowsOf`/`rebuildRows` · `processMat`+`gateFired` · `featVal`+`curve`+`follow` · словари · контракт `Param`+`findP`/`baseFor`/`applyOne` · `mkScrub` · `captureScene`/`applyScene`/`dmerge` · `audio-core.js` целиком (87 строк, DOM-free, с тестом).

### 10.5 Панели — `synthex-engine/engine/Synthex Engine — v1.html` (5917 строк)

`Synthex/` — концепт-демо от 03.07 без `mkScrub`. `synthex-web/` — лендинг, панелей нет. **Тянуть только из `synthex-engine/engine/` + точечно из `music-viz/mvj-inject.html`.**

**Двухсторонняя раскладка** (:95–104): не grid и не flex на body, а **два `position:fixed` борта по 276px**; вьюпорт — прямоугольник между ними; таймлайн докается в его низ (`#timeline{left:276px;right:276px}` :328). Левая `#panel` — СТАТИКА (объект/построение/вектор), не схлопывается. Правая `#rightcol` — ДЕКОР/инспектор, три схлопываемых `.sidep` (Behaviour → Finish → Export). Схлопывание :5399–5408 с `aria-expanded`, ре-триггером анимации и `scrollIntoView({block:'nearest'})` с уважением к reduced-motion. Хром прячется классом `body.noui` (:366) + `?noui` в URL; **рендер знает про панели** — `px = noui?0:PANEL_W*DPR` (:1473–1474), сцена реально расширяется.
⚠ **276 захардкожен в 4+ местах.** Первым делом завести `--panel-w` (есть в `mvj-inject.html:14`).

**`mkScrub` (:3716–3774) + CSS (:224–238) — главный артефакт, берём эту версию.** Строгое супермножество EQJ-версии (:268–298, предок, «порт Наряд 5»):

| Возможность | engine | mvj |
|---|---|---|
| **drag-breakout** (упор 28px → прорыв без предела) | **есть** | нет — `clamp` в pointermove |
| границы жеста фиксируются на pointerdown (`span/mn/mx`) | **есть** | нет — чувствительность плывёт |
| **bipolar-заливка ОТ нуля** (min<0<max) | **есть** | нет — всегда от левого края |
| стрелки выходят за диапазон | **есть** | нет |
| ↑↓ прямо в режиме ввода, Shift ×10 | **есть** | нет |
| `aria-label`/`valuemin`/`valuemax` | **есть** | только `valuenow` |
| Shift ×0.25 точность, click→type, Esc-cancel, blur-commit, `setLive` | есть | есть |

Формулировка Роба (W-ROB-20, `docs/…WTERM.md:242–249`): *«упёрся в край → продолжаешь тянуть — тянется дальше, весь вьюпорт, как оси XYZ/Figma»*. Проверено синтетическим drag: `0.2 → край 2.5 → порог держит 2.5 → прорыв 10.04`. Единственная внешняя зависимость — глобальный `clamp`.

**Дозабрать из mvj:** `cfg.extend` + внешний `extendParamRange` (:844) для параметров с чужим range/number · `mirror()` синк близнецов (:862–863) · `--panel-w`, `--h-row:24px`, `--h-btn:30px` (:11, :14) · **тёмная полярность `:root[data-eqj="dark"]` + `.eqj-theming` one-shot** (:47–51) ← **это база тёмной темы BRYK** · тонкие скроллбары (:146–150) · M/S/✕ `.mtgl` (:208–219) и грамматику слоя `.lrow` (:106–131) — **в движке их нет**, а карточке режима с PRESENCE они нужны · `mount()` в слот `data-mnt` (:847–870) — под карточки режимов.

**Декларации:** ряд = `[key, name, min, max, step, help?]`, сигнатура **`(0,1,1)` → тумблер** автоматически (`row()` :3775–3790). Группы = `['#селектор', [[ряды…]], callback]` (`GROUPS` :3963–3994), пересборка `rebuildPanel()` (:3998+). Доступ к state — `gp()/sp()` (:3705–3712) со скоуп-роутингом source vs root.
⚠ **Контекстное скрытие рядов — ПОКАЗОМ, не пересборкой** (:3949–3954): `rebuildPanel()` внутри обработчика `input` уничтожил бы ровно тот ползунок, который тянут. Схлопывание по клику на legend снято сознательно (:3398) — легенды просто заголовки, панель скроллится.

**Токены (:56–75, :192–194):** `--fz-s/m/l` 9/10/11 · `--sp-0..3` 2/4/8/12 · `--r-ctl/mid/outer/full` 4/6/10/99 · `--tint/tint-hi/tint-card` 7%/12%/4% ink · `--ease-out cubic-bezier(0.23,1,0.32,1)` · `--dur-press 100ms` / `--dur-pop 180ms` · `--sec-zone #f7f7f7`. **`--accent-live: var(--ink)`** — акцент это градация чернил, не хью; цвет зарезервирован под `--accent-warn` (запись/варн). `--muted` затемнён `#8b8880 → #6f6c64` ради AA (3.25:1 → 4.81:1). Панель на **моно**, гротеск (`--grotesk`, локальный Inter без сети) **только** у legend/.sphead/#ptitle. Иерархия `#ptitle 11 > legend 11/600 > .row label 10 = .sval 10` — лейблы приведены к размеру чисел специально (W-ROB-16), «ряд читается цельным». Капс и `.1em` **сняты по слову Роба** (W-ROB-15).

**Состояния (:368–393) — законы, не строки:** hover всегда за `@media (hover:hover) and (pointer:fine)` · hover не трогает выбранное (`:not([aria-pressed="true"])`) · press = **только transform** `scale(.97)`, никогда `transition:all` · focus-visible **один токен на всю панель** (:223), не на компонент · disabled = `opacity:.42; pointer-events:none` · reduced-motion глушит панель полностью · `prefers-contrast:more` → inset-линия в ink. Клавиатура-транспорт игнорит события внутри `.scrub` (:5690), иначе Space правил значение и ставил паузу. **Фокус восстанавливается после `rebuildPanel`.**

**Driven (`driveApply`/`driveRestore` :4737–4762) — берём как есть.** База и live разведены **физически**: `.sfill` = редактируемая база, `.slive` = риска живого значения (абсолютный элемент, двигается только `left` — без reflow, можно звать 60 раз/сек). `driveRestore()` возвращает базы после кадра — **база не мутируется насовсем**; страховка в начале `driveApply` на случай, если прошлый кадр умер до restore. Диапазон цели читается **из `aria-valuemin/max` самого DOM** — реестр `SCRUBS[path]` и ARIA как единый источник правды; offset нормирован 0..1 и масштабируется диапазоном конкретного скраба, поэтому один LFO одинаково ведёт и `BPM 40..220`, и `alpha 0.05..1`. Контракт в UI (:512): *«Drive modulates dials live · each dial keeps its editable base»*.

**Дисциплина:** `docs/PANEL_MAP.md` — карта всех `fieldset[id]` и `SEC:`-маркеров; гейт `map-gate` в `probe/health.cjs` падает, если карта разошлась с кодом. Плюс `probe/w15-zones.cjs` — автотест геометрии зон (panel-zones 22/22). **Переносим вместе с кодом.**

### 10.6 Цвет и шрифт — что есть и чего нет

**Пикера-окна в Synthex НЕТ.** «Окно с ползунками и пипеткой» — это **нативный `<input type="color">`**, окно рисует macOS (её системная панель действительно имеет и ползунки, и экранную пипетку). Своей HSL/OKLCH-плоскости, hue-кольца и пипетки в движке не написано. Для BRYK это значит: механика бесплатна и работает, но **окно нельзя стилизовать** — на тёмной аппке откроется macOS-панель, не наша.

**Банк цветов ЕСТЬ** — стопы градиента, `buildStops()` + `#addStop` (:5327–5352), CSS `.crow.stoprow` (:279–283). Ряд = свотч `.spick` + hex-поле + позиция + `×`. **Ёмкость 2–6 жёстко — для BRYK поднять (Роб: 8 и больше).** Три решения забрать дословно:
1. `+ Color` ищет **самый широкий промежуток** и ставит стоп в его середину, не в конец списка.
2. Цвет нового стопа берётся **из уже посчитанной рампы в точке p** — добавление цвета не меняет сам градиент, только даёт новую ручку.
3. `del` прячется через `visibility`, не `display` — сетка ряда не прыгает при 3→2.

`normStops()` (:1870–1876) — единая точка санации: чинит битый массив, клампит позиции, валидирует hex, **сортирует по p**. Переупорядочивания драгом нет и не нужно: порядок — производная позиции.

**Полоса-рампа `#rampBar`** (:5220–5255, :5356–5385) — контрол с двумя смыслами на одной поверхности: драг ручки = позиция стопа, драг между ручками = сила рампы, dblclick = добавить стоп. Приём `amtHist`: dblclick прилетает **после** двух `pointerdown`, каждый уже подвинул `amount` — история из двух значений откатывает сдвиг.

**Две цветовые структуры, разного рода — не путать:**

**(A) `state.palette` — НЕ банк.** Ровно 4 слота фиксированной длины, добавления/удаления нет (:1332): `[0]=A/левый край`, `[1]=Paper`, `[2]=B/правый край`, `[3]=Lead`. Свои пикеры только у `[1]` и `[3]`. **У A и B пикеров нет намеренно: `syncRamps()` записывает края обратно в `palette[0]`/`palette[2]` (:1878)** — один источник правды, это стопы. Санация битого массива при загрузке cfg :1405–1406 (`palette[1]=undefined` роняло `paperNow→invHex→hexToRgb`).

**(B) `state.tone.stops` — настоящий банк.** `[{c:'#rrggbb', p:0..1}]`, 2–6 штук, с добавлением и удалением.

**Хранение — плоские hex-строки, но смешивание УЖЕ в OKLab.** Это поправка к §2.5: перцептивная интерполяция строить не надо, она есть. `mixHex(a,b,t)` через `srgbToOklab` (:713) и `makeRamp(stops,n)` (:716–719) — чистая самодостаточная математика, конвертит стопы в OKLab и сортит по pos. Формат хранения при этом остаётся hex, поэтому сериализация в cfg тривиальна (`JSON.stringify`, :1804). **CSS-переменных в цветовом пути нет вообще** — токены `:root` красят только хром панели; цвет артефакта идёт прямым чтением state в canvas-код: `syncRamps()` печёт **LUT на 256 позиций** `toneRamp.css[0..255]`, рендер бьёт индексом (:1922, :2956, :3161, :3376, :5246). `TINT` (:1986) — Map-кэш производных оттенков, сбрасывается в `syncRamps`.

**Извлекаемый блок цвета: ~130 строк JS + ~12 CSS**, зависимости только `clamp01`/`clamp` и токены. `normStops` (1870–1876) · `syncRamps` (1877–1880) · `makeRamp/mixHex/hexToRgb/srgbToOklab` (709–719, самодостаточны) · `buildStops`+`#addStop` (5327–5352) · `RB`/`rbX`/`rbP`/`drawRampBar` (5227–5255) · ramp-bar жесты (5356–5385) · `toHex` (5217–5219) · `syncPalette`/`applyColour` (5388–5396) · CSS (118–129, 279–283). `stepT` (:1903–1907) нужен только под ступенчатую рампу, иначе `t=>t`; `viewHex` заглушить как `h=>h`, если инверсия не переносится.

**Свой пикер-попап (если решим, что macOS-окно не годится): ~80–120 строк, из них новой работы одна.** Математика OKLCH готова и проверена в `color-tuner.html` (~25 строк с гамут-клиппингом) · ползунки = три `mkScrub` в трёх `row()`, ноль нового кода · контейнер = готовый паттерн `#helpPopover` (CSS :186–189 + разметка :441): `position:absolute`, `bottom:calc(100% + 8px)`, `hidden`, `role="dialog"` · пипетка = обёртка над `window.EyeDropper` на 5 строк с фичедетектом. **Реально новое — только SL-квадрат на канвасе (~40 строк), и то если хотим 2D-выбор вместо трёх ползунков.**
⚠ Баг задокументирован в CSS (:118–126) и стоит перенести вместе с кодом: `.cpick` (роль, есть `data-i`) и `.spick` (стоп, `data-i` нет) **разведены по классам** — раньше оба были `.cpick`, `syncPalette` шла по всем, у стопа `+undefined = NaN`, `palette[NaN] = undefined`, `toHex(undefined) = #000000`, и любая правка Lead красила все свотчи стопов в чёрный. **Один класс — один смысл.**

**`EyeDropper API` — ноль вхождений во всём хозяйстве.** Дописывается: `new EyeDropper().open().then(r=>applyColour(i,r.sRGBHex))` + фолбэк по `'EyeDropper' in window` (Safari не поддерживает).

**Выбора шрифта НЕТ вообще.** Ноль по `queryLocalFonts` / `new FontFace` / `document.fonts.*` / шрифтовой UI / drag-drop `.ttf` — по всей папке `Projects`. Стратегия Synthex с буквами принципиально другая: **не «выбери шрифт», а «принеси аутлайн»** — режимы Symbol/Custom и кнопка `Add SVG` (:415–419). Движок рисует кривые, а не глифы.
Для §9.4 писать с нуля, **оценка ~60–90 строк**. Переиспользовать можно только грамматику выпадашки: `mkSel` (:4171–4176) + `.tinSelect` (:285–291), и мини-канвас превью по образцу `#formPrev`/`#flowPrev` (:317–324).
Практика: `queryLocalFonts()` только Chromium, нужен secure context + жест + permission, в Safari/Firefox нет → фолбэк-список обязателен. `document.fonts.check()` **врёт на fallback-именах** — надёжнее замер ширины строки в canvas против заведомого fallback. Пользовательский шрифт файлом: drag-drop → `FileReader` → `new FontFace(name, buffer)` → `document.fonts.add()`, при экспорте base64-инлайнить, иначе артефакт потеряет шрифт.

### 10.7 Поправки к ранним секциям

- **§2.5 (цвет):** палитра BRYK — по 10.1; UI банка — из движка (10.6) с поднятым потолком; математика OKLCH — из `color-tuner.html`; пикер — нативный `<input type="color">` (macOS-панель), плюс `EyeDropper` дописать.
- **§2.6 (перформанс):** `fleet-finalize` не применяется (тёмная база, решение 10.0.1); scene-only invert через CSS-фильтр **нельзя** на камеру и растр (инвертировать видео неверно) и он **не бейкается** в `captureStream` — если нужен инвертированный экспорт, делать на уровне палитры.
- **§7.1:** номера строк подтверждены; item-контракт уточнён (10.3); скругление подтверждено; **SVG-пайплайн и привязка ассета к оси — работа, не перенос**; `faceHeading` имеет потолок 0.8.
- **§7.2.2 (SVG):** переклассифицировано из «перенос» в «разработка».
- **§8.3 (пружинные переходы) и §4 (Cascade):** в шаффлере нет ни переходов между раскладками, ни stagger — строится с нуля на `springAccel`.
- **§2.4 / §9.1:** грамматика мод-строки имеет **восемь** элементов (добавлен `nu`); **конфликт строк чинится на сумму до того, как строить веса присутствия** — иначе PRESENCE не сработает.
- **§9.3 (UI):** раскладка = два fixed-борта 276px (не grid), нав слева / инспектор справа; завести `--panel-w`; карточка режима = `.lrow`-грамматика из EQJ + скраб из движка + `mount()`.
- **§9.4 (формация):** пикер шрифта пишется с нуля; текст = **поле ввода с дефолтом `BRYK`**.
- **§3 (стек):** пост-проход WebGL поверх Canvas-2D **уже существует и проверен** (10.2) — это не отступление от «2D до упора», а финальный слот, отключаемый целиком.

### 10.8 Поправки к фазам

- **P0** += счётчик FPS и **честная рамка кадра** (в шаффлере её нет, кадрирование вслепую) · панель на `mkScrub` из движка + `--panel-w` + тёмная полярность из EQJ · `PANEL_MAP.md` + гейт с первого дня.
- **P1** += контракт `Param` и разметка контролов через `data-key` (10.4C) с самого начала — иначе потом переписывать · аккумулятор сумм вместо last-wins · инвариант base-sync.
- **P2** += SVG-пайплайн (парс viewBox + растеризация в целевой размер) и маппинг ассета на ось/полосу.
- **P3** += аудио-панели EQJ, пересобранные по канону движка, ревью `apple-design` · снятие потолка `faceHeading` · пружинные переходы и stagger (с нуля).
- **P4** += пост-слот: Chromatic как есть, fbm/liquify со снятой радиальной маской, плоскостное стекло через `frostOrDispersion` с нормалью высотного поля · Canvas-2D фолбэк аберрации, если GL не встал на боевом ноуте.

### 10.9 Ревью `apple-design` — известные расхождения заранее

Скилл лежит orphan: `~/.claude/skills/.agents/skills/apple-design/` (через `/apple-design` не зовётся, нужен симлинк в корень `skills/`).

1. **Резинка вместо порога.** Apple требует **прогрессивного** сопротивления на границе; скраб держит жёсткие 28px и отпускает. Логика верная, кривая нет — заменить порог на нарастающее демпфирование: `rubberband(overshoot, dim, 0.55) = (overshoot*dim*c)/(dim + c*|overshoot|)`. Поведение снаружи то же.
2. **Стекло на панели — только после замера.** Apple тянет к `backdrop-filter` на хроме; панель висит над канвасом, жующим 60fps. Блюр по всей панели стоит кадров. Не по умолчанию.
3. **Springs вместо CSS-transition** на всём, что можно схватить: анимация от **presentation-значения**, передача velocity при отпускании, проекция инерции `current + (v/1000)*d/(1-d)`, `d≈0.998`. Дефолт `damping 1.0`; bounce (`~0.8`) только там, где жесту предшествовал бросок.
4. **Отклик на pointer-down**, не на release — в скрабе уже так.

---

## 11. 2026-07-18 · Аудио-панель собрана · первый код трека

**Статус:** первый рабочий код BRYK. Панель входа звука и распределения частот пересобрана с нуля по канону Synthex, прогнана через `apple-design`, закрыта гейтом **36/36**.

```
 bryk/
 ├─ audio-panel.html    ★ панель (single-file, dark, канон Synthex)
 ├─ audio-core.js       snapshot DSP + правки BRYK (см. 11.2)
 ├─ audio-core.test.js  node-тест: 3 BPM-кейса + контракт фич + trim/shift
 ├─ probe/panel.cjs     гейт: 36 проверок (playwright из synthex-engine)
 └─ serve.sh            localhost:8931
```

Запуск: `./serve.sh` → `http://localhost:8931/audio-panel.html` (`?fix=1` включает фикстуры).
Гейт перед любой сдачей: `node audio-core.test.js && node probe/panel.cjs`.

### 11.1 Решения Роба, снятые в этой итерации

8. **Живой ± трим темпа.** «Чтобы можно было немножечко добавить и отнять в real time, так чтобы не заново его ресетить и перезапускать». Отдельно от него **подгонка в такт** (фаза), отдельно SYNC как жёсткий сброс.
9. **Правый нижний угол — зона включения и выключения интерфейса.** Скрывать хром, оставлять только анимацию. Плюс доп-настройки, всплывающие при наведении. Помечено как финальная доработка, реализовано сразу, потому что дёшево.

### 11.2 Правки в снапшот `audio-core.js` (мы владелец файла)

🔴 **Найденная проблема:** `nudge(d)` выставляет `override`, а PLL-коррекция фазы работает **только когда `override == null`** (`:69`). То есть любая подкрутка темпа глушила автодетект насовсем. Это ровно то, на что Роб жаловался: «чтобы не заново его ресетить и перезапускать».

Добавлено (все правки не разрушающие, детект продолжает идти под ними):

| API | Что делает |
|---|---|
| `setTrim(v)` / `getTrim()` | ± смещение поверх эффективного темпа, кламп ±30. **Не трогает `override`**, поэтому PLL и автокорреляция живут дальше |
| `shift(d)` | сдвиг фазы на долю доли, с заворотом. Темп не трогает. `resync()` остаётся жёстким сбросом фазы в 0 |
| `auto()` | теперь ещё и обнуляет `trim` (возврат к детектору должен быть полным) |

Формула: `useBpm = (override ?? bpm) + trim`, клампится 20–300.

Тест дополнен восемью проверками: трим поднимает эффективный темп ровно на заданное · источник остаётся `auto` · часы продолжают идти под тримом · сдвиг двигает фазу на ~0.25 · сдвиг не трогает темп · отрицательный сдвиг заворачивается в `[0,1)` · `auto()` чистит трим · трим клампится.

### 11.3 Что исправлено против EQJ-оригинала

| Было в EQJ | Стало |
|---|---|
| BPM нативным `input[type=number]` со спиннерами, при том что скрабы в файле уже были | скраб с drag-breakout и exceed→extend |
| Две самодельные сетки `.mtrow3`/`.mtrow4` на шесть контролов | одна канон-грамматика ряда |
| `disabled` инлайн-стилями (`b.style.opacity='.42'`) | класс `.is-disabled` + `aria-disabled`, состояние в CSS |
| 🔴 **ARM необратим**: `armed=true` ставился ДО запроса прав. Отказ оставлял кнопку `ARMED` навсегда и разблокировал транспорт без часов | три состояния idle → requesting → armed/failed, `armed` ставится в `.then`, откат в `.catch` с возвратом на `clock` |
| `syncTransport` читал `inSel.value`, а не `curInput` (совпадало случайно) | единственный источник правды — `curInput` |
| Лампа в `--accent`, соседние метры в `--accent-live` | один токен на смысл: `--accent-beat` |
| Хардкод `26px`/`18px` мимо шкалы | токены |
| Магические `0.10 / 0.09 / 3.2 / 0.7 / <5` в кадровом цикле | именованные `WARM_*` |
| `v==='fix90'?90:v==='fix174'?174:124` (добавь `fix140` → молча 124) | `+v.slice(3)` |
| Label-unlock только на `mousedown` — клавиатура и тач видели «input a1b2c3» | `mousedown` + `focus` + `keydown` + `touchstart` |
| `setStatus` через `innerHTML` с конкатенацией `err.name` | `textContent` + четыре вида фидбэка (`idle`/`ok`/`live`/`err`) |
| Мёртвый CSS `#musicP input[type=range]` (в панели нет ни одного range) | выброшено |
| `<div id="mstatus">Fixture running</div>` при буте на `clock` — ложь до первого кадра | честный стартовый текст |
| `[` / `]` задокументированы, но **никогда не были забиндены** | забиндены, и на **фазу**, а не на темп |
| Клик по `−`/`+` затирал ввод, если поле в фокусе | проверка `isEditing()` |

### 11.4 Что сохранено дословно (хорошие решения EQJ)

- **Пересоздание core на реальном `sampleRate`** в `ensureCtx()`: границы полос считаются от `sampleRate/fftSize`, без этого на 44.1 против 48 кГц все полосы уезжают.
- **`echoCancellation/noiseSuppression/autoGainControl: false`** на живом входе. Все три — обработка речи, музыку они едят.
- **`getDisplayMedia({audio:true, video:true})`** с немедленным глушением видеотрека: без `video:true` Chrome не предложит вкладку вообще. Плюс отдельная проверка `getAudioTracks().length` с человеческой инструкцией «поставь галку Share tab audio» — это единственная реальная причина отказа.
- **Warming-подложка**: первые 5 секунд AGC ещё не развёл min/max, реальные уровни у нуля и панель выглядит сломанной. Бегущая волна с фазовым сдвигом по индексу метра + мутный цвет + подпись читаются как «греется». `Math.max`, а не замена — настоящий сигнал пробивает подложку сразу.
- **Метры через `transform: scaleX`**, не `width`: только композитинг, нулевой layout.
- **Не перетирать поле в фокусе** при зеркалировании живого BPM.
- **Бут на `clock`** — никакого запроса разрешений при загрузке.

### 11.5 Новое: честный спектр

🔴 **Найденное:** «распределения частот» в EQJ по сути не было. Шесть полос `sub…treble` — единственное, что видел пользователь, и **каждая нормализована своим скользящим AGC** (`audio-core.js:52–54`). Поэтому их высоты **между собой несопоставимы**: `sub` в тишине показывает то же, что `bass` на бочке. Как индикатор «полоса живёт» работает, как картина спектра вводит в заблуждение.

Добавлен канвас спектра из **сырых бинов** по логарифмической оси частот 20 Гц … 16 кГц, с медленным peak-hold и рисками границ шести полос, чтобы метры ниже можно было найти в картине. Бренд-градиент синий → жёлтый → красный по оси частот. Подпись под ним честно говорит, что полосы ниже нормализованы независимо.

### 11.6 Ревью `apple-design` — что применено

1. **Резинка вместо порога (находка №1 из §10.9 — закрыта).** Скраб Synthex держит жёсткие 28px и отпускает. Apple требует прогрессивного сопротивления: жёсткий порог читается как «замёрзло», нарастающее — как «отзывается, но дальше ничего нет». Чистая асимптотическая резинка тоже не годится, она бы не пустила дальше, а требование Роба (W-ROB-20) — доехать куда угодно. Решение: резинка на первом участке, затем плавный возврат к 1:1. Непрерывно и по значению, и по производной.
   ```js
   const RESIST_PX = 26;
   function resist(over){ const c=0.55;
     const soft=(over*RESIST_PX*c)/(RESIST_PX+c*over);   // асимптота (Apple rubberband)
     const t=Math.min(1, over/(RESIST_PX*2));            // 0→1 на двойной зоне
     return soft*(1-t) + over*t; }                       // …и отпускаем в линейность
   ```
2. **Отклик на pointer-down**, 3px гистерезис до перехода в drag, `scale(.97)` на нажатии — только transform.
3. **Лампа бита с пружинным оседанием** вместо линейного спада: атака 0.55, релиз 0.12. Удар читается телом, а не графиком.
4. **Пространственная привязка**: тулбар угла растёт из самой кнопки (`transform-origin:100% 100%`), а не из центра. Вход и выход по одному пути.
5. **Из ловушки есть выход**: при скрытом хроме угловая кнопка остаётся (гаснет до 18%, поднимается на hover/focus). Мёртвый угол оставил бы пользователя без пути назад.
6. **`backdrop-filter` на панели не ставим** — панель висит над канвасом на 60fps, блюр стоит кадров. Только после замера.
7. **reduced-motion** гасит всю панельную анимацию, `prefers-contrast:more` усиливает разделители.

### 11.7 Найденный баг канона Synthex (ретрофит)

🔴 **Легенды групп клипаются под пользовательским переопределением интервалов (WCAG 1.4.12).** В движке `legend` позиционирована абсолютно с **фиксированной `height:14px`** (`engine:206–218`). Фиксированная высота на текстовом контейнере — прямое нарушение закона §5 `adaptive-typography`; при включённом стресс-тесте текст обрезается.

Абсолютная посадка была нужна, чтобы нативная рамка `fieldset` не шла сквозь текст. Но у нас (и у движка) `border:0`, разделитель рисуется `box-shadow: inset 0 -1px`. Рамки нет — трюк не нужен. В BRYK легенда переведена в обычный поток без фиксации высоты.

**Это стоит отнести обратно в Synthex Engine** — там ровно та же конструкция и, вероятно, ровно тот же дефект.

### 11.8 Гейт

`node audio-core.test.js` — 3 BPM-кейса (±0.2), контракт фич, 8 проверок trim/shift.
`node probe/panel.cjs` — **36/36**: консоль чиста · panel-canon (нет голых range, у каждого скраба число + aria + tabular-nums, ноль хекса вне `:root`, нет `transition:all`) · геометрия (276px, высоты контролов 22/22/22/22, нет горизонтального скролла, кнопки ≥30px) · контраст (подписи 5.64:1, числа и легенды 16.15:1, акценты 6.21 / 11.75 / 4.81) · WCAG 1.4.12 без обрезаний · спектр рисует · метры на transform · бут не просит прав · детектор ловит фикстуру и остаётся `auto` под тримом · база ≠ эффективный темп · сдвиг фазы не трогает темп · AUTO чистит трим · хром прячется и возвращается.

Контраст меряется **чтением пикселя из канваса**, а не парсингом строки: computed-цвета приходят как `oklch()`, и разбор цифр из строки даёт мусор (первая версия проверки на этом и села, показав всем парам 1:1).

### 11.9 Открытое

- Тулбар угла сейчас несёт заглушки (Mirror camera / Show meters) — наполняется на P2/P3 по мере появления камеры и режимов.
- Диапазон трима по умолчанию ±6 BPM, расширяется скрабом на ходу. Проверить по глазу на реальном сете: возможно, ±3 честнее как дефолт.
- Шаг сдвига фазы 0.02 доли. Тоже под глаз Роба.


---

## 12. 2026-07-18 · P0/P1 сшиты — приложение работает

**Статус:** BRYK запускается и держит 60 fps. Гейты: `audio-core.test.js` зелёный, `probe/panel.cjs` 36/36, `probe/app.cjs` 36/36.

```
 bryk/
 ├─ index.html          ★ приложение (1370 строк, single-file, zero-dep)
 ├─ audio-panel.html    аудио-панель отдельно (стенд/референс)
 ├─ audio-core.js       DSP + правки BRYK (§11.2)
 ├─ audio-core.test.js  node: 3 BPM-кейса · контракт фич · trim/shift
 ├─ probe/panel.cjs     гейт панели  · 36 проверок
 ├─ probe/app.cjs       гейт приложения · 36 проверок
 └─ serve.sh            localhost:8931
```

Запуск: `./serve.sh` → `http://localhost:8931/index.html` (`?fix=1` — фикстуры).
Гейт: `node audio-core.test.js && node probe/panel.cjs && node probe/app.cjs`

### 12.1 Что собрано

- **Левая колонка** — аудио целиком из §11: источник, ARM с тремя состояниями и откатом, BPM/Trim/Phase, спектр на сырых бинах, 10 метров, warming-подложка.
- **Правая колонка** — Modes · Shape · Camera · Palette · Formation · Post.
- **Единый пул** (§9.1): один массив тел, режимы — «программы» над ним. Реализованы пять: `grid · ring · tunnel · scatter · word`.
- **PRESENCE как вес, а не прозрачность.** Цель тела = взвешенный бленд целей активных режимов, веса нормируются. При `w→0` режим перестаёт тянуть тела вообще. Морф A→B — это два веса. Проверено: добавление второго режима на равном весе сдвигает тело на 0.91 world-единицы, то есть бленд реальный, а не подмена картинки.
- **Транспорт к цели** — экспоненциальный follow (`1-exp(-dt*6)`), кадрово-независимый. Смена режима **приезжает**, а не щёлкает (в шаффлере был hard cut, §10.3).
- **Материал** — процедурные текстуры примитивов (rect/circle/triangle) со скруглением по правилу движка (процент от короткой стороны, запекается в текстуру). Смена палитры перерисовывает текстуры, не сцену.
- **Палитра** — банк стопов с добавлением/удалением, интерполяция в **OKLab**, LUT на 256, потолок 10 (Роб просил 8+). «+ цвет» ищет широчайший промежуток и берёт цвет из уже посчитанной рампы, поэтому добавление ручки не меняет градиент. Пять пресетов по клавише `P`.
- **Формация** — текст растеризуется в маску, маска сэмплится в точки, тела летят на них. Дефолт `BRYK`, поле ввода, выбор шрифта через `queryLocalFonts()` с курируемым фолбэком (Chromium-only API, в Safari/Firefox его нет). Клавиша `W`.
- **Мод-матрица** — контракт `Param` из EQJ, **с починенным суммированием** (§10.4): отклонения от базы аккумулируются и пишутся один раз, а не «последняя строка побеждает». Базы восстанавливаются после кадра, живое значение показывается риской на скрабе — ползунок не дёргается под рукой.
- **Пост** — один WebGL-проход поверх готового 2D-кадра, 2D-канвас скармливается как текстура, без FBO. Хроматическая аберрация 12-tap. При 0 канвас скрыт и ничего не загружается.
- **Угол** — `H`/кнопка прячет обе колонки, сцена расширяется на весь экран, кнопка остаётся живой. `B` блэкаут, `F` фуллскрин, `P` палитра, `W` формация, `T` тап, `[`/`]` фаза.

### 12.2 🔴 Найденное и починенное: бюджет квадов

Перспективный квад стоит клип + аффинный `drawImage` на ячейку сетки, и в Canvas2D это дорого независимо от плотности сетки. Замерено на старте: **300 развёрнутых тел = 20 fps**, при огрублении сетки до 2×2 — всего 26.

Причина архитектурная: движок шаффлера строился под пару десятков крупных кадров-фотографий, а не под рой. §10.3 это предсказывал; цифра оказалась хуже ожидаемой.

Два хода, второй решающий:
1. **LOD** — тело мельче 34px рисуется билбордом (своей перспективы оно всё равно не покажет), 34–90px получает сетку 2×2 вместо 4×4. Дало 20 → 26 fps. Мало.
2. **Бюджет квадов** — полную обработку получают только **80 самых крупных** тел кадра, остальные билбордятся. Порог вычисляется по `P.k` каждый кадр. **Цена кадра перестала зависеть от количества тел.**

Итог (headed, реальное окно): 60 fps на всех сценах, включая **800 тел с тремя режимами в блендинге и аберрацией**. До правки эта же сцена не поднималась выше 20.

Правило на будущее: **любой новый эффект per-body с клипом обязан иметь бюджет.** Без него сцена деградирует не постепенно, а обвалом.

### 12.3 🔴 FPS в headless не гейтится — и не должен

Headless Chromium рисует канвас через SwiftShader без vsync. На одном и том же коде: 300 билбордов — 110 fps, 300 развёрнутых — 31 fps; в headed-окне обе сцены дают ровные 60. Цифры не про машину Роба и не сравнимы с ней.

`probe/app.cjs` печатает fps **информационно** и гейтит вместо него **форму кривой**: билбордный путь масштабируется сублинейно, а бюджет квадов держит развёрнутый путь (1000 развёрнутых ≥ 60% от 300). Это то же решение, к которому пришёл Synthex Engine («perf-line — INFORMATIONAL, never gates — headed-only»).

Судить кадры — в реальном окне, на боевом ноуте.

### 12.4 Правки к каноническим утверждениям

- **§10.3 (шаффлер):** «`drawQuadWorld` берём как есть» уточняется — берём, но **обязательно с бюджетом**. Без него ядро не годится для роя, только для десятков крупных тел.
- **§11.7 (баг легенды):** фикс перенесён в `index.html`. Ретрофит в Synthex Engine по-прежнему открыт.
- **Честная усечка:** гейт различает обрезанный текст **с** `title` (полная строка в одном ховере — допустимый паттерн) и **без** (потеря содержимого). Первая находка на «Hue drift ← centroid» решена укорачиванием подписи, а не ослаблением проверки.

### 12.5 Открытое / следующий заход

- **Камеры нет.** §2.2 и §7.2.4 (видео как текстура, маска формы внутри texture-canvas) не тронуты — это следующий крупный кусок.
- **Мод-рэк уровня 2** не построен: матрица сейчас с зашитыми связками (`bassFast→size`, `rmsSlow→persp`, `onset→zoom`, `flux→face`). Контракт `Param` под рэк уже готов, панель строится поверх него.
- **Физика (§8)** не подключена: `springAccel`, `attract`, `curl`, боиды, `resolveBodies`. Пул под них готов — силы двигают `x/y` поверх целей режимов.
- **Сцен-банк** не сериализуется. Формат из §10.4 (`captureScene`/`applyScene`/`dmerge`/`rebuildRows`) переносится как есть.
- **Квантование по `onBar`/`onPhrase`** для смены режимов не включено, хотя флаги в ядре есть.
- **Тулбар угла** несёт заглушки — наполняется вместе с камерой.
- По глазу Роба: дефолтный размах трима (сейчас ±6 BPM), шаг фазы (0.02), `QUAD_BUDGET` (80), `LOD_QUAD_PX` (34).

### 12.6 Правки по глазу (визуальная приёмка 2026-07-18)

Скриншоты вскрыли четыре вещи, которых числа не видели. Все четыре — настоящие, не косметика.

1. 🔴 **Матрица раздувала сцену.** `bassFast → size` стояла с глубиной 0.55, а глубина считается долей ПОЛНОГО диапазона параметра. На диапазоне 8–160 это раздувало тело с 52 до 135px на каждый удар: не пульс, а строб из гигантских плиток, лезущих за экран. Глубины перенастроены под фикстуру 124 (`size` 0.55→0.14, `persp` 0.60→0.40, `zoom` 0.12→0.06, `face` 0.70→0.55), диапазон `size` сужен до 8–120, дефолт 52→34. **Правило: глубина это доля диапазона — чем шире диапазон, тем меньше число.**

2. 🔴 **Палитра уходила в грязь, потом в зелень.** Смешивание в OKLab по прямой давало между синим и жёлтым бежевый (низкая хрома в середине отрезка). Перевод в OKLCH по короткой дуге убрал бежевый, но выдал **зелёный**, которого в бренде нет: короткая дуга 250°→88° идёт через 170°. Длинная пошла бы через маджентовый. **Вывод: модель была неверной.** Три доминанты Роба это НАБОР, а не градиент. Тела берут цвет стопа напрямую, по одной текстуре на цвет палитры; рампа осталась для превью и hue-drift. Мути и зелени нет, на сцене ровно синий/жёлтый/красный.

3. 🔴 **Формация не читалась — два наложенных бага.**
   а) Точки раздавались как `pts[i % pts.length]`. При 420 телах и 930 точках маска сканируется построчно, поэтому тела занимали только первые 420 точек, то есть верх букв, и слипались в полосу. Исправлено на страйд по всей маске.
   б) Даже после этого плитка была в 2–5 раз крупнее ячейки маски и забивала просветы букв. **Слово из частиц читается только когда частица размером с ячейку** — частицы и есть пиксели. Введены: адаптивная плотность маски (число точек подгоняется под число тел, §8.1 Motion Primer) и `sizeScale` у режима — режим может запросить множитель размера, множитель блендится теми же весами, что и позиция. Параметр `fit` (1 по умолчанию, 0 возвращает контроль ползунку Size).
   Итог: `BRYK` читается на 600 телах.

4. Подпись `warming…` наезжала на ось «16k Hz» — метрам добавлен верхний отступ.

**Метод, который сработал:** гейты 36/36 и 38/38 были зелёными на всех четырёх дефектах. Числа проверяют, что механизм жив, но не что он **выглядит правильно**. Визуальная приёмка обязательна и не заменяется пробами.

### 12.7 Гейт квадов — структурный, а не таймингованный

Первая версия гейтила отношение headless-FPS (1000 развёрнутых ≥ 60% от 300) и **флапала на 0.56 против порога 0.6** — ровно тот шум SwiftShader, о котором §12.3. Заменено на детерминированный инвариант: сколько бы тел ни было на сцене, число тел, идущих по дорогому квад-пути, ограничено бюджетом. Проверено на 200 / 600 / 1200 телах — везде ровно 80 при потолке 80, рост прекращается. Это измеримо в headless и не зависит от машины.

**Правило:** гейтить структуру, а не время. Время — информационно и только headed.

---

## 13. 2026-07-18 · Вариатив форм · физика взаимодействия · перебалансировка колонок

**Задание Роба:** (а) интеграция взаимодействия между частицами — particle-dance и Motion Primer; (б) вариатив форм, не выбор одной из трёх, а все вместе и в смеси с PNG, рандомайзер по входу; (в) правая колонка распухла, левая под музыкальный вход почти пустая — распределить грамотнее, вход ужать, оставив главное (source), и перенести влево то, что туда логично.

Гейты после захода: core OK · panel 36/36 · app 38/38.

### 13.1 Раскладка — смысловой раздел вместо переноса блоков

Не «подвинуть пару групп», а разрезать по смыслу, как в каноне Synthex (левая = статика/построение, правая = декор):

| | Слева · **DRIVE** (что задаёт поведение) | Справа · **LOOK** (как это выглядит) |
|---|---|---|
| | Audio (компакт) · Spectrum (схлопнут) · **Modes** · **Interaction** | **Shape mix** · Camera · Palette · Formation · Post |

**Аудио ужато:** источник и ARM теперь в одну строку (селект тянется, кнопка 22px рядом), лампа/BPM/conf одной полосой, спектр и метры уехали в **схлопнутую по умолчанию** группу. Кнопка ARM переименована в `ARM`/`LIVE`/`RETRY` — длинные подписи не влезали в узкую строку. Заголовок левой колонки `Audio` → `Drive`, потому что она больше не только про звук.

Схлопывание сделано на легенде (легенда и есть контрол, `role="button"`, Enter/Space, `aria-expanded`).

**Результат замерен:** содержимое левой колонки 1121px, правой 1136px. Было примерно 500 против 1400.

### 13.2 Вариатив форм — веса, а не выбор

`state.shapes = {rect, circle, tri}` — **веса**, не переключатель. Плюс `assetW` для дропнутых картинок. Банк текстур стал декартовым произведением **лиц × цветов палитры**: три примитива на трёх цветах дают девять текстур, и каждое тело носит одну.

Раздача — **seeded weighted**: `hash01(i)` по `mixSeed`. Отсюда два нужных свойства: сцена держит свою идентичность (не мерцает каждый кадр) и **RANDOMISE** перекатывает её одним движением. Проверено: перекат меняет раздачу, повтор без переката её сохраняет.

**PNG/SVG:** drop в любое место окна (оверлей на весь экран) или кнопка ADD FILES. Ассет cover-fit'ится в квадрат, режется тем же скруглением, что примитивы, и **тонируется в цвет палитры** через `source-atop` с ползунком `Asset tint` (0 — родной цвет ассета, 1 — полностью в палитру). Это ответ на грабли §10.3: чужая картинка иначе дерётся со сценой, а не живёт в ней.
Учтён баг шаффлера: SVG без `width`/`height` даёт `naturalWidth = 0`; здесь размер проставляется до растеризации, битой текстуры не будет.

### 13.3 Физика — третий регистр (§8.2 наконец собран)

Силы двигают тело в world-space **поверх** целей режимов, поэтому режим остаётся провайдером цели и ничего не знает про столкновения. При `Physics on` транспорт к цели превращается из lerp в **пружину на той же скорости**, что и силы — отсюда «тело тянет раскладка и толкает сосед в одном кадре».

| Сила | Как сделано |
|---|---|
| **Collide** | попарное расталкивание через **равномерный пространственный хэш**. План (§8.5) закладывал потолок ~150 тел на O(N²) — с хэшем те же силы держат 900+, потому что цена идёт по плотности, а не по популяции |
| **Swirl** | curl-поле аналитически из двух синусов: `ψ = sin(a)·cos(b)`, ротор берётся руками. Дивергенция нулевая по построению, поэтому тела **закручиваются, а не стекаются в сток**. Ноль зависимостей, не тянем реализацию шума |
| **Attract** | магнит с усилением у центра (`boost/(d+soft)`, Motion Primer) |
| **Gravity** | биполярная, вверх тоже |
| **Flock** | когезия к локальному среднему, переиспользует ту же решётку |
| **Damping / Follow** | характер прилёта |

🔴 **Найдено при проверке: магнит был слабее раскладки.** При `attract=2` и `follow=3` рой оставался на радиусе 2.4 — пружина к сетке била магнит в пять раз. Это ломало приём «Магнит-дроп» (§8.3) в принципе: build-up обязан уметь всосать всё к центру. Коэффициент поднят так, чтобы магнит выигрывал на максимуме. Проверено: 2.78 → **0.73** при включении, обратно к 2.78 при отпускании, без схлопывания в точку.

### 13.4 🔴 Сетка вылезала за кадр

Шаг сетки был константой 0.42 независимо от количества. На 320 телах это 18 колонок = 7.6 world = 960px на сцене шириной 888 — композиция падала с обоих краёв. Шаг теперь выводится из количества (`EXT/max(cols,rows)`), `gap` масштабирует от этого. Проверено на 120 / 320 / 700 телах: габарит держится 2.13 → 2.26 при полукадре 3.5.

### 13.5 Открытое

- **Рандомайзер «по input»** сейчас ручной (кнопка). Автоперекат по `onBar`/`onPhrase` — следующий шаг, флаги в ядре есть.
- Ассеты не сериализуются в сцену (как и в EQJ — там растровых данных нет вообще). Решить: base64 в json или ссылки.
- Формы пока не смешиваются **внутри одного тела** (маска формы × картинка) — §7.2.4 про камеру закроет этот же пайплайн.
- Камеры по-прежнему нет.

---

## 14. 2026-07-18 · Камера-рельс — третий кит из §0 закрыт

Гейты: core OK · panel 36/36 · app 38/38.

### 14.1 Обе роли камеры реализованы

Как и решено в §0.3 — камера входит **двумя ролями**, и обе живут на одном декоде:

1. **Текстура внутри фигур.** Фигуры режут живое видео. Реализовано по §7.2.4: маска формы применяется **один раз внутри texture-canvas**, а не на каждый инстанс. Три canvas'а на кадр (rect/circle/tri) обслуживают сколько угодно летающих «видео-кругов». Проверено: центр текстуры непрозрачен, угол круглой текстуры пуст — маска реально режет.
2. **Слой в миксе.** Отдельный `#camcv` под или над геометрией, со своим blend (screen/normal/multiply/overlay/difference/lighten) и прозрачностью. Порядок слоёв — переключение z-index, бесплатно.

Камера входит в тот же **микс форм**, что примитивы и ассеты: `camW` это ещё один вес. «Летающий треугольник с толпой внутри» — просто запись в той же раздаче.

### 14.2 Стилизация — не опция, а дефолт

§2.2 требовал этого прямо, и требование верное: клубная камера тёмная и шумная, сырая вебка на проекторе читается как ошибка. Конвейер: `contrast/brightness/saturate` фильтром → **дуотон в активную палитру** (multiply тёмным стопом + screen светлым, два прохода держат средние тона читаемыми). Ползунки Duotone / Contrast / Gain, зеркало флагом. «Чистое видео» — это дуотон в ноль, то есть опция.

Дуотон уже положил видео в палитру, поэтому камера-текстуры **не размножаются по цветам палитры** — перекраска по стопам только замутила бы их. Три текстуры, не девять.

### 14.3 Честный фолбэк

Нет камеры или отказ в правах → слой выключен, камера-лица не входят в микс вообще, фигуры остаются на палитровых заливках. Никаких чёрных дыр (требование §2.2). Сообщение человеческое, кнопка уходит в RETRY. Клавиша `C` включает и выключает.

### 14.4 🔴 Найденный баг специфичности CSS

`#camcv{display:none}` **никогда не применялось**: селектор `#stage canvas` это id + элемент (0,1,1) и он бьёт голый id (0,1,0). Тот же дефект был у `#glcv` и его маскировал инлайн-стиль, который пишет кадровый цикл — то есть пост-слой скрывался не правилом, а побочным эффектом рендера. При загрузке до первого кадра оба оверлея были `block`.

Исправлено поднятием специфичности (`#stage #glcv`, `#stage #camcv`). **Урок: если правило работает только потому, что JS каждый кадр пишет инлайн-стиль, это не правило.**

### 14.5 Как проверялась камера без камеры

Гейт не может рассчитывать на физическое устройство. Пайплайн проверяется подстановкой: `videoWidth/videoHeight/readyState` подменяются на видео-элементе, а `drawImage` перехватывается и подставляет синтетический кадр вместо `<video>`. Дальше проверяется весь путь — снапшот, дуотон, маска формы, вход в микс, слой, FPS. Реальное устройство проверяет только Роб глазами.

### 14.6 Открытое

- Автоперекат раздачи форм по `onBar`/`onPhrase` (рандомайзер «по input») — ручная кнопка есть, квантование нет.
- Сцен-банк не сериализуется (ассеты и камера — отдельный вопрос: base64 или ссылки).
- Мод-рэк уровня 2 (панель поверх готового контракта `Param`).
- Приёмы драматургии §4/§8.3 как **пресеты** («Магнит-дроп», «Собраться», «Врубить толпу») — все силы под них уже есть, не собрана только раскладка кнопок.

---

## 15. 2026-07-18 · Квантование · приёмы · банк сцен · мод-рэк — хвост закрыт

Гейты: core OK · panel 36/36 · **app 49/49** (было 38 — новое покрытие: квантование, приёмы, сцены, рэк, камера).

### 15.1 Квантование — всё ложится на музыку

`QUANT` (off / beat / bar / phrase) и очередь `PENDING`. Любое действие, пущенное через `fire()`, ждёт нужного импульса из `audio-core` и срабатывает на нём, а не на скорости реакции. Через очередь проходят приёмы и вызов сцен. Дефолт — **bar**.

Плюс `Auto-randomise`: перекат раздачи форм на каждый такт. Это и есть «рандомайзер по input», о котором просил Роб.

### 15.2 Приёмы — драматургия §4/§8.3 одним жестом

Приём это функция собственного нормализованного прогресса. Пишет через те же сеттеры, что панель, поэтому ниже по течению никто не отличает руку от приёма.

| Приём | Что делает | Клавиша |
|---|---|---|
| **MAGNET** | build-up всасывает всё к центру (attract до 2.6, persp растёт), на дропе импульс наружу и возврат перспективы | `1` |
| **GATHER** | сцена уходит в scatter и вихрь, затем **снап обратно в сетку** — chaos→order с guilloché, перенесённый на геометрию | `2` |
| **CROWD** | камера-слой вспыхивает в полный кадр на два такта и уходит | `3` |
| **SNAP** | всё замерзает в жёсткую сетку (follow 18) и отпускает | `4` |

Каждый приём запоминает контекст на старте и **возвращает заимствованное** в `done()`.

### 15.3 Банк сцен — с настоящей миграцией

8 слотов в localStorage, export/import json, вызов слота квантуется. Клавиши `5`–`8`.

Миграция по образцу EQJ, который работал (`mvj-inject:883`): **свежий дефолт, затем `Object.assign` сохранённых данных поверх**. Сцена, записанная до появления поля, возвращается с дефолтом этого поля, а не с `undefined`.
Отличие от EQJ: **поле версии реально читается**. Там писали `v:4` в хранилище с ключом `_v2` и никогда его не смотрели (§10.4) — это миграция, которой нет.

### 15.4 Мод-рэк уровня 2

UI поверх контракта `Param`, который уже умеет суммировать. Строка = `feature ▸ param ▸ depth ▸ curve ▸ mode`, добавление и удаление. Семнадцать фич, шесть кривых. Живёт в схлопнутой группе — это подготовка, а не то, чем рулят в сете.

### 15.5 🔴 Один инвариант, два нарушения — обоих не было бы, будь у него единственный вход

Тот самый base-sync из §10.4, который я задокументировал и дважды нарушил:

1. **Загрузка сцены не сбрасывала базы матрицы.** Сцена ставила размер 40, а `restoreMatrix` в конце **того же кадра** возвращал закешированные 77. Сцена загружалась и молча откатывалась.
2. **Приём писал в модулируемый параметр напрямую.** `state.cam.persp = v` выглядит рабочим и отменяется через кадр: drive пересобирает live из базы.

Лечение — не два патча, а один вход: **`setDriven(key,v)`**, который пишет через сеттер параметра И перебазирует. Через него теперь идут приёмы; сцены всегда чистят `BASES`.

**Правило в канон:** если параметр может быть под модуляцией, писать в него напрямую нельзя — только через базу. Прямая запись даёт ровно тот баг, который невозможно поймать глазами (значение появляется и через кадр исчезает) и который на гейте виден мгновенно.

### 15.6 Состояние трека

Реализовано: аудио-вход с честным ARM · спектр · клок с тримом и фазой · единый пул · 5 режимов с весами · физика (collide/swirl/attract/gravity/flock на пространственном хэше) · микс форм с PNG/SVG и сидовым рандомайзером · камера в двух ролях с дуотоном · формация со шрифтами · палитра OKLCH-набором · пост-проход · квантование · 4 приёма · банк сцен · мод-рэк · перформанс-режим.

Не сделано: ассеты не сериализуются в сцену (base64 или ссылки — решить) · мастер-макро ENERGY/DENSITY/CHAOS (§9.2) · камера как маска внутри формы **в связке с ассетом** (сейчас либо/либо) · приёмы не редактируются из UI.

Открыто по глазу Роба: пороги LOD и `QUAD_BUDGET`, размах трима, шаг фазы, длительности приёмов.

---

## 16. 2026-07-18 · Объединение particle-dance × Motion Primer — весь вариатив построения

**Задание Роба:** «нужны все вариации построения как в Motion Primer, так и в particle-dance; сейчас показываешь сугубо как particle-dance. Объедини, доработай.»

Гейты: core OK · panel 36/36 · **app 55/55**.

### 16.1 Разделение труда между источниками

Справедливо, что было «сугубо particle-dance»: пять режимов до этого захода — самодельные, приблизительные. Взял настоящую математику из обоих исходников, не пересказом.

| Источник | Что даёт | Как вошло |
|---|---|---|
| **particle-dance** `patternPoint` (`:275–376`) | **ГДЕ** — 17 паттернов построения | портированы дословно + `axisPoint`, `baseParticle`, `rng`, `randomAxis` |
| **Motion Primer** `BEHAVIORS` / `MANNER_PRESET` (`:146–158`) | **КАК СЕБЯ ВЕДУТ** — 6 поведений и 6 манер | пресеты над блоком физики |

Это и есть честное объединение: у Motion Primer поведения — не позиции, а числа cohesion / separation / flow, то есть ровно то, чем у нас управляется силовой блок. Позиции же — предмет particle-dance.

### 16.2 Банк паттернов — 17, портированы дословно

`cloud · lattice · vortex · ribbon · burst · wave · spring · zigzag · spiral(DNA) · check · cross · axes · orbitals · fan · steps · glyphs · form`

Заметка: `check` и `cross` **есть в коде particle-dance, но отсутствуют в его же списке `PATTERNS`** — то есть в оригинале они недоступны из UI. У нас доступны все 17.

Каждое тело носит seeded-запись `baseParticle` (id · phase · band · точка на единичной сфере · lane · axis) — без неё паттерны не работают, они читают именно её. Для `form` добавлены минимальные полиэдры (octa, cube); в particle-dance таблица больше.

**Два слота — Pattern A и Pattern B**, у каждого свой выбор из 17 и свой PRESENCE. Значит морфится **любая пара** из семнадцати, а не фиксированный набор. Плюс структурные Grid / Ring / Tunnel / Scatter / Word остаются.

### 16.3 Поведения и манеры

`swarm · pack · magnet · orbit · fall · scatter` — каждое разом выставляет весь силовой набор (follow / collide / swirl / attract / gravity / flock / damp).
`Free · Cluster · Disperse · Flock · Flow · Vortex` — манера роя.

Числа Motion Primer заданы в пикселях его 2D-мира, наш мир ±3 world-единицы, поэтому separation/flow **отображены, а не скопированы**: характер несут отношения между манерами, а не абсолютные значения. Обе выборки идут через `fire()`, то есть квантуются на такт.

### 16.4 Гейт: контракт вместо счёта

Проба падала на `5 modes registered`, потому что режимов стало семь. Счёт режимов — дизайнерское решение, оно будет двигаться и дальше; проверять его бессмысленно. Заменено на то, что обязано держаться: **слоты паттернов существуют**, и **каждый паттерн банка даёт конечные, ограниченные, невырожденные точки** — это дословно контракт `assertCore` самого particle-dance (non-finite или `|p|>5` там тоже провал). Прогоняются все 17 по очереди.

**Правило:** гейтить контракт, а не инвентарь.

### 16.5 Открытое

- `form` идёт на двух полиэдрах вместо полной таблицы particle-dance.
- Манеры не разведены с поведениями по приоритету: выбор манеры перетирает часть набора поведения. Живьём это читается нормально (манера — уточнение), но формально это два источника правды на одни поля.
- Мастер-макро ENERGY / DENSITY / CHAOS (§9.2) по-прежнему не собраны — теперь под ними есть что раскладывать.

---

## 17. 2026-07-18 · Сверка всей базы · дальнейший план · логика панели · практики туса

**Задание Роба:** сверить всё ещё раз, прикинуть наилучшие решения, разжевать дальнейший план: логика панели, кто за что отвечает; подготовить ультра-режим — терминал параллелит максимум агентов, задачи распределены. Плюс (добавка): практики «как визуалу слышать музыку на шумной тусе» + книги/статьи.

> Нумерация: секции ресёрча Fable переименованы **§10→R10, §11→R11** — коллизия с кодовыми §10–§16 терминала снята. «§10.x» без префикса всюду = секции терминала.

### 17.0 Сверка: план ↔ реализация (по коду и гейтам, 2026-07-18 вечер)

✅ Закрыто против плана: три кита §0 (геометрия+цвет / mic / камера) · пул+веса §9.1 · 17 паттернов + 6 поведений + 6 манер §16 · Pattern A/B морф-слоты · камера в двух ролях с дуотоном §14 · формация со шрифтами · палитра OKLCH-набором (решение Роба 10.0.2) · пост-проход · квантование QUANT+fire() §15.1 · 4 приёма (MAGNET/GATHER/CROWD/SNAP) · банк сцен с миграцией · мод-рэк ур.2 · перформанс-режим. Гейты: core OK · panel 36/36 · app 55/55. Инварианты-жемчужины, поднятые терминалом в канон: **base/setDriven** (§15.5) и **гейтить контракт, не инвентарь** (§16.4).

⬜ Открытый бэклог (сведён из §15.6, §16.5, R10–R11): мастер-макро ENERGY/DENSITY/CHAOS · деформация сетки квада (FLAG/MORISAWA-волна, R10.3) · эхо-туннель warp-feedback (R11.3) · волна-осциллограф · калейдоскоп · fluid · butterchurn · RD · сериализация ассетов в сцены · камера-маска+ассет вместе · form-полиэдры полная таблица · приоритет манеры/поведения (два источника правды) · приёмы из UI · пороги LOD/QUAD_BUDGET по глазу Роба · **git в bryk/ отсутствует** — завести немедленно (урок MUSICVIZ: наработки без git чуть не потеряли).

### 17.1 Логика панели — кто за что отвечает (разжёвано)

Принцип: у каждого параметра ЧЕТЫРЕ возможных автора — рука · приём · рэк · сцена. Все пишут через один вход (`setDriven`/базы, §15.5). Панель делится не по «где влезло», а по ВРЕМЕНИ использования:

```
 ЛЕВАЯ ПАНЕЛЬ = ЧТО НА СЦЕНЕ (глаза)          ПРАВАЯ ПАНЕЛЬ = МУЗЫКА (уши)
 ┌────────────────────────────────┐            ┌────────────────────────────────┐
 │ MACRO   ENERGY·DENSITY·CHAOS   │ ← сет      │ INPUT   источник·ARM·CAL·метры │ ← настройка
 │ MODES   карточки: PRESENCE+M/S │ ← сет      │ CLOCK   BPM·tap·trim·фаза     │ ← сет (tap)
 │   Pattern A ▸ 17 · Pattern B ▸ │ ← сет      │ RACK    мод-строки (схлопнут) │ ← подготовка
 │ LOOK    палитра·пост-слот      │ ← подгот.  │ SCENES  банк 8·export/import  │ ← сет (вызов)
 │ WORD    текст·шрифт·Fill/Frame │ ← сет      └────────────────────────────────┘
 └────────────────────────────────┘
 ХОТКЕИ = ПЕРФОРМАНС: приёмы 1–4 · сцены 5–8 · QUANT · T tap · B blackout · H хром · F fullscreen
```

- **В сете** DJ трогает: 3 MACRO, PRESENCE-фейдеры, морф A/B, WORD-текст, tap, вызов сцен, приёмы. Всё остальное — подготовка дома.
- **MACRO — верх левой** (агрегат «что на сцене»); раскладка макро→параметры декларируется в пресете и живёт в сцене. ENERGY→speed/attract/kick-глубины · DENSITY→count/gap/scale · CHAOS→jitter/swirl/manner-сдвиг.
- **Иерархия авторов при конфликте:** рука > приём > рэк (рука перебазирует; приём возвращает заимствованное в done(); рэк модулирует вокруг базы). Сцена = смена всех баз разом.
- Решение Роба 10.0.5 в силе: грамматика панелей Synthex, ничего не изобретаем; это раскладка ЗОН, не новый UI.

### 17.2 Практики «слышать музыку на тусе» (ресёрч по статьям/книгам)

**Вход — иерархия честности** (единодушно во всех источниках): 1) booth/line-out с DJ-микшера (или сплиттер) — профессиональный стандарт; 2) BlackHole для системного звука; 3) микрофон — фолбэк, и тогда направленный, у динамика, не в толпе.

**Шумоглушение при mic-входе:**
- **CAL-кнопка** (в INPUT): захват 5–10с фона площадки → noise-floor по полосам → спектральное вычитание/порог. Разовая калибровка на месте.
- **Бит слушать в sub/bass** — толпа шумит в mid/high, кик пробивает низом; highpass <25Гц против гула вентиляции. Полосный селектор «что считать битом».
- **Onset-детектор**: refractory-период ~100мс (двойных срабатываний нет), гистерезис порога, adaptive whitening (Stowell & Plumbley 2007) — выравнивание спектра до детекта, классика для шумных сцен. Наш median+MAD уже хорош, whitening — апгрейд.
- BPM: confidence-гейт (авто ниже порога → держать последний), **tap всегда истина** (уже канон).

**Как часто реагировать (пейсинг):** change-бюджет по уровням — мелкое (пульс/цвет) на бит · среднее (раздача форм, манера) на bar · крупное (режим, сцена, приём) на phrase — наш QUANT ровно это, дефолт bar подтверждён практикой. Тишина/брейк = покой, экран не трясётся от шума (gate по rmsSlow). Габитуация: одинаковый ответ на каждый кик замыливается — Auto-randomise на такт (§15.1) + вариация глубин уже лечат.

**Книги/статьи (полка BRYK):** Spinrad «The VJ Book» (2005, есть на archive.org) · Faulkner/D-Fuse «VJ: Audio-Visual Art and VJ Culture» (2006) · Eskandar «vE-jA: Art + Technology of Live Audio-Video» (2006) · Jaeger «VJ: Live Cinema Unraveled» (2006) · Gibson/Arisona et al. «Live Visuals: History, Theory, Practice» (Routledge 2022, open-access — проверить при билде) · Bello et al. «A Tutorial on Onset Detection in Music Signals» (2005) · Stowell & Plumbley «Adaptive whitening for real-time onset detection» (2007).

**В код идёт:** CAL noise-floor · band-селектор бита · refractory+whitening в onset · rmsSlow-gate покоя. Трек W3 делегата.

### 17.3 Ультра-режим — принципы параллелизма

Один файл index.html ≠ песочница для толпы агентов. Схема:

1. **ИНТЕГРАТОР** — единственный владелец `index.html`; вживляет очередь анкерными патчами (метод builder-engine), гейт после каждого мержа.
2. **Параллельные воркеры** владеют СВОИМИ файлами: `audio-core.js` · новые модули-слои (`fluid-layer.js`, `milk-layer.js`, `rd-layer.js`) · данные (`shapes-data.js`) · математика в `probe/*.cjs` · QA-скрипты. Ноль пересечений по файлам = ноль конфликтов.
3. Воркер сдаёт: модуль + **анкер-спеку патча** (куда и что вживить, ≤30 строк) + контрактный гейт. Интегратор вживляет за минуты.
4. Полный расклад задач — **`BRYK_DELEGATE_ULTRA_2026-07-18.md`** (11 воркеров, приоритеты, контракты, гейты).


---

# §18. 2026-07-18 (вечер) · План перестройки интерфейса — НА УТВЕРЖДЕНИИ

Собран по указанию Роба «прикинуть общий план, понять как это всё перестроить по интерфейсу».
Источники прочитаны, не пересказаны: mvj-inject (EQJ), synthex-music, Synthex Engine v1, весь index.html, делегат-MD, apple-design, panel-canon.
Статус: ждёт ответов Роба по флагам «ГЛАЗ РОБА» в §10 ниже. До ответов не реализуется.

# BRYK · Перестройка интерфейса

Дата: 2026-07-18 · файл: `/Users/robertvagin/Claude/Projects/bryk/index.html` (2929 строк) · канон: `BRYK_MASTER_PLAN_2026-07-18.md`, `BRYK_DELEGATE_ULTRA_2026-07-18.md`

---

## 1 · ЧТО ЛОМАЕТСЯ СЕЙЧАС

**Левая колонка не последовательность, а куча.** Порядок филдсетов сейчас: `Bodies` (316) → `Layers` (343) → `Formation` (350) → `Space` (360) → `Forces` (369). Тела стоят ПЕРЕД слоями, хотя тела принадлежат слою, который их ставит. Слои зажаты в середине

**Formation живёт отдельно от того, чем управляет.** `#fText` / `#fFont` / `list installed fonts` (351-353) это параметры слоя `word`, а лежат отдельным филдсетом. Отсюда нота 354 «Add a **Word** layer to build this text out of bodies», то есть панель извиняется за собственную раскладку

**Карточка не раскрывается в подробное взаимодействие.** `L.open` (2593) даёт плоский столбик скрабов и всё. Хуже: раскрытие висит на клике по имени (2539) и ТОГГЛИТ, значит повторный клик по активному слою его закрывает. EQJ на этом месте держит идемпотентность (`mvj-inject.html:653`), и держит её ровно ради переименования

**Объяснения съедают высоту, которая нужна контролам.** 9 штук `<p class="note">` в двух колонках, самая длинная 345-347 на три строки. Инфо-дока нет, легенды без иконок: `grep -c lucide index.html` = 0

**Стек эффектов не объявлен.** Три канваса `#camcv/#cv/#glcv` (388-392), в базовом правиле (63) z-index нет, порядок покраски = порядок DOM. При `body.camover` (71-72) `camcv:2`, `cv:1`, `glcv` остаётся `auto` и падает в z-0 полосу: **пост уходит под сцену**. Плюс `runPost` пишет `gl_FragColor=vec4(col,1.0)` (2108-2122) и берёт текстуру только с `cv` (2115), поэтому `chroma>0.001` стирает камеру целиком. Две ошибки гасят друг друга ровно в одном из четырёх состояний

**Цвет реализован как градиент.** `rebuildTextures` (990-1002) берёт цвет стопа напрямую, правильно, и следующей же строкой (996) крутит его по кругу OKLCH через `hueShift`. Дрейф (2712-2726) при дефолте `hueDrift:0.35` даёт ±12°, при 1.0 ±34°: навий 275.9° гуляет в 242-310, жёлтый 90.9° гуляет в 57-125. **Зелёный рождается здесь**, не в рампе. Отдельно: CSS-токены (22-24) живут на четвёртой палитре, `--red` фактически оранжевый, `--blue` фактически небесно-голубой

**Три бага семейства «фантомный ключ `rect`».** Проверено в текущем файле (18:09), поправка к исходнику: wedge контрол ИМЕЕТ (2324), исходник устарел
- 2326 дублирует 2323: второй `mixRow('wTri')` подмешивает ВТОРОЙ скраб в тот же `<span data-mnt="wTri">` и перетирает `SCRUBS.wTri`
- 2262 `syncAllScrubs` мапит `wRect: state.shapes.rect`, ключа нет (643: `{tri, wedge, circle}`), а `wWedge` в мапе отсутствует: wedge не пересинкается после вызова сцены
- 2739 итерирует `['rect','circle','tri']` для прогрева камерных текстур, тогда как `faceList` (977) работает по `['tri','wedge','circle']`: wedge-камера не прогревается

Решение Роба: панель сейчас, баги потом. Держим в срезе 8

---

## 2 · МОДЕЛЬ ИЗ EQJ

Источник: `/Users/robertvagin/Claude/Projects/music-viz/mvj-inject.html` (1116 строк). Комментарий в BRYK на 1528-1531 уже ссылается на `mvj-inject:548-688`, то есть перенос начат, но не закончен

### Уже стоит в BRYK, менять нельзя

| EQJ | BRYK | что это |
|---|---|---|
| `layers[]`, `layers[0]=TOP`, `addLayer` делает `unshift` (670-671) | 1533, 1555-1559 | массив, новый слой наверх |
| `activeId` + `activeLayer()`, `matrix` живой АЛИАС, не копия (653-655) | 1533-1535, `focusLayer` 1552-1554 | одна строка, из-за которой «любое аудио на любой визуальный вариант» вообще работает |
| `anySolo()` / `layerVisible(L)` (554-555) | 1537-1539, комментарий автора сохранён дословно: «solo FIRST and unconditional, EQJ :555» | единственный предикат видимости |
| `moveLayer` через swap (656) | 1573 | реордер |
| `.lrow` / `.l1` / `.l2` грамматика (628-634) | 2531-2570 | ряд карточки |
| mute/solo взаимоисключающие (645-650) | 2566-2573 | ставим одно, гасим другое |

### Переносим дословно, сейчас нет

1. **Идемпотентный фокус.** `setActive` (653) с ранним выходом и авторским комментарием: *«idempotent — re-clicking the active layer won't churn the comb, so a dblclick-rename on it survives»*. В BRYK `focusLayer` (1552) выхода не имеет, и клик по имени вдобавок тоглит `L.open`. Разводим: клик = фокус, раскрытие отдельным жестом
2. **Инлайн-переименование** через `nm.replaceWith(inp)`, Enter коммит / Escape отмена / blur коммит, `ev.stopPropagation()` на keydown (637-643). Сейчас в BRYK стоит системный `prompt()` (2541)
3. **`renameLayer` пробрасывает лейбл в три места** (658-659): строка, бейдж рэка, имя прокси
4. **Единый хвост на каждой ветке add:** `seed*Rows(L); matrix=L.matrix; renderLayers(); composite(); renderMatrix();` (670-687). В BRYK хвост `focusLayer(L.id); seedRows(L); renderLayers(); buildMatrix()` (1557), не хватает `composite()`, потому что композитора нет. Появится в срезе 5
5. **`allowExtend` + колбэк `extend`, который расширяет И дескриптор, И атрибуты подлежащего инпута** (508-520). В BRYK `mkScrub` (534-612) вычисляет `allowExtend` по умолчанию (538), но параметры карточки (2589-2592) и депт рэка не регистрируются в `SCRUBS` вообще, поэтому не пересинкаются
6. **Panel-is-base-editor.** Правка параметра в панели обязана писать сквозь в любую строку матрицы на том же пути: `_r.base=v; _r._v=v; _r._last=null;` плюс зеркалить скраб (510-520, инвариант заявлен на 1101). В BRYK `onInput:v=>p.set(v)` (2591) матрицу не трогает, значит драйв-петля перетрёт руку на следующем кадре. Это не косметика, это единственный шов между колонками
7. **Самоотчёт о неклассифицированном.** `window.__mvNeedsHuman` (544). Порт как `window.__brykNeedsHuman`
8. **Граница «движок против пресета».** EQJ держит её жёстко: `ROSTER` кнопка → `addLayer` (732), а «quick looks» дропдаун (947-949) и билдер-пилюли (536-541) правят АКТИВНЫЙ слой. Строка 947 прямой контрпример: `var L=synLayer(); if(!L){ addLayer('synthex'); L=synLayer(); } else setActive(L.id);`

### Флаг по фразе Роба

«По сути, любой выбор пресета это и есть новый слой» EQJ **опровергает** в узкой форме: выбор ДВИЖКА = новый слой, выбор ПРЕСЕТА = правка активного. В BRYK граница уже стоит правильно: кнопки из `PROGRAMS` делают `addLayer` (2463-2467), а варианты `PATTERNS` (17 штук, 1263-1266) меняют активный слой через `sel.onchange` (2561-2563). Оставляем как есть, но это расхождение с формулировкой, и его надо подтвердить (срез 2)

### Единственное место, где BRYK обязан отличаться

EQJ вынес слои ИЗ левой панели. Комментарий дословно (`mvj-inject:74` и `:723`):

> *«LAYERS · хлястики под панелью (Наряд 7·B · Роб): весь блок ВЫНЕСЕН из левой панели, тонкие адаптивные edge-tabs у правой кромки панели + hover-intent флайаут с полными контролами слоя. Место под реордер/удаление есть, сцена открыта.»*

Причина у EQJ: каждый слой там владеет своим канвасом или iframe, `composite()` (mvj:588-605) раздаёт им z / blend / opacity / display, а список слоёв съедал высоту, нужную параметрам

**BRYK этого скопировать не может: один общий пул тел** (решение Роба §9.1). Слой в BRYK не канвас, а провайдер целей: `PROGRAMS[prog].target(i,N,t,P,ctx)` (1370-1435). Все слои пишут в один `pool`, деля его через `L.w` (presence) и `L.share`. Тела рисуются одним проходом (2771-2790). Следствия, каждое надо назвать вслух:

- **`composite()` по слоям не существует и существовать не может.** z / blend / opacity per layer физически не к чему приложить
- **blend и opacity из карточки EQJ (630-633) на слои-программы НЕ переносятся.** Они переезжают на слои эффектов (§7), где канвасы реальные
- **BRYK-эквивалент уже написан:** presence `L.w` (1546, скраб 2583) и `share` (1546, ряд 2586). Это то же самое «сколько тебя в миксе», выраженное в раздаче тел, а не в альфе
- **Раз слой не съедает канвас, список слоёв остаётся В ЛЕВОЙ ПАНЕЛИ.** Прямое требование Роба, осознанное расхождение с EQJ 7·B. Hover-intent тайминги 280/180/30ms (733-746) не портируются
- **`layers[0]=TOP` меняет смысл.** В EQJ порядок массива задаёт z покраски. В BRYK он задаёт **приоритет раздачи тел**: кто первым забирает долю пула через `share`. Тултипы ▲▼ обязаны говорить «раньше в раздаче / позже», а не «выше / ниже», иначе оператор ждёт z-order и не получает его

---

## 3 · ЛЕВАЯ КОЛОНКА

Левая панель это контекст того, что мы строим. Последовательность: сначала кусочки, которые выбираем, потом настройки под ними

```
┌ BUILD ──────────────────────────────── 276px ┐
│  Build                              (ptitle) │
├──────────────────────────────────────────────┤
│ ⌁ MACRO                        [срез 7]      │
│   ENERGY   ▓▓▓▓▓░░░  0.62                    │
│   DENSITY  ▓▓▓░░░░░  0.38                    │
│   CHAOS    ▓░░░░░░░  0.12                    │
├──────────────────────────────────────────────┤
│ ⬔ LAYERS                                     │
│   [+ Axis][+ Waterfall][+ Pattern][+ Grid]   │  ← пресет-полка, из PROGRAMS (2463)
│   [+ Ring][+ Tunnel][+ Scatter][+ Logo]      │     выбор = новый слой
│   [+ Word]                                   │
│                                              │
│   ┌ 1 · Word · formation      ▲ ▼ ⧉ ✕   ▾ ┐ │  ← РАСКРЫТА (активная)
│   │ [Cloud ▾]   presence ▓▓▓▓▓▓░  hide solo│ │
│   │ ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄ │ │
│   │  share        ▓▓▓▓░░░░  0.50           │ │
│   │  BODIES  △ ▓▓▓  ◣ ▓░░  ● ▓▓▓  ⬚ ░░░   │ │  ← марки ВНУТРЬ (ответ 1)
│   │          asset tint ▓▓░   [RANDOMISE]  │ │
│   │  FORM    scale ▓▓▓  jitter ▓░  fit ▓▓▓ │ │  ← PROGRAMS[prog].params
│   │  TEXT    [BRYK        ]  [Inter    ▾]  │ │  ← Formation ВНУТРЬ (ответ 2)
│   │          [list installed fonts]        │ │
│   │  ▸ 3 mappings                          │ │  ← фокусит рэк справа
│   └────────────────────────────────────────┘ │
│   ┌ 2 · Axis · lanes         ▲ ▼ ⧉ ✕   ▸ ┐ │  ← СВЁРНУТА
│   │ Axis · lanes  presence ▓▓▓▓▓▓▓░ hide solo│
│   └────────────────────────────────────────┘ │
│                                              │
│   ⌄ POOL                       общее на все  │  ← подсекция, свёрнута по умолчанию
│     Count ▓▓▓  Size ▓▓  Picture radius ▓░    │
│     Outline only ○  Face heading ▓▓          │
│     Size variation ▓▓  Tilt variation ▓░     │
│     [ADD FILES] [CLEAR]     12 assets        │
├──────────────────────────────────────────────┤
│ ⚛ FORCES                       общее на пул  │
│   Behaviour ▾   Manner ▾   Physics on ●      │
│   Interact in 3D ○  Follow ▓▓  Damping ▓▓▓   │
│   Collide ▓  Body radius ▓  Swirl ▓  ...     │
│   Logo as void ▓░░                           │  ← переезд из Formation
├──────────────────────────────────────────────┤
│ ⬢ SPACE                        камера, общая │
│   Perspective ▓▓  Zoom ▓▓▓  Tilt ▓  Spin ▓   │
├──────────────────────────────────────────────┤
│ ✧ EFFECTS                          [срез 5]  │
│   [+ grain][+ scanlines][+ bloom][+ chroma]  │
│   ┌ chroma   ⇈POST  op ▓▓░  blend ▾   ✕  ┐  │
│   ┌ grain           op ▓▓▓  blend ▾ ▲▼ ✕ ┐  │
├──────────────────────────────────────────────┤
│ ⓘ  Info                               (foot) │  ← прибит, §6
└──────────────────────────────────────────────┘
```

**Почему такой порядок.** MACRO наверху, потому что три ручки на весь сет (мастер-план §17.1). LAYERS сразу под ним, это «кусочки, которые мы выбираем». Всё, что описывает КАКИЕ тела и КАК они расставлены, живёт внутри карточки. POOL подсекцией внизу LAYERS, потому что это свойства пула как материала (сколько, какого калибра, насколько разнобойные), а не свойства раскладки. FORCES и SPACE ниже: общее, трогается реже. EFFECTS отдельной зоной, у неё своя грамматика карточки

**Что распускается.** Филдсет `Bodies` (316-341) перестаёт существовать как филдсет. Веса марок `△ ◣ ●` + assets + asset tint + RANDOMISE уходят в карточку. Count / Size / Picture radius / Outline only / Face heading / Size variation / Tilt variation / ADD FILES / CLEAR остаются глобальными и садятся в POOL

**Что распускается вторым.** Филдсет `Formation` (350-359): `#fText` / `#fFont` / `fFonts` в карточку `word`, `Logo size` в карточку `logo`, **`Logo as void` уезжает в FORCES**, потому что это правило потока для всего пула (`avoidLogo`, 1505-1526 работает позиционной коррекцией после интегрирования), а не свойство слоя. Флаг Робу, срез 2

---

## 4 · ПРАВАЯ КОЛОНКА

Роб: «Правая колонка почти как нужно, инпуты в целом нормальные». Структуру не трогаем

**Порядок остаётся сигнальный** и он уже написан в комментарии 393-395: `source → clock → beat → meters → MOD RACK → moves → look → scenes`. Это грамматика EQJ (`mvj-inject:712-720`), где музыкальная панель первый ребёнок `#rightcol`

**Меняется четыре вещи, все мелкие:**

1. `legend[data-toggle]` (157-159, проводка 2512-2520) единственный collapsible в файле и единственный не-Lucide глиф (`▾` в `::before`). Приводим к грамматике Synthex: `<button class="sphead">` с шевроном через CSS `mask` из Lucide `chevron-down` / `chevron-right` (`Synthex Engine — v1.html:130-160`). Заодно `.sidep>.spbody.sopen` анимация подъезда снизу с `void body.offsetWidth` и `scrollIntoView({block:'nearest'})` (Synthex 5397-5408)
2. Бейдж `#rackLayer` (452, пишется 2598) уже показывает активный слой. Добавляем клик: скроллит левую панель к карточке этого слоя. Единственное новое поведение справа
3. Ноты правой колонки в `DESC`: 455-456 «feature ▸ parameter ▸ depth…» и 429 `armNote` / 437 `camNote` (эти два остаются как СТАТУС, у них динамические строки на 1975-1977 и 2423-2425)
4. Токены `--yellow` / `--red` / `--blue` (22-24) на брендовую тройку, §9. `--accent-live` = navy (26), `--accent-beat` = yellow, `--accent-warn` = red остаются как есть по ролям

**Почему разрез именно такой.** Формально это два разных вопроса к одному параметру: левая отвечает «какое БАЗОВОЕ значение», правая отвечает «что его двигает и насколько». Один параметр может иметь базу слева и модуляцию справа одновременно, и они не должны драться. Это единственное место, где колонки связаны кодом, и связь надо написать явно (§2, пункт 6), иначе драйв-петля перетирает руку на следующем кадре

Иерархия авторов при конфликте, из мастер-плана §17.1 дословно: **рука > приём > рэк > сцена**. Рука перебазирует, приём возвращает заимствованное в `done()`, рэк модулирует вокруг базы, сцена меняет все базы разом

---

## 5 · КАРТОЧКА СЛОЯ

### Свёрнутая

```
.lrow[.act][.mut][.open]
├ .l1    [1] · Word · formation           ▲ ▼ ⧉ ✕    ▸
└ .l2    [Cloud ▾]     presence ▓▓▓▓▓▓░    hide  solo
```

| элемент | что делает | код |
|---|---|---|
| `.ln` номер | позиция в РАЗДАЧЕ тел, не z | 2535 |
| `.lname` клик | `focusLayer(L.id)` + раскрыть. Никогда не закрывает | 2539, перепишется |
| `.lname` двойной клик | инлайн-переименование `replaceWith(input)`, Enter / Escape / blur | EQJ 637-643, заменяет `prompt()` 2541 |
| `▲ ▼` | `moveLayer(id,±1)` (1573). Тултип: «раздать тела раньше» / «позже» | 2543-2545 |
| `⧉` | `duplicateLayer` (1566): новый seed, копия `params` и `matrix`. **В EQJ этого нет вообще**, наш net-new, оставляем | 2546 |
| `✕` | `removeLayer` (1560), фокус переезжает на `layers[0]` | 2547 |
| `▸ / ▾` | раскрытие. Единственное, что закрывает карточку | новое |
| variant `<select>` | только у программ с `variants` (сегодня один `pattern`, 17 вариантов). У остальных статичный `.lprog` | 2556-2563 |
| presence | `mkScrub` 0..1, регистрируется как `SCRUBS['w_'+L.id]` | 2583-2584 |
| `hide` / `solo` | взаимоисключающие, `layerVisible` (1539) единственный предикат | 2566-2573 |

### Раскрытая, в порядке

1. **`share`** (0..1). Первый, потому что отвечает «сколько тел мои» и определяет всё, что ниже. Нота 345-347 объясняет его так: *«share splits the pool so two forms stay legible instead of averaging into one»*. Эта фраза становится строкой `DESC`
2. **BODIES** (переезд, ответ Роба 1). Веса `△ tri` / `◣ wedge` / `● circle` / `⬚ assets`, `asset tint`, кнопка `RANDOMISE`. Пишет в **`L.shapes`**, новое поле в `makeLayer` (1542-1550)
3. **FORM.** Объявленные параметры программы: `PROGRAMS[L.prog].params` (1370-1435). Для `axis` и `waterfall` это `AXIS_PARAMS` (1348-1357, 8 рядов), для `pattern` это `PAT_PARAMS` (1361-1368, 6 рядов), у остальных инлайн-списки по 2-3 ряда. Ряды генерируются из деклараций, как сейчас (2586-2593). Ноль самодельных сеток
4. **TEXT / MARK.** Только у `word` и `logo`. `#fText`, `#fFont`, `list installed fonts` в карточку Word; `Logo size` в карточку Logo
5. **`▸ N mappings`.** Счётчик строк рэка этого слоя плюс кнопка, которая фокусит рэк справа. Не список: список живёт справа, `matrix` там же

### Взаимодействие

- **Открывает:** клик по имени (фокус + раскрыть) или клик по счётчику mappings. Закрывает только шеврон или фокус на другой карточке
- **Фокус:** `focusLayer(id)` (1552) перенацеливает `matrix` на `L.matrix`, `buildMatrix()` (2225) перерисовывает рэк, `#rackLayer` (2598) пишет имя. Ровно две строки EQJ (`:654`), они уже есть
- **Идемпотентность:** `focusLayer` получает ранний выход `if(id===activeId && activeLayer()) return;` (EQJ `:653`). Без него переименование не переживает перерисовку
- **Раскрыт один слой.** Фокус раскрывает активный и схлопывает остальные. EQJ per-card collapse не имеет вообще (сворачивание на уровне контейнера), но у BRYK панель одна и высота конечная. Это наше отличие, флаг Робу

### Стартовый пресет на буте

Ответ Роба 3: **Axis + Word**. Сейчас бут ставит один `axis` (2900-2902) с настроенными параметрами и комментарием, почему не два (*«two at full weight average into a band»*). Комментарий останется верен только если у слоёв разные `share`. Порядок вызовов задаёт, кто раздаёт тела раньше: `addLayer` делает `unshift`, значит последний вызванный окажется первым. Если Axis должен раздавать первым, `addLayer('word')` вызывается ДО `addLayer('axis')`

---

## 6 · ИНФО-СНОСКА И ИКОНКИ

Источник: `/Users/robertvagin/Claude/Projects/synthex-engine/engine/Synthex Engine — v1.html` (5917 строк)

### Док

**Разметка** (Synthex 438-452): `<div id="foot">` последним ребёнком `#panel`, панель `display:flex;flex-direction:column`, `.pbody` скроллится, `#foot` прибит. Внутри `<button id="helpBtn">` (Lucide `circle-help`, 13×13, stroke 2), `<div id="footText" aria-live="polite">Info</div>`, `<div id="helpPopover" role="dialog" hidden>`

**CSS** (Synthex 177-201): `flex:none`, `border-top:1px solid var(--line)`, `padding:8px 14px`, `gap:8px`, текст `var(--muted)` 10px/14px гротеском (намеренно не моно панели), `-webkit-line-clamp:2`, круглая 24px кнопка слева. Дефолтная строка покоя буквально слово `Info`

**Высота.** В Synthex ровно 49px, потому что совпадает с высотой таймлайн-бара, ноль перепада. У BRYK таймлайна нет; чему равна константа, **не найдено в исходнике**. Замерить `#corner` (267) и зафиксировать одним числом

**Сеттер** (Synthex 3703-3704):
```js
const setFoot=txt=>{ const f=document.getElementById('footText'); if(f)f.textContent=txt; };
const footDefault=()=>setFoot('Info');
```

**Проводка, весь механизм** (Synthex 4296-4303): два делегированных слушателя на контейнер, `mouseover` + `focusin` → `show`, `mouseleave` + `focusout` → `footDefault`. Цепочка фолбэка: `[data-desc]` → ближайшая `.seg button` → `title` → `textContent`. Клавиатура ведёт себя ровно как мышь, и это плюс `aria-live="polite"` вся история доступности целиком. У BRYK **две** скроллящиеся колонки, обе кормят один док: `['#panel .pbody','#rightcol .pbody']`

**Попап шорткатов** (Synthex 4304-4308): `helpBtn` тоглит `hidden`, `pointerdown` вне `#foot` закрывает, Escape закрывает и возвращает фокус на кнопку. Содержимое = хоткеи BRYK (`T / [ / ] / H / B / P / C / 1-4 / 5-8 / W / F`, подсказки 501-505, проводка 2641-2661) плюс жесты сцены (нота 366 переезжает сюда целиком)

### Где живут строки

Один плоский `const DESC={}` по ключу пути параметра (Synthex 3550ff). Ключ: `data-mnt` слот для глобальных (`count`, `size`, `persp`, `damp`, `swirl`) и `prog.paramKey` для параметров программ (`axis.gap`, `word.scale`, `pattern.dance`)

Трёхуровневый фолбэк на фабрике ряда (Synthex 3775-3792): **`def[5] || DESC[k] || label`**. Ни один контрол не остаётся без описания, даже новый

Стиль всех ~150 записей Synthex: одно предложение, без точки в конце, строчная после двоеточия, говорит что делает значение и часто что значит 0. Единицы («in px», «in degrees») там, где они не очевидны

Не-схемные ряды (карточка, кнопки, селекты, счётчик mappings) ставят `dataset.desc` литералом

### Ноты: что умирает, что остаётся

| строка | нота | судьба |
|---|---|---|
| 318-319 | «Weights, not a choice — every face above zero is in the mix…» | **DESC** на `△ ◣ ●` внутри карточки |
| 332 `assetNote` | «No assets — primitives only.» | **остаётся СТАТУСОМ** (динамика 2336-2338) |
| 345-347 | «A layer is a way of placing bodies, with a PRESENCE weight. share splits the pool…» | **DESC** на пресет-полке и на `share` |
| 354 `fNote` | «Add a Word layer to build this text out of bodies.» | **умирает**: поле текста теперь ВНУТРИ карточки Word, объяснять нечего. Динамические строки про Local Font Access (2621, 2626, 2627) остаются статусом внутри карточки |
| 357-358 | «Add a Logo layer to gather bodies into the mark…» | **DESC** на `Logo as void` в FORCES |
| 366 | «Drag the stage to orbit, wheel to zoom, double-click to reset.» | **в попап**, секция Scene |
| 429 `armNote` | «A gesture is required before the browser will start an AudioContext.» | **остаётся СТАТУСОМ** (динамика 1975-1977) |
| 437 `camNote` | «No camera. Shapes fall back to palette fills…» | **остаётся СТАТУСОМ** (динамика 2423-2425) |
| 455-456 | «feature ▸ parameter ▸ depth ▸ curve. Rows belong to the focused layer…» | **DESC** на легенде рэка и на `+ mapping` |

Итого: 4 гасятся полностью, 3 остаются статусом, 2 уезжают в попап и DESC. `.note` сохраняется как класс только под статус и ассеты. Ровно тот же расклад, что в Synthex (299): *«.note используется только для status/asset messages, never for control explanation»*

Плюс сегодня в BRYK 47 `title=""` статикой и 9 динамических. Статические на лейблах переезжают в `DESC` (они и есть описания), динамические остаются `title` (они меняют состояние: 1996/2002 подмена на «ARM a live input first», 2194 слоты сцен, 2635 показать/скрыть интерфейс). Один `title` под нож: 2588 «open-layer param label echoes the param label», чистый шум

### Иконки

Механизм 1:1 (Synthex 5853-5888): один map `P={}` инлайн-путей, ключ = **первое слово легенды** в нижнем регистре, `wrap()` фиксирует `viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"`, одноразовый проход на буте с гардом `if(l.querySelector('svg'))return;`

CSS одной строкой (Synthex 342): `legend svg{margin-right:6px;vertical-align:-2px;flex:none}`

Ловушка, записать комментарием: любое динамическое обновление легенды через `textContent` **стирает иконку** (Synthex 4106 логирует ровно этот случай)

Path-data берётся 1:1 из локального пака `~/.claude/skills/icon-canon` (Lucide 1995 глифов), не рисуется руками. `stroke-width:1.8` спецификация именно легенд, `2` для отдельно стоящих кнопочных глифов

| легенда BRYK | ключ | Lucide |
|---|---|---|
| Macro | `macro` | `sliders-horizontal` |
| Layers | `layers` | `layers` |
| Pool | `pool` | `shapes` |
| Forces | `forces` | `atom` |
| Space | `space` | `box` |
| Effects | `effects` | `sparkles` |
| Sound | `sound` | `audio-lines` |
| Camera · video | `camera` | `video` |
| Mod rack | `mod` | `waypoints` |
| Moves | `moves` | `wand-sparkles` |
| Look | `look` | `palette` |
| Scenes | `scenes` | `save` |

---

## 7 · СТЕК ЭФФЕКТОВ

### Ловушка специфичности, которую надо снять первой

Канон: мастер-план §14.4, комментарий в файле 64-66. Подтверждено живым репро в Chromium: `#stage canvas` = (0,1,1), голый `#someLayer` = (0,1,0). Любое будущее правило по id молча проигрывает, и провал невидим, потому что инлайн-стили рендер-петли (2110, 2111, 2121, 2751, 2752) его заклеивают. Текущий код лечит симптом поднятием специфичности (`#stage #glcv`), мина остаётся взведённой для каждого следующего слоя

### CSS перестаёт называть слои

Заменяет 62-72 целиком:

```css
#stage{position:fixed;top:0;left:var(--panel-w);right:var(--panel-w);bottom:0;
  overflow:hidden;isolation:isolate}          /* блендинг заперт внутри сцены */

/* ОДНО правило на все слои. Ни один слой больше не назван в CSS */
#stage > .lyr{
  position:absolute;inset:0;width:100%;height:100%;display:block;
  z-index:var(--z,0);
  opacity:var(--op,1);
  mix-blend-mode:var(--bm,normal);
  pointer-events:none;
}
#stage > .lyr[hidden]{display:none}           /* (0,3,0) бьёт (0,2,0). Декларативное off */
```

CSS читает переменные, JS владеет значениями. Конкурс специфичности невозможен, потому что селектор один

### Тиры и пин шейдеров

```js
const TIER = { BG:0, SCENE:100, OVER:200, POST:300 };   /* 100 слотов на тир */
const STACK = [];

/* ПИН, в обе стороны: не-шейдер не может войти в POST,
   и ни один не-POST слой не получает z, дотягивающийся до POST */
if (L.tier === 'POST' && L.kind !== 'gl') L.tier = 'OVER';
L.order = Math.min(99, Math.max(0, L.order|0));
```

Роб: «Шейдеры только верхними слоями, дать приоритет, чтобы не становились под объекты». Это ровно тот пин, и это **дыра, которую EQJ/MUSICVIZ оставил открытой**: там пина нет вообще (проверено на `pin`, `alwaysTop`, `forceTop`, `layers.sort`), `fx:bloom` можно закопать кнопкой ▼ под сцену, и `backdrop-filter` тихо начинает фильтровать только то, что случайно оказалось ниже. На пиннутых строках ▼ дизейблится

### Один массив на порядок отрисовки и порядок покраски

```js
for (const L of STACK) if (L.on && L.draw) L.draw(L.ctx, cv.width, cv.height, f);
```

Сегодня порядок вызовов написан руками (`camTick` 2737, геометрия 2766+, `runPost` 2883), а порядок покраски = порядок DOM. Два независимых списка, совпадающих ровно пока кто-то их синхронит. Один сортированный массив снимает весь класс

### POST это ОДИН канвас, не N

Браузер режет живые WebGL-контексты (Chrome ~16, старший умирает молча). **N контекстов запрещены категорически.** Один слой `kind:'gl'`, один контекст, упорядоченная цепь проходов на двух ping-pong FBO, регистрация `registerShader({id, order, frag, uniforms})`

Проход 0 грузит источник (`texImage2D` с канваса сцены, ровно текущий паттерн 2115), каждый следующий читает предыдущий FBO. **Это и есть «наслаивать, чтобы взаимодействовали»:** проход N буквально сэмплит выход N-1, а не просто ложится сверху блендом

Две вещи чинятся заодно:
- фрагмент обязан отдавать настоящую альфу `vec4(col, a)`, контекст `{alpha:true, premultipliedAlpha:false}`, `gl.blendFunc(gl.ONE, gl.ONE_MINUS_SRC_ALPHA)`. Текущий `vec4(col,1.0)` (2108-2122) и есть тот занавес, что стирает камеру
- пост сегодня сэмплит только `cv`. Для N слоёв сцены нужен flatten-шаг

### Прозрачность и наслаивание

Роб: «уметь делать прозрачными либо наслаивать, чтобы взаимодействовали». Три механизма, по слоям:

1. **Элемент:** `.lyr` прозрачен по умолчанию
2. **Кадр:** гасить предыдущий кадр через `destination-out`, а не заливкой фона. Дословно из MUSICVIZ (3242): `ctx.globalCompositeOperation='destination-out'; ctx.fillStyle='rgba(0,0,0,'+clamp((1-opts.trail)*0.5,0.015,1)+')'` с комментарием *«fade prev frame → light trails, bg stays transparent»*. Тот же приём в движке Synthex (1466-1470). **Без этого стек бессмыслен**, каждый слой перекрывает нижние
3. **Слой:** `--op` и `--bm` из карточки эффекта

Blend полярно-зависим (MUSICVIZ 3653-3654): новый слой по умолчанию `screen` на тёмной сцене, `multiply` на светлой; смена полярности переписывает blend всем слоям по словарям `LIGHTER={screen,plus-lighter,lighten,color-dodge}` и `DARKER={multiply,darken,color-burn}`. Иначе `screen` на белом всё выбеливает

### Потолок производительности

1440×800 CSS при `DPR = min(2, devicePixelRatio)` (2667): каждый слой 2880×1600 = 4.6 Mpx ≈ **18.4 МБ GPU-текстуры**. `camcv` и `glcv` уже матчат `cv.width` (2742, 2112), то есть полное разрешение без исключений

| что | потолок | почему |
|---|---|---|
| 2D-слои `blend:normal` | 8-12 комфортно | композитор стекает квады на GPU, цена в памяти, не в кадре |
| 2D-слои с не-normal blend | **4-6** при DPR 2 | каждый сбивает композитор с быстрого пути в групповой проход; `isolation:isolate` держит группу ограниченной, и это важно |
| GL-проходы с загрузкой 2D-канваса | **одна полноразмерная загрузка на кадр** | 18 МБ CPU→GPU за вызов, ~1.1 ГБ/с при 60 fps. Две на грани, три складываются. Это уже самая дорогая строка в `runPost` |
| N WebGL-контекстов | никогда | молчаливая потеря контекста, худший из возможных отказов |

Поэтому цепь = один контекст с FBO: проход FBO→FBO стоит только fill rate, таких 6-10 за цену одной лишней загрузки

**Митигации в конструкцию сразу, по отдаче:**
1. цепь POST на 0.6-0.75 масштаба с апскейлом. Chroma, bloom, grain родного разрешения не требуют, это ~2× на каждом проходе
2. авто-клампинг DPR до 1.5 когда включён flatten или цепь длиннее 2
3. dirty-флаг на загрузку: пропускать `texImage2D` если источник не менялся в этом кадре
4. потолок DOM-стека ~8 канвасов, всё сверх этого в шейдерную цепь, она масштабируется много лучше композиторных слоёв

**Честная развилка.** Результат DOM `mix-blend-mode` **никогда** не читается обратно в текстуру. Шейдеру, которому нужен смешанный выход, требуется flatten-проход: рисовать 2D-слои в один офскрин в порядке стека через `globalCompositeOperation`, маппленный из blend каждого слоя, и грузить уже его. Это архитектурная вилка, не оптимизация. Запускать лениво, только когда шейдер объявил, что ему нужен полный композит

### Карточка эффекта

Та же грамматика, что карточка слоя, плюс то, чего у слоёв-программ нет и быть не может (§2): `id · label`, скраб `op`, `blend ▾` из словаря BLENDS (9 режимов, короткие имена `BLEND_SHORT`, MUSICVIZ 3436-3437), `hide` / `solo`, индикатор `⇈POST` (нередактируемый, читается из `tier`), `▲▼` (▼ дизейблен на пиннутых), `✕`

### Что переезжает

Существующие `#camcv` / `#cv` / `#glcv` (388-392) регистрируются как `BG` / `SCENE` / `POST`. `body.camover` (71-72, проводка 2449, 2184) снимается и становится `order` камерного слоя. `runPost` (2059-2122) переезжает в POST-тир одним `kind:'gl'` слоем

---

## 8 · APPLE-DESIGN

Роб: «Полностью все рекомендации использовать нельзя. Ломать наш input нельзя»

### Закон ввода, не подлежит обсуждению

`mkScrub` (534-612), `resist` (524-528), `mount` (613), `mountToggle` (620)

1. **Тягучка.** `pointerdown` захватывает `{x, v, w, span, mn, mx}` + `setPointerCapture`. Мёртвая зона 3px (`Math.abs(dx)>3`). `Shift` = чувствительность 0.25
2. **Ввод с клавиатуры.** Клик короче 3px вызывает `enterEdit()`, то есть **это тот же жест, что и драг**. Enter или Space на самом скрабе тоже открывает поле. Стрелки: `±step*(shift?10:1)`, коммитят немедленно
3. **Значение выходит за диапазон.** `allowExtend` вычисляется, не пишется руками: `if(cfg.allowExtend==null) cfg.allowExtend = !(cfg.min===0 && cfg.max===1 && cfg.step===1);` (538). Ручная простановка это ровно то, из-за чего 14 контролов, включая PRESENCE, остались без неё. `min`/`max` мутируются навсегда (561, 604-605), `aria-valuemin/max` переотдаются на каждом `paint()` (558). Единственное исключение сигнатура тумблера `(0,1,1)`

Ни одна рекомендация apple-design этого не отменяет

### Таблица

| § | Рекомендация | Вердикт | Правило имплементеру |
|---|---|---|---|
| 1 | отклик на pointer-down, не на release | **ADOPT** | уже в коде, `:active` даёт `scale(.97)`; ноль debounce на пути ввода |
| 1 | непрерывный фидбэк во время жеста | **ADOPT** | `setV(q,false)` на каждом move; `onCommit` только на отпускании |
| 2 | 1:1 tracking + `setPointerCapture` + grab offset | **ADOPT** | есть; значение считается от точки захвата, не от центра поля |
| 2 | история позиций под velocity | **REJECT** | инерции у числа быть не должно; не копить, не хранить |
| 3 | interruptibility | **ADAPT** | значение не анимируется вообще, перехватывать нечего. Запрет жёсткий: `pointer-events` не блокируется нигде кроме `disabled` |
| 3 | анимировать от presentation-значения | **ADOPT узко** | только `.slive` и хром |
| 3 | без CSS-transition на хватаемом | **ADAPT** | на треке скраба transition нет и не будет; `.slive` держит `transition:left` под `prefers-reduced-motion:no-preference` |
| 4 | springs вместо fixed-duration | **ADAPT** | пружины **только на не-числовых**: лампа бита (attack .55 / release .12), раскрытие карточки, угловой тулбар |
| 4 | `damping 1.0` дефолт, bounce ~0.8 после броска | **ADOPT** | критическое демпфирование на всю панельную моушн. Bounce нигде не заслужен, броска в панели нет |
| 5 | velocity handoff на релизе | **REJECT** | число обязано остановиться там, где остановился палец |
| 6 | momentum projection | **REJECT** | флик «бросил бы» значение и молча вызвал `extendParamRange` на диапазон, которого никто не просил |
| 7 | симметричные пути входа/выхода, `transform-origin` от триггера | **ADOPT** | карточка раскрывается из своей строки и уходит тем же путём |
| 8 | промежуточные кадры телеграфируют исход | **ADAPT** | применимо к `.sfill` (она и есть телеграф значения) и к раскрытию. Отдельной «подсказки направления» не строим |
| 9 | rubber-band на границе | **ADAPT** | компромисс уже собран, коллизия 2 |
| 10 | гистерезис ~10px до коммита | **ADAPT** | у нас 3px: скраб плотный, 10px съедали бы полезный ход. Порог различает click→type и drag, а не выбирает ось |
| 10 | параллельное распознавание жестов | **N/A** | один жест на контрол |
| 11 | только `transform`/`opacity`, rAF | **ADOPT** | метры на `scaleX`, не на `width`. Новые кадровые индикаторы только на transform |
| 11 | `will-change` под предстоящее движение | **ADAPT** | точечно и только на живущих. Глобальный на панели над 60fps-канвасом отнимает память GPU у сцены |
| 11 | motion blur / stretch | **REJECT** | инструментальная панель. Смаз на цифре читается как лаг |
| 12 | translucency / `backdrop-filter` | **REJECT по умолчанию** | коллизия 3 |
| 12 | вес материала кодирует иерархию | **ADAPT** | непрозрачные слои: `--panel` фон, `box-shadow: inset 0 -1px var(--line)` разделители, `--muted` против `--ink`. Тот же смысл без цены |
| 12 | никогда не стекло на стекле | **N/A** | стекла нет |
| 12 | scroll edge effect вместо 1px границы | **REJECT** | ещё один composite-слой поверх канваса. 1px граница токеном честнее |
| 12 | vibrancy | **N/A** | следствие отменённого стекла |
| 13 | каузальность / гармония / полезность фидбэка | **ADAPT** | гармонию держим (визуал в том же кадре, что событие). Haptics на десктопе нет. **Звук как фидбэк запрещён: звук здесь вход, а не выход** |
| 14 | `prefers-reduced-motion` | **ADOPT** | гасит всю панельную анимацию. Скраб продолжает работать 1:1, потому что 1:1 это не анимация |
| 14 | `prefers-reduced-transparency` | **N/A** | панель уже непрозрачна |
| 14 | `prefers-contrast: more` | **ADOPT** | усиливает разделители и рамку focus-visible |
| 15 | optical sizing, трекинг под кегль | **ADAPT** | коллизия 4. Трекинг по РОЛИ, не по кеглю: legend .01em, кнопки .02em, лейблы .01em, числа ровно 0 |
| 15 | leading обратно размеру | **ADOPT** | 1.3 на строках, 1.5 на служебных подписях. Ноль фиксированных высот на текстовых контейнерах |
| 15 | уважать пользовательский размер, spacing в rem/em | **ADAPT** | кегль панели не масштабируем (инструмент фиксированной плотности), но **WCAG 1.4.12 обязан выживать**: никаких `height:` на элементах с текстом |
| 15 | системный шрифт по умолчанию | **ADAPT** | `ui-monospace` для чисел (обязателен, `tabular-nums` держит колонку), гротеск для лейблов |
| 16 | восемь принципов как словарь ревью | **ADOPT** | Agency и Familiarity прямо обслуживают закон ввода: право выйти за задуманный диапазон |
| 16 | wayfinding, из ловушки есть выход | **ADOPT** | при скрытом хроме угловая кнопка остаётся видимой на 18%, поднимается на hover/focus (285-286) |
| 16 | grouping & mapping, контрол рядом с тем, чем управляет | **ADOPT** | совпадает с §3: марки внутри карточки, не в глобальном филдсете |
| 16 | confirmation только на разрушительном | **ADOPT** | `extendParamRange` подтверждения не требует, расширение обратимо |
| 17 | интерактивный прототип вместо статики | **ADOPT** | гейт `node audio-core.test.js && node probe/panel.cjs` перед сдачей |

### Четыре коллизии, разобранные

**1 · Пружина на скрабе: нет.** Скраб не «хватаемый объект», он прямой ввод числа. Пружина ломает закон в трёх точках:
- **точность:** пружина оседает асимптотически с порогом остановки. Введённое `137.5` доедет до `137.4993` и встанет по порогу. Печатное значение обязано попасть в движок байт-в-байт и мгновенно, `setV(nv,true)` без промежуточного состояния
- **мутация диапазона:** овершут за границей вызовет `extendParamRange` на значении, которого никто не вводил. `min`/`max` двигает только явное намерение, никогда физика
- **рассинхрон:** пружина вставляет лаг между пальцем и цифрой, и заливка, число и то, что слышит движок, расходятся. Закон 1:1 из §2 самого apple-design это запрещает

Правило: **value-путь без анимации вообще**

**2 · Rubber-band против W-ROB-20.** Компромисс уже в коде (524-528), трогать не надо:
```js
const RESIST_PX = 26;
function resist(over){ const c = 0.55;
  const soft = (over*RESIST_PX*c)/(RESIST_PX + c*over);  // асимптота Apple
  const t    = Math.min(1, over/(RESIST_PX*2));          // 0→1 на двойной зоне
  return soft*(1-t) + over*t; }                          // и отпускаем в линейность
```
Первые ~26px за краем сопротивляются (край ощущается), к 52px кривая возвращается к 1:1 (край не держит). Непрерывно и по значению, и по производной. Три границы применения: **только на drag** (печать за диапазон расширяет мгновенно, без сопротивления), **только при `cfg.allowExtend`** (тумблер `(0,1,1)` клампится жёстко), **никогда не откатывает** значение назад после отпускания

**3 · `backdrop-filter`.** Запрещён по бюджету кадра, не по вкусу: панель висит над канвасом на 60fps, блюр по её площади композитится каждый кадр. Условие снятия бана: замер p95 frame-time на боевом ноуте, панель открыта, сцена под нагрузкой, до и после. Регрессия больше 1ms = бан остаётся. Замена, дающая ту же иерархию: непрозрачный `--panel`, `inset 0 -1px` разделители, разведение `--ink` / `--muted`

**4 · Типографика.** Шкала 9/10/11 mono фиксирована: плотный инструмент, `tabular-nums` держит колонку чисел, плавающий кегль ломает и сетку, и выравнивание. Из apple-design берём не масштабирование, а два других требования: **трекинг по роли** и **устойчивость к пользовательскому переопределению интервалов** (WCAG 1.4.12). Второе важнее: фиксированные высоты на текстовых контейнерах запрещены, это тот самый дефект `legend{height:14px}` из Synthex, который в BRYK уже снят (79-81) и подлежит ретрофиту обратно в движок

### Инварианты, которые обязан ловить гейт `probe/panel.cjs`

1. ноль `transition` / `animation` на `.scrub`, `.sfill`, `.sval`
2. `resist()` вызывается только внутри ветки `cfg.allowExtend`; ввод с клавиатуры её не проходит
3. ноль `backdrop-filter` в файле
4. ноль `height:` на элементах, содержащих текст (legend, label, `.sval`)
5. `letter-spacing` на `.sval` не задан или равен 0
6. каждый `.scrub` несёт `.sval` / `.sedit`, ноль голых `input[type=range]`. Сегодня один есть: позиция цветового стопа (741)

---

## 9 · ЦВЕТ

Роб: «дифференциация цвета реализована как будто градиент, нужно усреднить везде, использовать цвета из исходника, но жёлтый подвести в более тёплый, синий в более digital звонкий»

Исходник подтверждён из SVG (`assets/mark-{circle,tri,wedge}.svg`): circle `#ce1c22`, tri `#f6c60a`, wedge `#2d2f83`. В коде они лежат верно в `BRAND` (810-814) и в `state.stops` (658)

### Закон

**Рампа допустима только там, где сама среда непрерывна** (фотография, карта яркости, ось данных). Везде, где цвет несёт ТЕЛО, цвет один из трёх, выбранный, а не смешанный

### Где градиент противоречит закону

| строка | что происходит | вердикт |
|---|---|---|
| **996** `rebuildTextures` | тело берёт цвет стопа напрямую (990-1002, верно), и следующей строкой `lch=[lch[0],lch[1],lch[2]+hueShift]` крутит его по кругу | **это и есть «как будто градиент». Снять hueShift здесь** |
| **719** `buildRamp` | тот же `hueShift` внутри рампы | снять |
| **2712-2726** драйвер дрейфа | `wantShift = state.hueDrift*(centroid-0.5)*1.2`. При дефолте 0.35 размах ±0.21 рад (±12°), при 1.0 ±0.6 рад (±34°). Навий 275.9° гуляет в 242-310, жёлтый 90.9° гуляет в 57-125. **Зелёный, который канон запрещает, рождается здесь**, не в рампе | заменить, см. ниже |
| **757-767** `#addStop` | новый стоп сэмплится ИЗ интерполянта: `const rgb=RAMP_RGB[Math.round(p*255)]`. Одним кликом промежуточный цвет становится полноценным цветом тела | брать из набора, не из рампы |
| **691-700** `mixLch`, **711-725** `buildRamp` | интерполятор и 256 сэмплов | оставить, кормят легальных потребителей |
| **929-939** camera duotone | непрерывный маппинг яркости через multiply+screen; побочно берёт `st[0]` / `st[last]` и при каноничном порядке **выбрасывает жёлтый целиком** | рампа законна, но пару задавать ЯВНО из набора: navy в тенях, yellow в светах (самая сильная) |
| **1011-1014** `assetTint` (деф 0.55) | альфа-смешение брендового цвета с фото | законно, композитинг |
| **2047-2048** спектр | `createLinearGradient` blue → yellow → red, причём в sRGB, не в OKLCH | законно как дата-виз, но кормить тремя брендовыми |
| **704** fallback `#3f6fe0` / `#e4552f`, **770-776** PALETTES 4-7 | сеют небрендовые цвета в приложение | перевести на тройку |
| **22-24** CSS-токены | **интерфейс живёт на четвёртой палитре** | заменить, чинить первым |

### Расхождение токенов с исходником

| токен | в коде | фактический hex | против бренда |
|---|---|---|---|
| `--yellow` | `oklch(0.84 0.165 88)` | `#f7c323` | близко к `#f6c60a` |
| `--red` | `oklch(0.62 0.205 32)` | `#e74328` | **это оранжевый**, L 0.62 против 0.545 |
| `--blue` | `oklch(0.66 0.160 250)` | `#3496ef` | **это небесно-голубой**, L 0.66 против 0.359 |

Роб прочитает это как «цвета не совпадают» раньше, чем доберётся до дрейфа

### Что делать с hueDrift, чтобы не потерять реакцию на музыку

Два честных варианта вместо вращения тона:
- **(i)** модулировать L и C, тон заперт
- **(ii)** центроид перевешивает, КАКОЙ из трёх стопов берёт тело (перестановка внутри набора)

Оба сохраняют «дышит под музыку» и не изобретают тонов. Это и есть «усреднить везде». Выбор за Робом, флаг в срезе 0

### Подведённая тройка

```
red    #ce1c22   oklch(0.545 0.209 27)    без изменений
yellow #fdbe0c   oklch(0.836 0.170 84)    было #f6c60a / 0.845 0.171 91
navy   #2436a7   oklch(0.402 0.180 269)   было #2d2f83 / 0.359 0.138 276
```

**RED держим.** Якорь и самый насыщенный из трёх. Любой сдвиг ломает узнавание

**YELLOW теплее.** Основной `#fdbe0c`, осторожный `#fdc103` / `oklch(0.842 0.172 86)`. Сдвиг: тон −7°, L −0.009, C практически без изменений. Уходит лёгкий лимонно-зеленоватый подтон, который у `#f6c60a` вылезает именно рядом с навием. Становится тёплое золото-охра вместо сигнального жёлтого, переход к `#ce1c22` читается как семья, оба на тёплой стороне колеса. На проекторе контраст к фону остаётся 12.3:1. Потолок sRGB: ниже 84° при этой светлоте красный канал упирается в стену (при H 82-84 всё выше C≈0.170 выходит из sRGB). `#ffbe00` выглядит теплее, но сидит на клиппинге и поплывёт в P3 и в печати. 84° последняя честная точка

**NAVY звонче.** Основной `#2436a7`, мягкий `#2735a4` / `oklch(0.398 0.176 270)`. Сдвиг: L +0.043, C +0.042, тон −7° от фиолета к чистому синему. У текущего фиолетовый подтон, и при L 0.359 он умирает на фоне `bgL:0.13` (652): контраст 1.77:1, на сцене это чёрная фигура. Нудж даёт 2.10:1 и переводит цвет из чернильно-издательского в экранно-синий, тот самый digital-регистр. Марка узнаваема: тон двигается на 7°, попарная различимость почти не меняется (dE к red 0.366 против 0.343 было, к yellow 0.565 против 0.575 было)

Если навию всё равно не хватит на проекторе, рычаг это **`bgL` (652, дефолт 0.13), а не четвёртый цвет.** Подъём фона до 0.18-0.20 даёт больше, чем любое дальнейшее наращивание хромы: дальше по этому тону идёт `#0900d1`, а это уже не марка

### Куда встают три значения

CSS-токены **22-24**, `BRAND` **810-814**, `state.stops` **658**, `PALETTES[0]` **770**, fallback **704**

### Заметка на полях

Комментарий 683-688 фактически неверен. Для navy (275.9°) → yellow (90.9°) короткая дуга это **+174.9° вверх, через маджентовую сторону**: t=0.25 даёт `#803c90` (фиолетовый), t=0.5 даёт `#c95277` (розовый), t=0.75 даёт `#f68140`. Зелёный дала бы ДЛИННАЯ дуга. Вывод канона верен, обоснование в комментарии описывает не тот путь. Правим комментарий вместе с кодом

---

## 10 · ПОРЯДОК РАБОТ

Каждый срез сдаётся отдельно, гейтится отдельно, откатывается отдельно. Скрин перед сдачей каждого визуального изменения (правило §1 делегат-MD)

### Срез 0 · ЦВЕТ-ТОКЕНЫ · ~2 часа

Три значения в пять мест (22-24, 810-814, 658, 770, 704). Снять `hueShift` из 996 и 719. Поправить комментарий 683-688
**Гейт:** скрин сцены + скрин обеих панелей; `probe/panel.cjs` зелёный; ноль небрендовых hex в `PALETTES` и fallback
🔴 **ГЛАЗ РОБА до продолжения:** тройка на экране и **решение по hueDrift, вариант (i) или (ii)**

### Срез 1 · ИНФО-ДОК И ИКОНКИ · полдня

`#foot` в левую панель, два делегированных слушателя на обе `.pbody`, `DESC`, попап шорткатов, иконки легенд из локального пака Lucide. Гасим 4 ноты, 3 оставляем статусом, 2 переносим
**Гейт:** ноль `<p class="note">` с объяснением контрола; каждый контрол при hover И при focus пишет строку в док; Escape и `pointerdown` закрывают попап; скрин обеих колонок
Независим от всего остального, можно делать параллельно

### Срез 2 · ЛЕВАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ · день

Перестановка филдсетов: место под MACRO → LAYERS (пресет-полка, стопка, подсекция POOL) → FORCES → SPACE → заглушка EFFECTS. Formation распускается: текст и шрифт в карточку Word, `Logo size` в карточку Logo, **`Logo as void` в FORCES**
**Гейт:** скрин левой колонки; `__bryk.layers()` не изменился по форме; сцена рендерит бит-в-бит то же
🔴 **ГЛАЗ РОБА:** последовательность зон и куда сел `Logo as void`

### Срез 3 · КАРТОЧКА · день

Разводим фокус и раскрытие: клик по имени = фокус + раскрыть (никогда не закрывает), шеврон закрывает. `focusLayer` получает ранний выход по идемпотентности (EQJ `:653`). `prompt()` (2541) заменяется инлайн-переименованием (EQJ `:637-643`). ▲▼ получают тултип про раздачу, не про z. Порядок раскрытого: share → BODIES → FORM → TEXT/MARK → счётчик mappings. Бут ставит Axis + Word
**Гейт:** двойной клик по АКТИВНОМУ слою переживает перерисовку; счётчик mappings совпадает с числом строк рэка; скрин свёрнутой и раскрытой карточки; сцена из старого json грузится
🔴 **ГЛАЗ РОБА:** раскрыт один слой или несколько; порядок Axis / Word в раздаче

### Срез 4 · МАРКИ В КАРТОЧКУ · день, риск выше остальных

Единственный срез, который трогает движок, а не панель. `L.shapes` новым полем в `makeLayer` (1542-1550); `assignFaces` (1028) начинает раздавать лица по разбиению `share`, а не глобально; `faceList` (966-979) и `rebuildTextures` (980) перестают читать `state.shapes` (643). Глобальными в POOL остаются: Count, Size, Picture radius, Outline only, Face heading, Size variation, Tilt variation, Asset tint, ADD FILES / CLEAR, RANDOMISE
**Гейт:** два слоя с разными весами марок дают визуально разные тела на одном экране; миграция сцены `state.shapes` → `L.shapes` всем слоям при `applyScene` (2156); банк текстур не растёт квадратично (сейчас декартово произведение FACES × палитра, 962-965)
🔴 **ГЛАЗ РОБА ДО НАЧАЛА**

### Срез 5 · СТЕК ЭФФЕКТОВ · два дня

`.lyr` + `--z/--op/--bm`, `TIER`, `registerLayer` с двусторонним пином, `reflow()`, один сортированный массив как источник и z, и порядка отрисовки. `runPost` переезжает в POST одним `kind:'gl'` слоем с FBO-цепью, фрагмент отдаёт настоящую альфу. `#camcv/#cv/#glcv` регистрируются как BG/SCENE/POST, `body.camover` (71-72) снимается и становится `order`
**Гейт:** проба-пиксель во всех четырёх комбинациях (camover × chroma) даёт ожидаемый слой; ноль `z-index` по id в CSS; ноль `#stage canvas` правил; FPS ≥55 при трёх включённых эффектах (гейт I3 мастер-плана)
🔴 **ГЛАЗ РОБА:** карточка эффекта и её отличие от карточки слоя

### Срез 6 · ПРАВАЯ ПОДГОНКА · полдня

`legend[data-toggle]` (157-159, 2512-2520) → `.sidep>.sphead` с шевроном-маской Lucide. Клик по `#rackLayer` скроллит к карточке. Ноты правой в `DESC`. Ретрофит: правка параметра в карточке пишет сквозь в базу строки рэка на том же пути (§2, пункт 6)
**Гейт:** скрин; focus-visible виден на всех интерактивных; правка параметра в карточке не перетирается драйв-петлёй на следующем кадре

### Срез 7 · MACRO · день, блокирован

Три скраба верх левой панели, раскладка макро→параметры декларацией, живёт в сцене v+1 (миграция §15.3). Через базы
**Гейт:** каждое макро двигает ≥5 параметров; сцена сохраняет и восстанавливает
Блокирован черновиком **W9-macro** (`macro-map.js`, 8-12 мишеней на макро с весами). §3 мастер-плана: «I1 MACRO (ждёт черновик W9-macro)»

### Срез 8 · БАГИ · ~2 часа

Роб: панель сейчас, баги потом
- **2326** дубль `mixRow('wTri','tri','triangle weight')` подмешивает второй скраб в тот же хост и перетирает `SCRUBS.wTri`. Удалить строку
- **2262** `syncAllScrubs` мапит фантомный `wRect: state.shapes.rect` и не мапит `wWedge`: wedge не пересинкается после вызова сцены
- **2739** прогрев камерных текстур идёт по `['rect','circle','tri']`, тогда как `faceList` (977) работает по `['tri','wedge','circle']`: wedge-камера не прогревается, `rect` фантом

Срез 4 перепишет первые два целиком. Если срез 4 сдвинется, чинить отдельно и раньше

### Срезы, требующие глаза Роба ДО продолжения

**0** (тройка + решение по hueDrift) · **2** (последовательность зон) · **3** (одна карточка раскрыта или несколько) · **4** (весь срез, он трогает движок) · **5** (карточка эффекта)

---

## 11 · ЧЕГО НЕ ДЕЛАЕМ

- **Не выносим слои в edge-tabs + флайаут** как EQJ 7·B (`mvj-inject:74`, `:723`). Требование Роба прямое: слои в левой панели. Hover-intent 280/180/30ms не портируется
- **Не даём слоям-программам blend и opacity.** Один пул, накладывать нечего. Blend и opacity живут только на слоях эффектов (§7)
- **Не делаем drag-реордер.** В EQJ его нет (`moveLayer` swap, `mvj-inject:656`), кнопок ▲▼ достаточно. Если понадобится, шов один: swap-then-`reflow()`
- **Не редизайним панели.** Это раскладка зон, не новый UI (решение Роба §10.0.5, §4 forbidden делегат-MD)
- **Не трогаем пороги LOD и QUAD_BUDGET**
- **Не строим редактор приёмов из UI.** Следующая волна, по глазу Роба
- **Не ставим `backdrop-filter` без замера** p95 frame-time до и после (§8, коллизия 3)
- **Не пружиним значение.** Value-путь без анимации вообще (§8, коллизия 1)
- **Не заводим четвёртый цвет.** Рычаг под навий это `bgL` (652), не новая марка
- **Не делаем сферу и шар** (§1 делегат-MD)
- **Не применяем пост-эффекты на ходу.** Пост живёт отдельным слотом (решение Роба §10.0.6)
- **Не открываем N WebGL-контекстов.** Один контекст, цепь на FBO (§7)
- **Не переносим `composite()` из EQJ.** У BRYK нет чего композитить по слоям-программам, только по слоям эффектов
- **Не трогаем `state` без миграции сцены.** Каждое новое поле в `makeLayer` требует ветки в `applyScene` (2156)

---

**Файлы, на которые ссылается план:**
`/Users/robertvagin/Claude/Projects/bryk/index.html` · `/Users/robertvagin/Claude/Projects/bryk/probe/panel.cjs` · `/Users/robertvagin/Claude/Projects/bryk/probe/app.cjs` · `/Users/robertvagin/Claude/Projects/bryk/audio-core.test.js` · `/Users/robertvagin/Claude/Projects/bryk/BRYK_MASTER_PLAN_2026-07-18.md` · `/Users/robertvagin/Claude/Projects/bryk/BRYK_DELEGATE_ULTRA_2026-07-18.md` · `/Users/robertvagin/Claude/Projects/music-viz/mvj-inject.html` · `/Users/robertvagin/Claude/Projects/music-viz/synthex-music.html` · `/Users/robertvagin/Claude/Projects/synthex-engine/engine/Synthex Engine — v1.html` · `~/.claude/skills/apple-design/SKILL.md` · `~/.claude/skills/panel-canon/SKILL.md` · `~/.claude/skills/icon-canon`
---

## A1. 2026-07-19 · Внешний аудит Fable — база терминала (документ и стоп, код не тронут)

**Метод:** независимый прогон гейтов в песочнице (стаб libXdamage + локальный сервер + chromium_headless_shell) + статический аудит `index.html` (3600+ строк) по чек-листу инвариантов. Все номера строк — по файлам на утро 2026-07-19.

### A1.1 Гейты — независимое подтверждение

| Гейт | Заявлено | Мой прогон |
|---|---|---|
| `audio-core.test.js` | PASS | ✅ PASS (все чеки, вкл. трим/фазу/AUTO) |
| `probe/panel.cjs` | 36/36 | ✅ **36/36 подтверждено** (консоль · панель-канон · геометрия · контраст · WCAG 1.4.12 · клок · хром) |
| `probe/app.cjs` | 55/55 | ⚠️ вся контрольная часть отработала, но на FPS-бенче с chroma браузер умирает (SwiftShader, софт-WebGL песочницы) — итоговая строка не печатается. Артефакт среды, не аппа. FPS в софт-рендере: 300 тел = 60 · 1000 тел = 52–55 · 500+chroma = 20 |

### A1.2 Инварианты — заявленное сверено с кодом, всё на месте

- **Аккумулятор модуляций:** `applyMatrix` (:2267) реально СУММИРУЕТ девиации по цели — блокер last-wins из §10.4 закрыт. Жемчужина: `dev*=base` для PRESENCE (:2290) — «выключено значит выключено», слой на нуле не поднимает сам себя ритмом.
- **base/setDriven (§15.5):** BASES/restoreMatrix в подвале кадра, driven-значение не живёт дольше кадра, `DRIVEN_WATCH`-хук для проб (:3800–3806).
- **Миграция сцен:** версия реально читается, mismatch → дефолты полей + статус «scene v… loaded into v…» (:2825).
- **SVG-ловушка (R10/§10.3-1) закрыта:** парс `viewBox`, размер до отрисовки, с комментарием-уроком (логотип и дропы).
- **«Лицом вперёд по вектору» (§9.5) реализован:** heading = `b−b.prev`, нормаль лерпится к нему по `LK.face`, квад банкуется; билборд-путь — `atan2(hy,hx)·face` (:3714–3728). Это ровно механика shuffler, унифицированная на пул.
- **Физика на uniform grid:** `cellKey/cellKey3` (:2461–2462), бакеты индексов, грабли задокументированы в коде (:2515, :2552).
- **Шрифты (§9.4):** `queryLocalFonts` за жестом + курируемый фолбэк через `document.fonts.check`, пикер никогда не пуст. Дефолт WORD = «BRYK» (решение 10.0.4) ✓.
- **Панель-канон:** `allowExtend` ×27 (exceed→extend), скраб с resist-кривой по apple-design, aria-роли; гигиена: 0 `console.log`, 0 TODO, один script-блок, `node --check` OK.
- **Решения Роба §10.0 в коде:** тёмная база `oklch(0.13 0.012 265)` ✓ · дуотон камеры ✓ · камера: один декод → все потребители (:3815+) ✓.

### A1.3 Находки

**P1**
1. `probe/app.cjs`: FPS/chroma-бенч может убить браузер ДО печати итога (софт-WebGL) → частичный прогон выглядит как крэш. Обернуть бенч try/catch и печатать итог всегда; бенч — после summary. (В трек W10.)
2. Клавишами вызываются только сцены 5–8 → банки 1–4 (:3638) — **4 слота из 8 недостижимы с клавиатуры**. Решить (Shift+5–8? два ряда?). needs-Rob.

**P2**
3. `serve.sh` биндит `127.0.0.1` — там, где `localhost`→`::1`, пробы падают с ERR_CONNECTION_REFUSED (поймано у меня). `--bind ::` устойчивее.
4. `index.bak.html` как механизм версий — заменяется git'ом (шаг 0 ультра-делегата, git в bryk/ всё ещё нет).
5. `ref-*.png` ×7 в корне трека — перенести в `refs/`.
6. `allowExtend` селективен — сверить по панель-канону, что он есть на всех числовых скрабах, где превышение осмысленно.

**needs-Rob:** пункт 2 (клавиши сцен) · порог chroma-фпс на живом железе (софт-рендер даёт 20, на маке терминал заявлял 60 — проверить глазом на проекторе).

### A1.4 Вердикт

База добротная. Расхождений «доки говорят одно — код другое» **не найдено**; терминал даже закрыл ловушки, которые я флагал в ресёрче (SVG viewBox, last-wins, face-по-вектору). Готово к ультра-волне: git init → I1 MACRO → I2 PANEL (делегат `BRYK_DELEGATE_ULTRA_2026-07-18.md`, дополнить трек W10 пунктом A1.3-1).

---

## A2. 2026-07-19 · Инвентарь отображений по наборам + спека «4 независимых слота»

**Задание Роба:** проверить, что ВСЕ отображения из наших мини-апок учтены и правильно присваиваются слоям; горячие клавиши добавить; слои сделать контейнерами — **ровно 4 независимых слота**.

### A2.1 Инвентарь: наборы → BRYK (сверено по коду index.html)

| Источник | В оригинале | В BRYK | Статус |
|---|---|---|---|
| **image-shuffler** LAYOUTS | 14 раскладок | axis-движок: flow · waterfall · sine-wave · zigzag (:1677–1686) + standalone: pulsing-circle · radial · vortex · helix · spring · grid · float · carousel · card-deck · flip-book (:1692+) | ✅ **14/14** |
| **particle-dance** паттерны | 15 в UI (17 в коде) | pat-банк :1420: cloud · lattice · vortex · ribbon · burst · wave · spring · zigzag · spiral/DNA · **check · cross** · axes · orbitals · fan · steps · glyphs · form | ✅ **17/17** (вкл. два скрытых) |
| **Motion Primer** | 6 поведений + 6 манер + formation | BEHAVIOURS :1429 (swarm·pack·magnet·orbit·fall·scatter) + MANNERS :1444 (отображены в наш мир) + word/mark (:1894–1907) | ✅ полностью |
| **EQJ** | LINES · BODIES · DOTS · FORMS | LINES (Synthex) — намеренно вне BRYK (standalone-решение §0); BODIES → силовой блок; DOTS/FORMS → pat-банк + BEHAVIOURS | ✅ по решению |
| **Камера** | — | слой cam2 + текстуры-маски camTexFor | ✅ |
| **Ресёрч-волна R10/R11** | quad-wave · эхо-туннель · волна · калейдоскоп · fluid · milk · RD | ещё не вживлены | ⏳ в треках делегата (I3, W4–W8) |

Вывод: **все отображения наборов учтены**, включая два паттерна, которых не было даже в UI оригинала. Пробелов нет; хвост — только ресёрч-эффекты, уже распределённые по воркерам.

### A2.2 Спека: 4 независимых слота-контейнера (решение Роба)

Сейчас слои — динамический список `layers[]` без потолка (:2013, addLayer :2110). Меняем модель:

- **`slots[4]` — фиксированные контейнеры S1–S4.** Слот = `{prog|null, w (PRESENCE), muted, solo, params, rows[]}`. Любая из ~40 программ инвентаря A2.1 назначается в любой слот; пустой слот — честный empty state карточки («назначь программу»).
- Слот **независим**: свои параметры, свой PRESENCE, свои мод-строки, свой вариант формы. Пул один (§9.1), слоты = 4 программы над ним с весами — архитектура не меняется, меняется контейнеризация UI и адресация.
- Почему фиксированные 4: урок EQJ §9.0-1 (лимит вживую) становится архитектурой; хоткеи стабильны (S1 всегда клавиша 1); сцена = снимок 4 слотов — карта сцен предсказуема; карточки в панели не прыгают.
- **Сцены v+1:** пишут 4 слота; миграция старых сцен: layers[0..3]→S1..S4, лишние отбрасываются со статусом.
- Морф A/B (§16.2 Pattern A/B) живёт ВНУТРИ слота как вариант программы-«pattern» — слоты это не отменяют.

### A2.3 Горячие клавиши (предложение, финал — по глазу Роба)

```
 1 2 3 4        фокус слота S1–S4 · двойное нажатие = mute/unmute · Shift+цифра = solo
 Q W E R        приёмы MAGNET / GATHER / CROWD / SNAP  (переезжают с 1–4)
 5 6 7 8        сцены 1–4 · Shift+5–8 = сцены 5–8  (закрывает A1.3-2: все 8 слотов достижимы)
 T [ ] \        tap · nudge · ресинк      B blackout · H хром · F fullscreen
 C камера · P палитра · Space следующий приём в фокус-слоте (квантовано)
```

Принцип: цифры = «куда смотрю» (слоты) и «что вызываю» (сцены), буквы = «что делаю» (приёмы). needs-Rob: двойное-нажатие vs Alt для mute; Space-семантика.

### A2.4 В делегат

Ультра-делегат дополнен: задача **I2a · SLOTS-4** (после I1 MACRO, сшивается с I2 PANEL — зона MODES = 4 карточки слотов) + правка клавиатурной карты + W10 дополнен пунктом A1.3-1 (try/catch на FPS-бенч).

---

## A3. 2026-07-19 · Живой фидбек Роба: слои по модели EQJ · Малевич-ансамбль · нейтральная чувствительность

**Источник:** Роб погонял апп вживую. Четыре сообщения + скрин MixEmergency (SAMPLES-блоки → BANK A–D → OUT → MIX-ручка). Это таста-фидбек высшего приоритета — правит §9.1 и A2.

### A3.1 🔴 Слои: weight-blend модель НЕ работает вживую — берём модель EQJ

Диагноз Роба: «слои не работают, всё полупрозрачное». Веса присутствия, бленд целей одного пула — вживую даёт кашу, а не слои. Решение — **логика EQJ, которая уже была и работала**: слой = отдельный присвоенный блок.

- **Слот S1–S4 = честный слой:** свой **суб-пул тел** (партиция общего бюджета, не бленд целей!), свой **blend-режим** (normal · multiply · screen · difference · exclusion — как в EQJ-стеке), своя **независимая прозрачность-фейдер** (opacity слоя, не «вес влияния»).
- Слои **не переключаются** — все четыре живут одновременно, каждый рисует свою картинку.
- **Поведение блока применяется ТОЛЬКО к своим телам** — блоки не конфликтуют по поведению by design. Межслойное взаимодействие остаётся одно: **avoid/collide через общий spatial grid** (слои «замечают друг друга» телами, но не силами).
- Морф A/B внутри слота остаётся (это внутрислойное дело).
- Рендер: партиция сортируется по слоям → каждый слой в свой проход с (blend, opacity) — через отдельные canvas-проходы или globalCompositeOperation, решает интегратор по FPS.

### A3.2 LEAD — лидер с эйсингом

«К слоям присваиваются лидеры, переключение 1–4 с эйсингом»: **LEAD = ведущий слой**. Лидер получает эмфазу (множитель энергии/яркости/масштаба ~1.0, остальные приглушаются к ~0.4–0.6), клавиши 1–4 **плавно переводят лидерство** — кроссфейд эмфазы по ease-кривой Motion Pad, 0.6–1.2с, квантуемо к bar. Это переключение фокуса как между деками, при живых всех слоях. *(needs-Rob: подтвердить семантику; если имелись в виду ещё и тела-вожаки внутри слоя — добавим вторым механизмом.)*

### A3.3 Арт-директива поведения: супрематизм Малевича

Дефолтное поведение частиц = **ансамбль геометрических фигур, которые ТАНЦУЮТ достойно**, не эпилепсия:

- **Минимальное наложение:** avoid включён по дефолту (resolveBodies), фигуры уважают чужое место — супрематическая композиция, не куча.
- **Масштабная иерархия:** log-распределение размеров — несколько КРУПНЫХ доминант + рой мелких (как у Малевича: большой чёрный прямоугольник и спутники).
- **Вариации инверсии:** редкие события flip fill↔stroke / цвет↔фон на onPhrase/приёме — не на каждый бит.
- **Композиционное равновесие:** мягкая центр-балансировка (лёгкая тяга к равновесию масс), медленные достойные траектории по дефолту.
- Референс UI — MixEmergency: **банк блоков** (все ~40 программ + приёмы) → назначение блока в слот, как SAMPLES→BANK A–D на скрине.

### A3.4 Мастер-контрол

Общий **MASTER** над стеком: мастер-фейдер (общая яркость/энергия всех слоёв разом, MIX-ручка референса) + 3 макро §9.2 сверху. Одна рука глушит/поднимает всё — «убирали присутствие эффекта и легче сводили» на уровне всего микса.

### A3.5 🔴 Калибровка чувствительности — везде

Диагноз Роба: «всё слишком чувствительное, дёргается; диапазон до 500, а мне нужно 0.5». Калибровочный наряд по ВСЕМ параметрам:

1. **Диапазон слайдера = рабочая зона** (полезные значения занимают середину хода), экстрим — только вводом числа через allowExtend. Критерий: если нужное значение 0.5 — слайдер до ~2, не до 500.
2. **Нейтральный дефолт** у каждого блока: при дефолтах — спокойный малевич-танец (A3.3), ноль дёргания.
3. **Response-кривые** на скоростных/силовых ручках (exp/квадрат) — начало хода деликатное.
4. **Мод-глубины дефолт ≤0.15** и мягче attack/release на дёргающихся мишенях; fast-огибающие — только на импульсные приёмы.
5. Пройти каждый блок отдельно и «свести к нейтральному», таблица калибровки (param · range · default · curve) — датированной секцией в канон.

### A3.6 В делегат

I2a переписан → **SLOTS-EQJ** (модель A3.1 вместо весовой), добавлены **I2c LEAD**, **I2d MASTER**, наряд **CAL-SENS** (A3.5) и малевич-дефолты. Весовые формулировки §9.1/A2.2 читать с поправкой A3.1: веса остаются ТОЛЬКО как внутренний механизм морфа A/B внутри слота.

---

## A4. 2026-07-19 · Волна терминала: слои · противоречия · словарь поля

**Роли:** терминал = интегратор (единственный владелец `index.html`) · три read-only аудитора чужими глазами · Роб = глаз и фидбэк по живому экрану.
**Гейты на конец секции:** audio PASS · panel 36/36 · app **69/69** (было 55/55, +14 контрактных).
**Коммиты:** `e007fdb` доки · `66770dd` физика на слой · `888c953` противоречия · `4164ba2` словарь поля · `3cf2277` геометрия кадра · `6665ae4` формация.

### A4.1 Силы переехали на слой (закрывает A3.1)

Суб-пулы `L.bodies`, `L.opacity` и `L.blend` были сделаны 18.07 (`d5a02d4`) — то есть половина A3.1 уже стояла. Не сделано было главное: **`state.phys` оставался ОДНИМ объектом на сцену**. Гравитация на одной карточке роняла весь экран, два поведения в стеке молча перетирали друг другу swirl. «Поведение блока применяется только к своим телам» не выполнялось.

- `DEF_PHYS()` на слой; `state.phys` стал аксессором на **фокусный** слой, поэтому правая колонка, пресеты поведений/манер и мувы адресуют «тот слой, на который ты смотришь», а не всех сразу.
- `PHYS_PARAMS` — восемь сил как мишени рэка, привязанные к `L.phys`. Близнецы из `GLOBAL_PARAMS` удалены (та же ловушка, что с `size`/`count`: рэк писал один объект, движок читал другой).
- **Столкновения вынесены из `stepPhysics` в `resolveContacts(act,dt)`** — единственная сила, которая по A3.1 обязана пересекать стек. Общий spatial grid, радиус пары = среднее, каждое тело толкается силой СВОЕГО слоя.
- Сцены v2: `phys` на каждый слой, миграция v1 (один глобальный набор → всем слоям).

### A4.2 Потолок скорости — причина «дёргается»

В движке **не было предела скорости**. Затухание не ограничивает ничего: оно задаёт установившуюся скорость, пропорциональную силе, а силы отмерены в тех же единицах, что мир. `gravity 1.6` при `damp 0.90` даёт ≈16 мировых единиц в секунду в кадре шириной 3.2 — тело пересекает экран за пятую долю секунды. Замер: два слоя, поставленные в 0.04 друг от друга, расходились на **573**.

`VMAX_PER_SEC=6`, кламп в подвале `stepPhysics`. Ловушка единиц: `b.vx` — смещение **за кадр**, не скорость; первая версия клампа была написана как u/s и оказалась в 80 раз слабее, чем нужно, — гейт остался красным и показал это.

### A4.3 Противоречия по фидбеку Роба (три аудита чужими глазами)

| Что | Диагноз |
|---|---|
| **Count в правой колонке** | Писал `state.count`, пересборка читала `L.count`. Единственный видимый Count не делал ничего. Стал мастером на все слои |
| **8 контролов look** | Их строки убрали из вёрстки, а `mount()` молча пропускал отсутствующий слот. Жили в `SCRUBS`, синхронизировались, подсвечивались модуляцией — и не существовали на экране. `mount()` теперь орёт в консоль и это ловит app-гейт |
| **Рандомайзер в карточке** | Вызывал глобальный `rerollMix()` — жмёшь на слое 3, пересдаются 1, 2, 4. `rerollLayer(L)` крутит `L.seed` и пересобирает облако, марки и посев |
| **`baseParticle(i,77)`** | Зашитый сид: у всех слоёв и дубликатов побайтно одинаковое облако. Теперь `L.seed` |
| **10 пресетов с пустой карточкой** | `keys:['speed']` против параметра `sceneSpeed`. Фильтр выбрасывал единственный параметр; посев бита был мёртв на всех десяти |
| **Физика в двух диапазонах** | gravity −3…3 рукой и −2…2 звуком, и так пять раз. Панель генерируется из `PHYS_PARAMS` |
| **NaN в калибре** | Старые сцены не писали `varSize` → `lerp(1,size,undefined)` → тело с NaN-размером не рисуется, без единой ошибки |
| **fill / frame** | `buildLogoMask(N)` объявлена с одним аргументом, режим передавался вторым и молча терялся. Реализован для `mark` и для `word`, режим вошёл в ключ кэша |
| **Logo size** | `PROG_EXTRAS` знал ключ `logo`, id пресета — `mark`. Контрол навсегда оставался в скрытой парковке |

### A4.4 Гейты, которые ничего не доказывали

Урок дня, в трёх экземплярах:

1. **Квад-бюджет** печатал `0 quads` на всех замерах — `setFace()` писал мёртвый глобал, ни одно тело не шло по квад-пути. Гейт был зелёным, потому что мерил пустоту. После починки: `41 → 48 → 53` при шестикратном росте тел; утверждение переписано на «не масштабируется», а не «не растёт на +2».
2. **Сцен-гейт** проверял, что три мёртвых поля переживают сохранение.
3. **Мой собственный гейт заполнения кадра** мерил «107% полукадра» **против той же неверной константы**, которую проверял. Переписан на пиксели экрана через `project` — и сразу показал правду.

Правило в силу: если гейт спрашивает у движка его же константу и сверяет движок с ней, он зелёный при любой ошибке в этой константе.

### A4.5 Словарь поля — четыре слова (Роб назвал сам)

```
density  сколько тел          → count, мастер + на слой
scale    размер композиции    → множит цель ЛЮБОЙ раскладки (универсально, все 28)
spacing  воздух между ними    → множит внутренний шаг раскладки
size     калибр одного тела   → L.look.size
```

Плюс **`size range`** — распределение размеров по Малевичу вместо равномерного: `u^2.2` смещает массу в мелкий конец и оставляет тонкий хвост доминант, `LO/HI` подобраны так, что **геометрическое среднее = 1** (`0.6875·ln0.56 + 0.3125·ln3.6 = 0`), поэтому кручение раздвигает поле, не меняя масштаб сцены. Иерархия 6.4×. Замер гейта: `range 0 → ×1.00 · range 1 → ×6.3 (mean 0.99)`.

Ключи НЕ переименованы там, где сменилась только подпись (`varSize` остался `varSize`). Переименование ключа с одной стороны — это ровно то, что убило десять пресетов сегодня утром.

### A4.6 🔴 Кадр оказался вдвое больше, чем думал движок

Главная находка волны. `project` решается обратно:

```
screen_x = W/2 + q.x·k ,  k = base·zoom/z ,  base = min(W,H)·0.46 ,  z = d = 3.2
край  ⇒  |q.x| = (W/min(W,H)) · d/(2·0.46) / zoom  =  (W/min) · 3.478 / zoom
```

В коде стояла константа **1.6**. Занижение в 2.17 раза. Каждая раскладка спрашивала «где край» и честно до него тянулась — до 46% пути. Ни одна настройка не могла это исправить, потому что край был потерян в самой геометрии. Константа пришла из `image-shuffler:547` (`vpHalf`), где тот же пропущенный делитель `d` компенсирован скрытым множителем сцены ×2.18 — заклеено, а не решено.

`viewportHalf()` теперь считает честно и учитывает `cam.zoom`.

**Следствие, которое поймал только скриншот:** кадр стал вдвое длиннее, а шаг вдоль дорожки остался абсолютным — поле превратилось в комок, едущий по пустой дорожке. Гейт был зелёным. Шаг переписан на **решатель плотности**: `fill = 2·reach / perLane` — шаг, при котором ЭТА толпа покрывает ЭТУ дорожку; `gap` стал долей от него (1 = полная дорожка), пол — одна ширина тела, чтобы тела не штабелировались. Сиды пресетов пересчитаны с абсолютных 0.40–0.46 на доли ≈1.

### A4.7 Веер дорожек и Waterfall

`laneOff = (lane-(axes-1)/2)·(perpReach·1.5/axes)` — веер делился на число дорожек, поэтому чем больше дорожек, тем уже доля каждой: 4 дорожки доставали до 56% полукадра, 12 — до 69%, **край недостижим при любых настройках**. Теперь крайние дорожки садятся ровно на `±perpReach·spread`, `spread` 1 = от края до края.

**Waterfall свёрнут в осевой движок.** Он объявлял в карточке angle, bend, bendFreq, tilt3d, sceneSpeed, spin и **не читал ни одного** — шесть живых ползунков в никуда, плюс тот же баг веера и тот же замороженный `state.size`. Всё, что у него было своего, — сдвиг фазы по колонкам; это стало параметром `stagger` (каскад), доступным каждому осевому пресету. Дубликат удалён, «one engine, four seeds» стало правдой.

### A4.8 Открыто

- **Панель** — перестройка по A4-фидбеку Роба: слева проводник приёмов с категориями в выпадашке и карточками слоёв, справа маппинг как контекст слоя, общий ритм в правом верхнем углу. Не начато.
- **Камера** — решение Роба 19.07: остаётся в правой колонке, нужна и как отдельный бэк, и как начинка фигур. Влево не переезжает.
- **Калибровка остальных параметров** по методике A4.2 (арифметика движка, не глаз): `gravity` у fall 0…1200, `urge` 10…400, `dance`, `frequency`, `reach`, `spin`, веса марок. Таблица — следующей секцией.
- **К переносу из наработок** (инвентарь 19.07): эхо на тело (`particle-dance:705-709`, ~6 строк) · трейл `destination-out` на слой (`mvj-inject:417`) · каскад `enterFactor` на радиус, не на альфу (`particle-dance:643-648`) · Motion Pad (`builder-engine/motion-core.js:12-33`) · `orientMode` direction/stack · решатель плотности маски (`Motion Primer:215-226`).
- **Ресёрч эстетики частиц** (Малевич ↔ триппи аудиовизуализатор) + разбор присланного пресета Karl Sims RD — в работе.
- Мёртвое, но не тронутое: Waterfall-долг закрыт, остаётся `P.sizeScale` — читается, не объявлен ни одним пресетом (`sizeMul` всегда 1); `cam2.posterize` читается, контрола нет; `BASES.delete(r.path)` в рэке не попадает по ключу (`layerId::path`).

---

## A5. 2026-07-19 · Второй заход: стендбай, словарь панели, плавность

**Гейты:** audio PASS · panel 36/36 · app **72/72**.
**Коммиты:** `f7d730a` мувы+плоская база · `02d2295` рельса · `0577322` 420 тел · `9d0c05a` стендбай · `2c1bdb8` фаза+лава · эхо и жизнь слоёв следом.

### A5.1 Стартовый экран — сцена Роба

Аппка открывается не на демо осевого движка, а на брендовом стендбае: `bryk-scene (1).json` слово в слово — Pulse burst, 631 тело, плоская камера, мягкие силы, его мод-матрица из семи строк. В тишине спокойно, под музыку оживает.

### A5.2 🔴 Мухи: рейт умножался на часы

**Причина арифметическая, не вкусовая.** Каждый анимированный пресет читал фазу как `t · rate`, где `t` — секунды с загрузки. Модуляция рейта от звука не ускоряла движение, а **прыгала фазу** на `Δrate · t`. Через минуту работы шевеление `dance` на 0.1 швыряло фазу на пять радиан за кадр. Ошибка **росла тем сильнее, чем дольше аппка открыта** — сглаживание огибающей не помогло бы никогда.

Фаза копится: `L.clock.{dance,speed,scene,flow} += dt·rate`. Исправлено в 14 местах (dance, axis speed, 11 раскладок, curl-поле). Гейт: тихий кадр 0.0112 → кадр после рывка ручкой 0.0113, было ×6.

По дороге гейт поймал две мои ошибки: нижний порог на пружину сломал семантику «hold formation = 0 отпускает слой», и накопленная фаза читалась, но не передавалась — код молча падал на старый путь.

### A5.3 Панель: рельса и человеческие имена

- Слои — строка: имя · opacity · blend. Больше не разворачиваются вниз, стек не прыгает.
- Ниже — одна секция настроек **выбранного** слоя: shapes · field · motion · pattern.
- Плашки по категориям вместо одного плоского списка из 28 одинаковых строк.
- Поштучные веса треугольник/клин/круг убраны — набор фигур одним селектом (`SHAPE_SETS`), RE-DEAL и `+ SVG` рядом.
- Два дропдауна behaviour/manner убраны: пресет, молча перезаписывающий восемь ползунков под собой, противоречит карточке.
- Силы названы по делу: `float · lava` · `avoid each other` · `gather to centre` · `hold formation` · `move together` · `fall`. Damping, body radius и 3D ушли из панели в модель (сцены и рэк их держат).
- Count из правой колонки удалён совсем — два контрола на одну величину противоречат друг друга при любом направлении записи.
- Правый нижний угол: `HIDE UI` вместо глифа ◐, который читался как инверсия. Подсказки и счётчик кадров переехали в инфо-док слева внизу.

### A5.4 Лава

Дрейф имел ровно одну фактуру: размер ячейки поля и скорость её эволюции были вшиты в `curlAt` константами. Добавлено: `lava scale` (0.15 — пара огромных долей на кадр, 3 — мелкое кипение), `lava speed`, `viscosity` (damping под своим настоящим именем), и **`lag spread`** — разброс инерции ПО ТЕЛАМ ±25%, сеяный по телу. Последнее из ресёрча: одинаковая λ на сотнях тел читается как один жёсткий трансформ, а не как материал.

### A5.5 Приход и уход слоя

`L.life.k` 0→1 на входе (0.8с) и 1→0 на выходе (0.6с). Тела одновременно гаснут и усаживаются — одна прозрачность читается как «выключили», усадка как «ушло». Каскад **от расстояния до центра**, не от индекса: индексный stagger читается как цикл `for`. Бюджет каскада — доля всего движения, поэтому приход длится одинаково при 200 и при 900 телах.

`removeLayer(id)` теперь провожает слой; `removeLayer(id,true)` убирает мгновенно — этот путь у отладочного API, потому что проба спрашивает про состояние, а не про хореографию.

### A5.6 Эхо-туннель

Вся психоделика MilkDrop в двух операциях, которые Canvas умеет нативно: прошлый кадр обратно в себя с масштабом 1.005–1.02 и поворотом, затем затухание. Плюс дрейф тона на несколько градусов за кадр — именно он отличает психоделику от простого хвоста. Числа — дефолты Гейсса.

**Замеренная поправка к фольклору:** затухание через `destination-out` на тёмном фоне **застревает** (при α=0.05 остаток 9 и дальше никогда), обычный `fillRect` доходит до нуля. Советуют обычно первое.

### A5.7 Калибровка

`dance` 0…4→0…2 · `frequency` 0…6→0…2.5 · `sceneSpeed` ±3→±1.5 · `spin` 0…2→0…0.8 · `reach` 20…300→20…180 · `urge` 10…400→10…220 · `fall gravity` 0…1200→0…700 · `mark scale` 1.6→1.15 (измерено: 1.6 давало 108% высоты кадра, обрезало сверху и снизу).

### A5.8 Ресёрч вернулся

Пресет Карла Симса **декодирован** (не угадан): он смешал незатухающую зону типа α с затухающей типа κ, интерполяция кольцами, сильный радиальный градиент размера фичи, emboss выключен. Вывод про вкус: глазу дают стабильную структуру для чтения и рядом зону, которая никогда не разрешается.

Ключевые числа к внедрению: каскад 20–50мс (взято), ζ=0.85 на позицию и 1.0 на цвет, λ 3–13 с⁻¹ с разбросом ±25% (взято), степенное распределение размеров с 4–7 дискретными классами и шагом ×2.7 (у нас непрерывный log — кандидат на квантование), порог «объекты→текстура» ≈1° между центрами ≈48px, рабочее число тел 200–800.

Не взято пока: квантование углов до π/4 как тумблер супрематического регистра, motion vectors слоем, рисование связей вместо тел (приём Риса), sub-frame motion blur.

### A5.9 Открыто

- Логотип как **пустота** (`logoAvoid`) работает; как форма сборки (`mark`) работает и теперь влезает в кадр. Оба режима fill/frame живые.
- Репозиторий на GitHub — вопрос видимости не решён, ждёт Роба.
- Из ресёрча: квантование углов, motion vectors, связи вместо тел.
- `P.sizeScale` читается и никем не объявлен · `cam2.posterize` без контрола · `BASES.delete(r.path)` в рэке не попадает по ключу.

---

## A6. 2026-07-19 · Публикация и хвост ресёрча

**Репозиторий:** https://github.com/robvagin-beep/bryk (публичный, решение Роба)
**Живое превью:** https://robvagin-beep.github.io/bryk/index.html — Pages с ветки `main`, каждый push обновляет. https = защищённый контекст, микрофон и камера работают.

**Гейты:** audio PASS · panel 36/36 · app **74/74**.

### A6.1 Стендбай перетюнен Робом под сглаженный движок

`bryk-scene (3).json`: swirl 0.1 → **0.4**, flock 0 → **0.46**, gravity 0 → 0.04, attract 0.09 → 0.01, look.scale 1.13, камера с наклоном −0.449 и разворотом 0.272 при persp 0.

Важное: он смог поднять дрейф только после того, как фаза стала копиться («у тебя они не дёргаются»). До этого swirl выше 0.1 давал мух, потому что модуляция прыгала фазу. Калибровка и сглаживание — одна и та же работа с разных сторон.

### A6.2 Квантование углов

`angles` на слое: 0 — свободно, 4 — прямые углы, 12 — циферблат. Одно округление `Math.round(rot/q)*q`, но оно переводит то же поле из дрейфа мусора в композицию, где фигуры согласны друг с другом. Гейт: 0 из 200 тел мимо сетки.

### A6.3 Веб связей (приём Риса)

Рисуем не тела, а отношения: линия между парой ближе `linkDist`, прозрачность падает с расстоянием — сеть истончается к краю, а не обрывается кругом. Ходит по той же пространственной сетке, что и столкновения, поэтому цена от плотности, не от популяции. **120 fps на 600 телах** при link 0.9 / reach 0.5.

Это ответ на вопрос «что в reaction-diffusion даёт залипание»: фронты и мембраны без единого шейдера.

### A6.4 Motion vectors и sub-frame blur

`streak` — штрих от прошлой позиции тела к текущей, умноженный на ручку. MilkDrop строит их по сетке, потому что у него нет тел; у нас есть тела и `b.px/py`, так что течение уже в руках. Короче пикселя — не рисуем.

`smear` — N штампов вдоль пути кадра с альфой 1/N и шторкой 0.5 (правило 180°). Быстрые тела перестают читаться как череда штампов. По умолчанию выключено, цена линейна по N. **112 fps при streak 4 + smear 3 на 631 теле.**

### A6.5 Гейт, который мерил не то

`stage is painting` требовал >5000 засвеченных пикселей — порог из времён, когда бут открывался на демо с полосами во весь кадр. Стендбай Роба разрежён намеренно и даёт ~4700, то есть гейт мерил ПЛОТНОСТЬ, называясь проверкой жизни. Порог 1500: пустой экран даёт 0, одинокое тело — сотни, спокойное поле проходит.

### A6.6 Что из ресёрча осталось невзятым

Квантование размеров по 4–7 дискретным классам с шагом ×2.7 (сейчас непрерывный log с геометрическим средним 1) · darken_center под аддитивную подачу · blur в цепочке обратной связи (замерено: `blur(4px)` ≈ 12 обычных drawImage, держать ≤1px или мелкий оффскрин).

---

## A7. 2026-07-19 · Аудит-сверка Fable + логический аудит мод-целей + ФИНАЛЬНЫЙ ПЛАН

**Задание Роба:** аудит всего в контексте BRYK после его настроек; свериться по шейдерам/деформаторам («по плаву, acid»); проверить клей «слева карточки — справа маппинг»; логический аудит бессмысленных целей в выпадашке маппинга; остаток к делегированию; финальный план. Цель-критерий (слова Роба, в канон): **«пульсирует · танцует · обходит друг друга · fluidity · супрематизм · красиво играет перспективой и автоспином · легко»**.

### A7.1 Сверка сделанного (по A4–A6, git и коду)

✅ Слои-EQJ закрыты полностью (A4.1: физика на слой, resolveContacts межслойно) · чувствительность вылечена системно (VMAX A4.2 + накопление фазы A5.2 — Роб подтвердил «не дёргаются» A6.1) · словарь density/scale/spacing/size + малевич-распределение размеров · геометрия кадра починена (A4.6 — кадр был вдвое занижен!) · стендбай = сцена Роба · GitHub + Pages живые · **acid-плав уже в аппе:** эхо-туннель с hue-дрейфом (числа Гейсса) · лава-curl с lag spread (материал, не трансформ) · веб связей (фронты «как RD без шейдера», 120fps/600 тел) · streak+smear · квантование углов = супрематический регистр · GL-пост (chroma). Принцип «по плаву» соблюдён: всё фазонакопительное, всё с ease. §18 (перестройка интерфейса, срезы 0–8) написан и ждёт утверждения.

### A7.2 Клей «карточки ↔ маппинг» — вердикт

Модель правильная: `paramsOf(L)` — единственная точка контакта (w=opacity, count, LOOK, PHYS, ключи программы), рэк адресует фокусный слой, `state.phys` — аксессор на фокус. Два хвоста: (1) ретрофит среза 6 обязателен — правка параметра в карточке должна писать сквозь в базу строки рэка (иначе карточка и рэк разъезжаются на модулируемых путях); (2) `BASES.delete(r.path)` не попадает по ключу `layerId::path` (A5.9) — удаление строки оставляет базу-сироту. Оба — в Волну М.

### A7.3 Логический аудит мод-целей (выпадашка рэка)

Диагноз Роба «вариации бессмысленные» подтверждаю: список = всё из paramsOf подряд, без фильтра и категорий. **Четыре критерия годной мод-цели:** непрерывная · не пересборочная · видима <300мс · музыкально интерпретируема («что услышу — то увижу»).

| Вердикт | Цели |
|---|---|
| **Оставить** (ядро) | w/opacity · size · scale · spacing · dance · sceneSpeed · phys.swirl · attract · follow · fall gravity · lava scale/speed · echo amount/zoom/rot · links · streak · face · cam.persp · cam.zoom · satDrift/hue-оси |
| **Только через gate beat/bar** | count (пересборка) · shuffle/re-deal-события · вызовы формации |
| **Убрать из выпадашки** | phys.radius (дребезг геометрии столкновений) · smear (ступенчатый N — мерцает) · angles (это РЕГИСТР, переключается рукой/приёмом, не огибающей) · blend (дискретный) · seed · всё hard:1 ступенчатое · cam.yaw как fast-мишень (только slow — автоспин и так есть) |
| **Добавить** (не хватает) | L.life.k (приход/уход слоя как мишень драмы) · echo.hue · lag spread |

Плюс **группировка выпадашки по категориям**: MOTION · SPACE · LOOK · FX · CAMERA — вместо плоского списка из ~30 строк. Дефолтный набор строк новой карточки — только из ядра.

### A7.4 Сверка эффект-принципа «acid/плав» — чего ещё нет

Есть (A7.1). Нет: **fluid-сим** (W6, R10 — главный «плав») · **quad-wave деформация** (W4 — FLAG/волна на текстурах) · **волна-осциллограф** · **калейдоскоп** · butterchurn (W7) · RD-шейдер (W8) · liquify/fbm фуллскрин-варп (донор готов, §10.2: снять радиальное затухание) · плоскостное стекло с дисперсией (донор unicorn-scene.json). Все строятся по принципу A7.1: фазонакопление, ease, ручки в рабочей зоне. Влезают в §18-срез-5 (стек эффектов = карточки с TIER).

### A7.5 ФИНАЛЬНЫЙ ПЛАН (порядок волн)

**Волна П · Панель** *(= §18, ждёт утверждения Роба по пяти точкам: срез 0 hueDrift · 2 последовательность зон · 3 раскрытие карточек · 4 марки-в-карточку · 5 карточка эффекта)* — срезы 0→1→2→3→4→5→6→8. Это «логика панелей: добавлять штуки, переключаться».
**Волна М · Маппинг** — курация целей по A7.3 + категории выпадашки + ретрофит карточка→база (срез 6) + фикс BASES.delete + MACRO (срез 7, ждёт W9-черновик) + LEAD с эйсингом (A3.2).
**Волна Э · Эффекты-плав** — fluid-layer (W6) → quad-wave (W4) → волна-осциллограф + калейдоскоп → liquify/fbm фуллскрин → butterchurn (W7) → RD (W8). Каждый = карточка в стеке эффектов среза 5, PRESENCE+blend, audio-мишени из R10.5/R11.
**Волна Ф · Финиш** — квантование размеров 4–7 классов ×2.7 (A6.6) · darken_center · приёмы-редактор из UI · сериализация ассетов в сцены (I4) · мёртвые поля (P.sizeScale, cam2.posterize) · аудио-надёжность туса W3 (CAL, band-селектор, whitening) · прогон на реальном сете = приёмка.

Правила волн прежние (делегат ULTRA): один интегратор, воркеры своими файлами, гейт+коммит на срез, скрин перед сдачей, глаз Роба на флагах. Критерий готовности каждой волны — не гейты, а фраза-цель из шапки A7.

---

## A8. 2026-07-19 (день) · Автономная волна: гейты · маппинг · MACRO · ткань · калейдоскоп

**Задание Роба:** «делай всё в автономном режиме, полностью весь пакет моих правок и того, что нам нужно было туда добавить. Просто автономно вноси всё то, что с 2-3 попыток уже не получается — оставляй на потом на моё рассмотрение».
**Роли:** одна сессия = один владелец `index.html`. Гейт + коммит на каждый срез, скрин перед сдачей.
**Гейты на конец секции:** audio PASS · panel 36/36 · app **84/84** · cards **33/33** · shelf **26/26** (было 78 / 19 / 18, и три из четырёх красными).

### A8.0 🔴 Параллельный писатель — инцидент, нужен твой ответ

В 14:45:38 чужой коммит `54bec1d` («Замеры вместо догадок») забрал мои несохранённые правки `index.html`, `probe/cards.cjs`, `probe/shelf.cjs` вместе со своими по `probe/app.cjs` и скриншотом. Значит на репозитории в этот момент работала **вторая сессия**. Мои правки уцелели, но это ровно тот инцидент, что записан в память как «параллельный писатель молча затирает правки» (BRYK 18.07).

В том коммите гейт по линкам был **ослаблен**: проверка пропускала пары длиннее 0.18 кадра ДО взятия максимума, то есть утверждение «ничего не пересекает пятую часть кадра» не могло провалиться. Переписано (A8.2). Правка про квад-бюджет оттуда же оставлена — она верная.

**Решение за тобой:** одна сессия на репо, или явное разделение файлов. Пока не решено, любой мой прогон может быть затёрт.

### A8.1 Гейт научился спрашивать «двигает ли ползунок картинку»

Две проверки в `shelf.cjs` устарели против твоих же решений 19.07 и мерили свой возраст: карточка больше не разворачивается внутрь строки (настройки в `#focusBody`), ракурс вернулся налево. Переписаны на текущий канон — движок под гейт не правился.

Проверка «waterfall не показывает четыре ключа» заменена общим вопросом ко **всем 39 пресетам**: подвинь каждый объявленный ключ и потребуй, чтобы вывод сдвинулся. Что нашлось:

- **Карточки паттернов показывали `amplitude`/`frequency`/`twist`/`lanes` на всех семнадцати**, а формула читает их подмножеством. Таблица не написана рукой — **выводится на буте из самой формулы** (`patReads`), поэтому новый паттерн получает верную карточку сам, а переставший читаться ключ уходит с панели сам.
- **`manner`** переводился из индекса в имя инлайн в кадровом цикле, и гейту пришлось бы держать вторую копию правила. Перевод стал функцией `behaviourParams`, которую зовут оба.

Гейт трижды поймал **сам себя**, и это стоит запомнить как класс: алиасинг сэмплирования (12 тел при факторе 6; `lanes` 20 ≡ 7 mod 13 — живые ползунки объявлялись мёртвыми) · силы под замер формы · пружина под замер транспорта (у затухания нет точки покоя, есть отношение).

### A8.2 Линк перестал пересекать кадр

Мировой радиус не ограничивает экранную длину: близкая к камере пара проецируется шире, и при persp 0.85 законная пара рисовала 241px поперёк кадра в 888. Ограничение теперь **в пикселях, с затуханием** — обрыв читался бы как мигание при вращении сцены. Замер на худших настройках: 177 при потолке 178.

Гейт держал вторую копию соседского поиска. Теперь **рендер сообщает самую длинную линию, которую положил на стекло**, а гейт её читает. Одна реализация, число про пиксели, которые видит человек.

### A8.3 Переименование и бейдж рэка

`prompt()` заменён на инлайн-поле в строке слоя (EQJ :637-643). `stopPropagation` на keydown здесь не косметика — сцена слушает 1-8, B, H, F, W, и имя, набранное в строке, вызывало сцены и гасило зал. Гейт проверяет именно это.

Бейдж рэка стал кнопкой: клик раскрывает карточку своего слоя и прокручивает к ней.

### A8.4 Волна М · выпадашка маппинга

Четыре критерия годной цели записаны в файл (непрерывная · не пересборочная · видима <300мс · музыкально читаемая), а **вердикт живёт на самом объявлении параметра** (`mod:'no'`), не в боковой таблице — боковая таблица это как разъехались `speed`/`sceneSpeed` и `logo`/`mark`.

Убрано: `angles` (регистр) · `smear` (целое число штампов) · `stroke` (тумблер) · `linkDist` (перевязывает соседей — веб прыгает) · `tint` (пересобирает банк текстур) · `phys.radius` (дребезг контактов).
Добавлено: **эхо-туннель целиком** (четыре ручки самого громкого эффекта не имели входа от музыки) · `lag spread` · `face`.
`count` остался и говорит правду: помечен `mod:'gate'`, и строка при выборе сама встаёт на гейт-кривую.

**Третий близнец найден и снят:** `face` был объявлен и на слое, и глобально; `paramsOf` отдаёт слой первым, глобальный не отвечал ничему. Та же болезнь, что у `size` и `count`.

Гейт держит обе стороны: барьер не пускает в меню, но **не сужает словарь** — сцена и макро дотягиваются до всех 40, меню предлагает 34.

### A8.5 Шов между колонками (ретрофит среза 6)

Скраб карточки звал `p.set(v)` и больше ничего. Если строка рэка гонит тот же путь, кадровый цикл возвращает базу в подвале кадра — правка руки жила одну шестидесятую секунды. И только на назначенных путях, то есть один и тот же контрол работал или нет в зависимости от рэка, и на экране об этом ничего не сказано. Теперь рука **перебазирует**, по слою (`rebase` потащил бы весь стек).

Гейт жмёт на настоящий скраб клавиатурой, а модуляцию читает через `watchDriven`: читать сам параметр бессмысленно, он уже восстановлен в базу. Ровно это чтение когда-то заставило считать камеру удалённой.

### A8.6 Срез 7 · MACRO — ★ решение за тобой

Три ручки вверху левой: **ENERGY · DENSITY · CHAOS**. Макро — не параметр, а **запись**: ставит каждый свой параметр в объявленную точку его собственного диапазона и перебазирует. Пишет **только пока его двигают**, никогда покадрово — иначе он молча перекрывал бы каждый ползунок карточки, который накрывает.

Раскладка объявлена **в долях собственного диапазона** каждого параметра: перекалибровка уносит макро с собой. Верх диапазонов **выведен, а не подобран** — покрытие кадра N·π(S/2)²/(W·H); первая версия гнала 745 тел при калибре 57px = 240% покрытия, сплошная стена. Держим ≈40%: 455 тел при 45px.

Поза трёх фейдеров живёт в сцене и **не переприменяется** при загрузке — значения, которые сцена восстанавливает, уже те, что макро произвели при сохранении.

★ **ФЛАГ:** какой параметр под каким из трёх слов — вкусовое решение, это моё прочтение §17.1. Таблица `MACRO_MAP` — одна правка.

| | что двигает |
|---|---|
| ENERGY | size · dance · collide · attract · streak · cam.persp |
| DENSITY | count · scale · spacing (инверсно) · links · varSize |
| CHAOS | swirl · lag spread · varTilt · flock (инверсно) · follow (инверсно) |

### A8.7 Волна Э · деформация сетки квада (R10.3 закрыт)

Единственный механизм, которого не было для всей тканевой семьи STG. Сетка 5×5 уже лежала с порта перспективы — вся фича это смещение узла до проекции. Волна идёт **по осям поверхности**, поэтому бежит ПО лицу, когда тело поворачивается, и работает на **любой текстуре**: марки, PNG, буква формации, живая камера. Флаг из видео.

Три вещи по дороге, все найдены замером:
- смещения по нормали мало: ткань в лоб — это складки к глазу и от него, круг при волне 0.34 вернулся кругом с вмятиной. Добавлен сдвиг **в плоскости** — сборка ткани вдоль хода волны. Физический член: складка забирает длину.
- инфляция клипа 0.6px отмерена под сетку 4×4; на восьми ячейка вдвое меньше — волосяные швы вернулись. Теперь пропорциональна ячейке.
- **складка дороже плоского квада**: те же 60 тел, 32 fps повёрнутых против 20 согнутых. Согнутое тело билуется **по два** — бюджет это обещание по времени кадра. 40 крупных складок вместо 80, кадр вернулся: 30 против 31.

Квад-путь открывался только по `face`, поэтому волна при heading 0 не делала ничего.

### A8.8 Волна Э · калейдоскоп

Один клин кадра, отражённый вокруг центра. Наше поле уже супрематическое, и радиальная симметрия делает из него орнамент, не добавляя ни одной новой марки.

**Дефект, который скриншот пропустил, а гейт нашёл:** зеркалил ДО того, как задан клин, поэтому отражался и сам клин — каждый нечётный сектор улетал на противоположную сторону поверх чётного, половина кадра оставалась чёрной. Читалось как симметрия (три ровных клина), поэтому пережило просмотр глазами. Клип принадлежит МЕСТУ, отражение — СОДЕРЖИМОМУ.

Клинья читаются с копии в 60%: N секторов это N полнокадровых заливок, при DPR 2 самое дорогое в аппе. Орнамент из повторённых копий — единственное место, где разрешение не читается. Замер: 109 → 74 fps на шести клиньях.

Число секторов **не получает фейдер** в рэке: переход с шести на семь по огибающей это склейка, а не модуляция. Поворот зеркала и подмешивание — получают.

### A8.9 Что НЕ сделано и почему — на твоё рассмотрение

- **LEAD с эйсингом** (A3.2) — не начато. Нужен твой ответ по семантике: лидерство между слоями (эмфаза с кроссфейдом) или тела-вожаки внутри слоя. В A3.2 стоял тот же вопрос и он не закрыт.
- **Стек эффектов как карточки с TIER** (§18 срез 5) — не начато. Это самый крупный оставшийся кусок: `.lyr` + `--z/--op/--bm`, двусторонний пин, POST одним GL-контекстом с FBO-цепью и честной альфой. Волна и калейдоскоп сейчас живут как ручки в LOOK, а не как карточки в стеке. Сознательное решение: сначала механизмы, потом их упаковка — переупаковать готовые эффекты дешевле, чем строить стек под несуществующие.
- **Fluid · осциллограф · liquify · butterchurn · RD** — не начаты, ждут стека.
- **Клавиши сцен 1–4** (A1.3-2) — четыре слота недостижимы, нужен твой выбор раскладки.
- **Мёртвое:** `P.sizeScale` · `cam2.posterize` без контрола.

